# Adversarial review for P16.02 — Define Advisory Observation Triage Artifact

Base branch: agents/p16-01-parse-advisory-observations-and-report-evidence
Branch: agents/p16-02-define-advisory-observation-triage-artifact

You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review**.
Your job is not a general code review. It is a targeted attack on whether this ticket's artifact contract is represented and validated correctly.

## Ticket scope

# P16.02 Define Advisory Observation Triage Artifact

## Outcome

- SOA has a structured advisory-observation triage artifact format.
- The artifact records source report path, ticket id, observation text, disposition, rationale, and optional patch commit or follow-up reference.
- Valid dispositions are exactly `patched`, `rejected`, `deferred`, `already-covered`, and `converted-to-ticket`.
- Non-patched dispositions require a non-empty rationale.
- Malformed artifacts are rejected with clear validation errors.

## Rationale

Red first: `bun test ./tools/delivery/test/p16-02.test.ts` failed because `../advisory-observation-triage` did not exist.
Why this path: added a dedicated delivery-tool artifact module with typed dispositions, strict validation, deterministic JSON read/write helpers, and stable merge semantics keyed by source report, ticket id, and observation text.
Alternative considered: extending the subagent-review ledger was rejected because Phase 16 explicitly keeps runner outcome and operator disposition as separate artifacts.
Deferred: the CLI command that discovers reports and writes this artifact remains in P16.03.
Contract note: ticket metadata was usable as written.

## Files touched

Implementation:
- `tools/delivery/advisory-observation-triage.ts`
- `tools/delivery/orchestrator.ts`

Tests:
- `tools/delivery/test/p16-02.test.ts`

Docs:
- `docs/product/delivery/phase-16/ticket-02-define-advisory-observation-triage-artifact.md`

## Invariants to hold

1. `validateAdvisoryObservationTriageArtifact` must reject any disposition outside exactly `patched`, `rejected`, `deferred`, `already-covered`, and `converted-to-ticket`.
2. Every non-`patched` observation must require a non-empty rationale after validation; empty strings and whitespace-only rationale must not pass.
3. Persisted artifacts must preserve source report path, ticket id, observation text, disposition, rationale, and optional patch/follow-up fields, while writing deterministic JSON with a trailing newline.
4. Re-running triage with the same source report path, ticket id, and observation text must update the existing decision instead of appending a duplicate.

## Attack surfaces to probe

1. `normalizeEntry` validation: can malformed artifact objects, missing strings, whitespace-only required fields, unknown dispositions, or empty non-patched rationales pass validation?
2. `writeAdvisoryObservationTriageArtifact` and `readAdvisoryObservationTriageArtifact`: can JSON round-trip drop required identity fields or produce unstable ordering that would churn future triage artifacts?
3. `mergeAdvisoryObservationTriageEntries`: does update-vs-append behavior use the correct identity key, and can an incoming duplicate leave two decisions for one observation?
4. `tools/delivery/orchestrator.ts` exports: are the new helpers publicly available to later CLI work without forcing consumers to import private implementation paths?
5. `tools/delivery/test/p16-02.test.ts`: do tests cover all five dispositions and the stated malformed-artifact cases strongly enough to catch contract breaks?

### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — probe `schemaVersion: 1`, JSON key preservation, deterministic observation ordering, and whether older/unknown versions are rejected clearly.
2. **CLI flag/arg symmetry** — no CLI flags are added in this ticket; verify no CLI parser/help text change is expected.
3. **Error-class breadth in `catch` blocks** — no `catch` block is added; verify read/write errors are not swallowed.
4. **Defensive layering at module boundaries** — probe public `validate`, `read`, `write`, and `merge` entry points for revalidation of caller-supplied artifacts and entries.
5. **Cross-file atomicity windows** — probe whether write behavior can create directories and write the artifact deterministically; note any partial-write risk only if it violates the ticket contract.
6. **Test-contract strength** — verify tests assert structural artifact shape and behavior rather than only prose; identify missing contract coverage if it can hide a real malformed-artifact acceptance bug.
7. **Doc-vs-code drift in the ticket Rationale** — compare the ticket Rationale with the implemented module and exported surface; surface drift only under Findings for human review.

## Diff context

The diff adds `tools/delivery/advisory-observation-triage.ts` with a schema-versioned artifact type, five allowed dispositions, validation helpers, JSON read/write helpers, deterministic sorting, and merge semantics keyed by source report path + ticket id + observation text. `tools/delivery/orchestrator.ts` re-exports the new helper functions and types. The P16.02 test file exercises round-trip persistence, all five dispositions, unknown disposition rejection, missing rationale rejection for non-patched observations, patched commit SHA preservation, source report/ticket identity preservation, and duplicate update behavior. The ticket doc Rationale was updated with the red failure, implementation path, rejected ledger alternative, and P16.03 deferral.

## Directives

**Scope:** Review only the implementation diff and directly related code paths named above. Do not expand into P16.03 command behavior.

**Advisory-only — no file writes:** Do not create, modify, or delete files. Return prose only.

**Read boundary for delivery docs:** Read the ticket Rationale and contract docs as needed, but do not write delivery docs.

**Coverage mandate:** For each attack surface listed above, report what you probed or why it does not apply.

**Finding discipline:** Report a finding only when code breaks a stated invariant, introduces a demonstrable correctness gap, or the spec permits a real unsafe production bug.

## Required output format

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface: `[probed | N/A — <reason> | blocked — missing-input]` plus what you tried and found.

**Actionable findings**
If none: `None.`

**Findings for human review**
If none: `None.`

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
