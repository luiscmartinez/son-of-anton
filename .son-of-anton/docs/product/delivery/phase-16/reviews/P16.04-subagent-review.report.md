**Invariant results**
1. `[held]` Probed `computeAdvisoryObservationWarnings`: parsed `Advisory Observations` are keyed and emitted as `untriaged_observation` when no triage artifact key matches.
2. `[held]` Probed P16.02 key symmetry: warning keys and artifact keys both use `sourceReportPath + ticketId + observationText`, so matching dispositions suppress only the exact observation.
3. `[held]` Probed `inspectSubagentReviewEvidence`: only `outcome === "clean"` and `terminatedReason === "completed"` rows produce missing/empty report warnings; reconciliation code remains separate.
4. `[held]` Probed `formatCloseoutSummary` and `reconcileReview`: closeout appends warning text, and advisory-only reports still return `{ kind: "clean" }`.

**Surface results**
1. `[probed]` Ledger discovery/parsing: state-provided `subagentRunnerArtifactPath` and top-level `reviewsDirPath` scan are both used. Missing ledger files are skipped; missing clean/completed reports produce suspicious warnings through `inspectSubagentReviewEvidence`.
2. `[probed]` `readTriagedObservationKeys` matching: the key exactly matches P16.02 schema and merge key. I did not find cross-observation suppression.
3. `[probed]` Suspicious evidence integration: warning generation is limited to clean/completed rows and does not touch runner ledger validity or reconciliation outcomes.
4. `[probed]` `formatCloseoutSummary` wiring: summary receives warnings as an argument and appends additive text; no clean-warning precondition exists in the formatter.
5. `[probed]` `reconcileReview` boundary: it still parses only `Actionable findings` for Condition B and ignores `Advisory Observations`.
6. `[probed]` Output stability/schema drift: no persisted schema changes found; warning output is additive text. See actionable finding for malformed artifact/JSON blocking closeout output.
7. `[N/A — no CLI flag or arg parser changed]` Closeout parser remains `--plan` only; no new deliver/closeout flags were introduced.
8. `[probed]` Error-class breadth: missing reports are warnings/skips, but malformed ledger or triage JSON can throw through closeout. See actionable finding.
9. `[probed]` Defensive layering: triage artifact validation is strict; ledger shape is cast, not validated, in the warning helper.
10. `[probed]` Cross-file atomicity: warning computation reads artifacts only and writes nothing; no new multi-file write dependency found.
11. `[probed]` Test-contract strength: tests cover untriaged, fully triaged, missing-report suspicious evidence, closeout formatting, and reconciliation non-blocking. They do not compute-test empty report prose or malformed artifact/ledger behavior.
12. `[probed]` Doc-vs-code drift: ticket rationale accurately describes the helper, closeout surfacing, and unchanged reconciliation semantics.

**Actionable findings**
1. `closeout-stack` can still exit non-zero from advisory warning computation, which violates the “warnings are non-blocking” closeout contract. `runCloseoutStack` calls `computeAdvisoryObservationWarnings` inside the top-level `try` after merge/push/PR cleanup work, and any JSON parse/schema error from `readJson` or `readAdvisoryObservationTriageArtifact` propagates to the catch and returns `1` instead of printing a warning-only closeout summary. Relevant lines: [tools/delivery/closeout-stack.ts:461](/Users/cesar/code/son-of-anton_p16_04/tools/delivery/closeout-stack.ts:461), [tools/delivery/advisory-observation-warnings.ts:60](/Users/cesar/code/son-of-anton_p16_04/tools/delivery/advisory-observation-warnings.ts:60), [tools/delivery/advisory-observation-warnings.ts:130](/Users/cesar/code/son-of-anton_p16_04/tools/delivery/advisory-observation-warnings.ts:130), [tools/delivery/advisory-observation-warnings.ts:192](/Users/cesar/code/son-of-anton_p16_04/tools/delivery/advisory-observation-warnings.ts:192). A corrupted `advisory-observation-triage.json` or malformed ledger should surface as warning/error prose without making closeout fail unless this command is intentionally validating the triage artifact.

**Findings for human review**
Tests do not exercise computed `empty_report` warnings or malformed JSON/schema behavior in closeout. I did not run tests to honor the advisory-only/no-file-writes directive.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: Review completed read-only with one actionable non-blocking semantics finding.
