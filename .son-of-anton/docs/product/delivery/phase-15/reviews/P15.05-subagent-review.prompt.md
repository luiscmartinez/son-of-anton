You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- Immediately before the `spawnSync(bin, args, …)` call in `cli-runner.ts` (inside the runner closure passed to `runSubagentWithFallback`), `emitSubagentInvoked(context.config, worktreePath, state.planKey, subagentTarget.id, runner)` is called with `void`.
- The project root for this emit is `worktreePath` (the ticket's worktree), not `process.cwd()`.
- The `payload` carries `runnerKind` (`'claude-cli'` or `'codex-cli'`) for downstream diagnostics.
- When the fallback runner fires (preferred unavailable, second attempt runs), a second `subagent_invoked` event is appended for the second runner. Each spawn attempt produces one event.
- Setting `codogotchi.enabled: false` suppresses the event for all spawn attempts.
- A write failure does not abort the runner — the spawn proceeds regardless of the emit outcome.

**New exports:**
- `emitSubagentInvoked(config, worktreePath, planKey, ticketId, runnerKind)` added to `soa-event-feed.ts`
- Called via `void emitSubagentInvoked(...)` inside the runner closure in `cli-runner.ts` before `spawnSync`

### Files touched

Implementation:
- `tools/delivery/soa-event-feed.ts`
- `tools/delivery/cli-runner.ts`

Tests:
- `tools/delivery/test/p15-05.test.ts`

### Invariants to hold

1. `emitSubagentInvoked` must write a JSON line to `${worktreePath}/.soa/events.ndjson` with `name === 'subagent_invoked'`, correct `plan_key`, `ticket_id`, and `payload.runnerKind` matching the `runnerKind` argument — and must never write `'skipped'` or any non-runner-kind string to `payload.runnerKind`.
2. When `config.codogotchi.enabled === false`, `emitSubagentInvoked` must not create or write to any file under `worktreePath/.soa/` (the gate must suppress the emit entirely).
3. The `void emitSubagentInvoked(...)` call in `cli-runner.ts` must be inside the runner closure passed to `runSubagentWithFallback` (not outside it), so each spawn attempt — including fallback attempts — independently fires an event with the correct `runner` kind for that attempt.

### Attack surfaces to probe

1. **`emitSubagentInvoked` — `runnerKind` forwarding into payload** — does `payload: { runnerKind }` correctly use the closure-local `runner` variable, not a hoisted or stale outer variable? Can a bad value (e.g., `'skipped'`, `'operator-recorder'`, or the outer `subagentSelection.kind`) ever reach this call instead of the per-attempt `runner` kind?

2. **`void` fire-and-forget on unhandled rejection** — `emitSubagentInvoked` is called with `void` inside a synchronous runner closure. `appendSoaEvent` already has a `try/catch` that swallows errors. Is there any path where a rejection from `emitSubagentInvoked` could escape the `appendSoaEvent` catch and become an unhandled rejection that aborts the process?

3. **Placement: emit inside closure vs. outside closure** — the `void emitSubagentInvoked(...)` must be inside the `(runner) => { ... }` callback passed to `runSubagentWithFallback`, not in the outer scope before the call. If it were outside, only one event would fire even for a two-attempt fallback run. Verify the exact placement relative to the `runSubagentWithFallback(...)` call.

4. **`worktreePath` vs. `cwd` at the call site** — the emit must use `worktreePath` (the ticket worktree directory, `subagentTarget.worktreePath`), not `cwd` (the invoking working directory). Verify the variable name at the actual call site in `cli-runner.ts`.

5. **`state.planKey` and `subagentTarget.id` binding** — are `state.planKey` and `subagentTarget.id` in scope and correct at the call site (same ticket context as the spawn), or could they reference a stale or wrong ticket at the call point?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `emitSubagentInvoked` appends a new `subagent_invoked` event name to the `.soa/events.ndjson` stream. Is there any consumer of this stream or schema version check that enumerates expected event names and would break on seeing an unknown name? Check `soa-event-feed.ts` and any reader code for exhaustive-match patterns. `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

2. **CLI flag/arg symmetry** — no new CLI flags or arguments were added or changed in this diff. `[N/A — no new flags]`.

3. **Error-class breadth in `catch` blocks** — `appendSoaEvent` wraps the entire write in a bare `catch {}` that swallows all errors silently. The new `emitSubagentInvoked` function delegates entirely to `appendSoaEvent` without adding its own catch. Are there any error classes (e.g., `EMFILE` too-many-open-files, non-`Error` thrown objects) that the existing catch might mishandle? `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

4. **Defensive layering at module boundaries** — `emitSubagentInvoked` accepts `runnerKind: 'claude-cli' | 'codex-cli'` typed by TypeScript. At the call site in `cli-runner.ts`, `runner` is typed as `'claude-cli' | 'codex-cli'` within the closure. Is there any runtime path where a different string could be passed (e.g., through a cast, any-typed variable, or future schema change in `SubagentRunnerKind`)? `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

5. **Cross-file atomicity windows** — the event write is a single `fh.write(JSON.stringify(event) + '\n')` followed by `fh.close()`. There is no multi-file atomicity concern. However: if two concurrent spawn attempts ran simultaneously (not the current flow — `spawnSync` is serial), could two concurrent `open(filePath, 'a')` calls interleave writes and corrupt the NDJSON? `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

6. **Test-contract strength** — the tests cover: enabled gate (two runner kinds), fallback two-event scenario, timestamp field, and disabled gate (two suppression scenarios). Do any tests assert structural output shape (e.g., `JSON.parse` passes, `payload` is a plain object) in a way that would catch malformed output? Are the disabled-gate tests exhaustive? `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

7. **Doc-vs-code drift in the ticket Rationale** — the ticket `## Rationale` section contains only the placeholder template text (unfilled, expected for this phase of delivery). No completed rationale claims are present to drift from. Read the ticket outcome section and verify the diff implements exactly what is described. `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

### Diff context

Three files changed:

**`tools/delivery/soa-event-feed.ts`** — added `emitSubagentInvoked`:
```diff
+export async function emitSubagentInvoked(
+  config: ResolvedOrchestratorConfig,
+  worktreePath: string,
+  planKey: string,
+  ticketId: string,
+  runnerKind: 'claude-cli' | 'codex-cli',
+): Promise<void> {
+  await appendSoaEvent(
+    config,
+    worktreePath,
+    buildSoaEventLine('subagent_invoked', {
+      plan_key: planKey,
+      ticket_id: ticketId,
+      payload: { runnerKind },
+    }),
+  );
+}
```

**`tools/delivery/cli-runner.ts`** — import and call site:
```diff
+  emitSubagentInvoked,

...

+                  void emitSubagentInvoked(
+                    context.config,
+                    worktreePath,
+                    state.planKey,
+                    subagentTarget.id,
+                    runner,
+                  );
                   const spawned = spawnSync(bin, args, {
                     cwd: worktreePath,
```

The call is inside the `(runner) => { ... }` closure passed to `runSubagentWithFallback`. The closure is invoked once per runner attempt (preferred, then fallback). `runner` is the closure-local parameter, `worktreePath` is the outer `const worktreePath = subagentTarget.worktreePath` assigned two scopes above. `state.planKey` and `subagentTarget.id` are from the outer `subagent-review` command handler scope.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
