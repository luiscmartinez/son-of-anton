You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**P15.02 — Emit ticket_started + ticket_completed**

Outcome:
- Running `deliver start <ticket>` in a consumer repo with `codogotchi.enabled` appends a `ticket_started` line to `.soa/events.ndjson` with `plan_key` and `ticket_id` matching the started ticket.
- Running `deliver advance` when a ticket transitions to `in_progress` (from any other state) appends a `ticket_started` line.
- Running `deliver advance` when a ticket transitions to `done` (from any other state) appends a `ticket_completed` line.
- An `advance` call that transitions both a previous ticket to `done` and a next ticket to `in_progress` appends both lines, in order: `ticket_completed` first, then `ticket_started`.
- Setting `codogotchi.enabled: false` suppresses both events.
- All emits are best-effort: a write failure does not abort the command or surface as an error to the caller.

Review Focus from ticket:
- Both events use `plan_key` from `state.planKey` and `ticket_id` from the transitioning ticket — not from CLI args.
- The `advance` emit fires from the orchestrator-level state diff, not from individual ticket-update calls.
- Order matters when both transitions happen in one `advance`: `ticket_completed` before `ticket_started`.
- The gate check sits inside `appendSoaEvent`; the call site does not branch on `codogotchi.enabled`.
- No `.soa/` directory is created when the gate is disabled.

Rationale:
Extracted `emitSoaEventsForTransitions(previousState, nextState, config, projectRoot)` as a shared helper covering both `start` and `advance` paths. Both reduce to a state diff: find tickets whose status changed to `done` or `in_progress` and emit the corresponding event. Alternative (inline calls without helper) was rejected to keep the contract explicit and testable.

### Files touched

Implementation:
- tools/delivery/cli-runner.ts

Tests:
- tools/delivery/test/p15-02.test.ts

### Invariants to hold

1. `emitSoaEventsForTransitions(prev, next, config, root)` must write exactly one `ticket_started` NDJSON line when a ticket transitions from any non-`in_progress` status to `in_progress`, with `name === "ticket_started"`, `plan_key === next.planKey`, and `ticket_id === the transitioning ticket's id`.
2. When `config.codogotchi?.enabled === false`, `emitSoaEventsForTransitions` must not create `.soa/` or write any file regardless of what transitions occur.
3. When both ticket A transitions to `done` and ticket B transitions to `in_progress` in a single call, the file must contain `ticket_completed` for A on line 1 and `ticket_started` for B on line 2 — in that order.

### Attack surfaces to probe

1. **`emitSoaEventsForTransitions` — undefined `nextTicket` path**: the loop calls `nextState.tickets.find(t => t.id === previousTicket.id)`. If a ticket in `previousState` is absent from `nextState`, `nextTicket` is `undefined`. The conditions `previousTicket.status !== 'done' && nextTicket?.status === 'done'` and `previousTicket.status !== 'in_progress' && nextTicket?.status === 'in_progress'` both evaluate to `false` via optional chaining — confirm this handles the absent-ticket case without emitting a spurious event or throwing.
2. **`start` case — correct previous state passed**: `emitSoaEventsForTransitions(stateForStart, nextState, ...)` is called with `stateForStart` as previous. When explicit policy flags are provided, `stateForStart` is `{ ...state, runPolicy: ... }` — confirm this does NOT mutate ticket statuses, so the diff correctly reflects `pending → in_progress`.
3. **`advance` case in cook mode**: `applyAdvanceBoundaryMode` calls `startTicket` when `ticketBoundaryMode === 'cook'`, which transitions the next pending ticket to `in_progress` inside `nextState`. Confirm that `emitSoaEventsForTransitions(state, nextState, ...)` (where `nextState` includes both the `done` transition and the next `in_progress` one) captures both transitions in the correct order, and that the cook-mode auto-start doesn't double-emit if the state somehow records both.
4. **`appendSoaEvent` gate check with absent `codogotchi` field**: `config.codogotchi?.enabled === false` uses optional chaining + strict equality. Confirm that when `codogotchi` is `undefined` (not in config), the check is `undefined === false` → `false`, so the event IS written (the default-enabled behavior). Read `resolveOrchestratorConfig` to verify the default is `{ enabled: true }` when the field is absent, and that tests use the explicit form.
5. **Event ordering per ticket within `emitSoaEventsForTransitions`**: the function checks `→ done` and `→ in_progress` independently for each ticket in order. A ticket can't simultaneously be both `done` and `in_progress`, but the loop doesn't short-circuit after finding a `done` transition for the same ticket. Confirm there is no path where a ticket emits two events (e.g., `ticket_completed` then `ticket_started`) for the same transition step.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `emitSoaEventsForTransitions` calls `buildSoaEventLine` which produces `{ name, ts, plan_key?, ticket_id?, payload? }`. The NDJSON line shape is the same as P15.01 defined. No schema version marker is included in the event line itself. Probe whether any consumer of `events.ndjson` could break if the P15.02 emitter changes the field names or omits `plan_key`.
2. **CLI flag/arg symmetry** — no new CLI flags added. `[N/A — no new flags in this diff]`
3. **Error-class breadth in `catch` blocks** — `appendSoaEvent` has a bare `catch {}` block that swallows all errors. This is intentional per the best-effort contract. Probe whether a non-I/O error (e.g., a TypeError from passing `undefined` as `plan_key`) would also be silently swallowed, hiding a programming error.
4. **Defensive layering at module boundaries** — `emitSoaEventsForTransitions` passes `nextState.planKey` and `nextTicket.id` directly to `buildSoaEventLine` without validation. Probe: if `nextState.planKey` is `undefined` or `nextTicket.id` is an empty string, what does `buildSoaEventLine` produce and what does `appendSoaEvent` write?
5. **Cross-file atomicity windows** — `emitSoaEventsForTransitions` is called after `saveState` and `syncStateToPrimaryIfNeeded` but before `emitNotificationWarnings`. If the process is killed between `saveState` and `emitSoaEventsForTransitions`, state is saved but no event is emitted. Probe whether this is an acceptable gap per the best-effort contract, or whether it represents a silent inconsistency.
6. **Test-contract strength** — tests assert on `parsed.name`, `parsed.plan_key`, `parsed.ticket_id` after reading the NDJSON file. They do not assert on the `ts` field type. Probe whether the tests cover the cook-mode dual-transition case (A→done + B→in_progress) correctly, and whether the disabled-gate test verifies both `start` and `advance` paths.
7. **Doc-vs-code drift in the ticket Rationale** — the ticket states "The gate check sits inside `appendSoaEvent`; the call site does not branch on `codogotchi.enabled`." Read the call sites in `cli-runner.ts` and confirm this is true. Also read the Rationale section for any claims about behavior that the diff does not implement.

### Diff context

Key changes in `tools/delivery/cli-runner.ts`:

1. New import at line 152: `import { appendSoaEvent, buildSoaEventLine } from './soa-event-feed';`

2. In the `start` case (after `await saveState(cwd, nextState)`):
```ts
await emitSoaEventsForTransitions(stateForStart, nextState, context.config, cwd);
```

3. In the `advance` case (after `syncStateToPrimaryIfNeeded`):
```ts
await emitSoaEventsForTransitions(state, nextState, context.config, cwd);
```

4. New exported function at bottom of file:
```ts
export async function emitSoaEventsForTransitions(
  previousState: DeliveryState,
  nextState: DeliveryState,
  config: ResolvedOrchestratorConfig,
  projectRoot: string,
): Promise<void> {
  for (const previousTicket of previousState.tickets) {
    const nextTicket = nextState.tickets.find((t) => t.id === previousTicket.id);
    if (previousTicket.status !== 'done' && nextTicket?.status === 'done') {
      await appendSoaEvent(config, projectRoot,
        buildSoaEventLine('ticket_completed', { plan_key: nextState.planKey, ticket_id: nextTicket.id }));
    }
    if (previousTicket.status !== 'in_progress' && nextTicket?.status === 'in_progress') {
      await appendSoaEvent(config, projectRoot,
        buildSoaEventLine('ticket_started', { plan_key: nextState.planKey, ticket_id: nextTicket.id }));
    }
  }
}
```

The `appendSoaEvent` function (from P15.01) gates on `config.codogotchi?.enabled === false`, creates `.soa/` dir, appends NDJSON via file handle in append mode, and swallows all errors.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
