Invariant results

1. [held] `appendReviewGapRecord` appends one JSON object line and preserves prior bytes; I probed missing file, newline-terminated file, and non-newline-terminated file paths.
2. [broken] `validateReviewGapRecord` rejects malformed scalars and unsupported vocabularies, but it allows `kind: "review-reachable"` with `reachability.classification: "spec-gap"` and no evidence/prompt lesson, so a review-reachable record can bypass the evidence gate.
3. [held] The helper remains focused under `tools/delivery/`; `tools/delivery/orchestrator.ts` only barrel-exports helper functions/constants/types and repo search found no CLI, promotion queue, or adversarial-prompt mutation.

Surface results

1. [probed] Append path: `needsLineBoundary` reads existing bytes and `appendFileSync` writes either `record\n` or `\nrecord\n`. Probes confirmed empty/missing, newline-terminated, and no-trailing-newline cases preserve all existing bytes before the appended payload.
2. [probed] Scalar validation: malformed phase, impossible date, bad SHA, missing subject, float/NaN/zero/negative rounds, unsupported kind, and unsupported reachability all reject.
3. [probed] Reachability: `classification === "review-reachable"` requires evidence and prompt lesson, and non-review classifications are not over-constrained. However, the separate `kind` field can still claim `review-reachable` while `reachability.classification` is non-review, bypassing the gate.
4. [probed] Vocabulary exports: helper constants match `docs/template/review-gaps/README.md` classifications: `review-reachable`, `spec-gap`, `qa-gap`, `completeness-gap`.
5. [probed] Barrel export: `tools/delivery/orchestrator.ts` exports only helper surface and types at lines 149-162; no CLI behavior is introduced.
6. [probed] Tests: targeted test passes. It covers valid append, invalid phase, commit provenance, reachability, rounds, kind, review-reachable evidence/prompt, and non-review records without prompt lessons, but misses the cross-field mismatch bug.
7. [probed] Output stability/schema drift: no existing persisted artifact shape is changed; output field order is stable through explicit normalized object construction.
8. [probed] CLI flag/arg symmetry: repo search found no parser/help/dispatcher changes for this helper.
9. [probed] Error-class breadth: no catch blocks were added; validation and append I/O failures propagate.
10. [probed] Defensive layering: `appendReviewGapRecord` revalidates via `validateReviewGapRecord`, but runtime checks trust that `kind` and `reachability.classification` may diverge.
11. [probed] Cross-file atomicity: helper uses one append write for the boundary plus JSON line; it does not rewrite, reorder, or truncate existing ledger bytes. Concurrent appends could still race around the read-before-append boundary, but that is outside this single-helper ticket contract.
12. [probed] Test-contract strength: tests prove the stated minimum cases, but do not cover malformed date, no-trailing-newline append, unsupported classification/kind symmetry, or missing SHA/subject separately.
13. [probed] Doc-vs-code drift: ticket rationale matches the helper’s newline-boundary behavior; template README matches code vocabulary. Broader Phase 19 plan still says `experiential-only` where current scaffold/helper use `qa-gap`.

Actionable findings

`tools/delivery/review-gap-ledger.ts:179`: `kind` and `reachability.classification` are validated independently, so this accepted record bypasses the required evidence gate: `kind: "review-reachable", reachability: { classification: "spec-gap" }`. This breaks invariant 2 because a record claiming review reachability can be written without evidence or prompt lesson. Fix recommendation: enforce `record.kind === record.reachability.classification`, or remove one of the duplicated classification fields; add a test that a `review-reachable` kind cannot be paired with non-review reachability.

Advisory Observations

- `docs/product/plans/phase-19-quality-control-review-gaps.md:67-68` still references `experiential-only`, while `docs/template/review-gaps/README.md:20-23` and `tools/delivery/review-gap-ledger.ts:3-15` use `qa-gap`. This is outside the ticket’s direct implementation contract but is real taxonomy drift.

- Test coverage could be tightened around the already-implemented behavior: malformed real-calendar date rejection, append when the existing ledger lacks a trailing newline, and separate missing SHA versus missing subject. These are not blocking because the implementation passed direct probes.

Runner termination

`runnerStatus`: completed  
`terminatedReason`: Review completed with one ticket-relevant actionable finding and no sandbox or runner interruption.
