You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** -
anything off-scope but real is welcome there.
Your job is not a general code review. It is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Outcome:

- `docs/template/review-gaps/README.md`, `ledger.jsonl`, and `promotion-queue.md` define the consumer scaffold for post-phase review-gap capture.
- `scripts/soa-sync.sh` creates `docs/product/review-gaps/` in consumer repos when missing.
- Sync is idempotent and does not overwrite existing consumer README, ledger, or promotion queue content.
- Tests prove fresh consumer sync creates the scaffold and reruns preserve existing review-gap files.

Rationale notes:

- Red first: sync tests should fail because no review-gap scaffold is copied today.
- Why this path: scaffold sync is the smallest stable foundation for later QC recording.
- Alternative considered: create the scaffold only when `/soa quality-control` first runs; rejected because the issue requires fresh consumers to receive the artifact home after sync.
- Deferred: ledger validation and skill flow belong to later tickets.
- Contract note: none.

### Files touched

Implementation:
- `scripts/soa-sync.sh`
- `docs/template/review-gaps/README.md`
- `docs/template/review-gaps/ledger.jsonl`
- `docs/template/review-gaps/promotion-queue.md`

Tests:
- `tools/delivery/test/p19-01.test.ts`

### Invariants to hold

1. In consumer-repo mode, running `scripts/soa-sync.sh` with `.son-of-anton/docs/template/review-gaps/` present must create `docs/product/review-gaps/README.md`, `ledger.jsonl`, and `promotion-queue.md` when each destination file is missing.
2. Sync must not overwrite any existing consumer `docs/product/review-gaps/*` file; pre-existing ledger content, README content, or promotion queue content must survive reruns byte-for-byte.
3. Source-repo mode must remain unchanged except for skill relinking; it must not create `docs/product/review-gaps/` from the source repo checkout.

### Attack surfaces to probe

Surfaces for this review:
1. `copy_review_gap_scaffold` source/destination path resolution - can consumer mode miss the subtree templates, copy from the wrong source, or create files in the source repo unexpectedly?
2. `copy_review_gap_scaffold` per-file existence guard - can an existing consumer file be overwritten, truncated, or replaced when only some scaffold files are missing?
3. `scripts/soa-sync.sh` call order in the consumer-only block - can migrations, AGENTS/CLAUDE injection, or orchestrator config scaffolding now behave differently or fail before scaffold copying is complete?
4. `tools/delivery/test/p19-01.test.ts` fixture fidelity - does the consumer fixture actually model a subtree checkout with template files under `.son-of-anton/docs/template/review-gaps/`, and do assertions cover both fresh creation and preservation?
5. Template scaffold content - does it introduce Phase 19 vocabulary only, without prematurely implementing ledger validation, prompt promotion, or QC command behavior from later tickets?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** - [probed] Check whether this adds or mutates persisted sync version files, existing migration versioning, or ledger schema semantics. It should add only a new scaffold copy step and an empty append-only `ledger.jsonl` template.
2. **CLI flag/arg symmetry** - [N/A - reason] No CLI flags or arguments are added or changed.
3. **Error-class breadth in `catch` blocks** - [N/A - reason] No new `catch` blocks are added.
4. **Defensive layering at module boundaries** - [probed] The shell helper crosses the boundary from source subtree templates to consumer docs. Check whether missing template directories are handled safely and whether missing individual files can create partial sync failure.
5. **Cross-file atomicity windows** - [probed] The scaffold copy writes three files independently. Check rerun behavior if a prior interrupted sync created only a subset of files.
6. **Test-contract strength** - [probed] The tests should fail pre-implementation, use a consumer fixture, and assert both creation and no-overwrite behavior. Check whether they cover all three files or only the ledger.
7. **Doc-vs-code drift in the ticket Rationale** - [probed] Compare the ticket outcome and rationale against the implementation. Surface drift if the code edits anything beyond scaffold sync or if scaffold copy does not match the documented consumer/source paths.

### Diff context

This ticket adds a new `docs/template/review-gaps/` scaffold containing `README.md`, empty `ledger.jsonl`, and `promotion-queue.md`. It adds `copy_review_gap_scaffold` to `scripts/soa-sync.sh`, using `.son-of-anton/docs/template/review-gaps` as the consumer source and `docs/product/review-gaps` as the destination; the helper creates the destination directory and copies only missing files from the fixed three-file scaffold list. The helper is called only inside the existing `IS_SOURCE_REPO=false` consumer block, after AGENTS/CLAUDE injection and before orchestrator config scaffolding. The new `p19-01` tests copy the template scaffold into a temporary `.son-of-anton` consumer fixture, assert fresh sync creates all three files, and assert pre-existing ledger content survives sync while missing companion files are filled in.

Relevant diff excerpt:

```diff
+copy_review_gap_scaffold() {
+  local source_dir="$REPO_ROOT/.son-of-anton/docs/template/review-gaps"
+  local dest_dir="$REPO_ROOT/docs/product/review-gaps"
+
+  [ -d "$source_dir" ] || return 0
+
+  mkdir -p "$dest_dir"
+  for file in README.md ledger.jsonl promotion-queue.md; do
+    if [ ! -e "$dest_dir/$file" ]; then
+      cp "$source_dir/$file" "$dest_dir/$file"
+      echo "  created: docs/product/review-gaps/$file"
+    fi
+  done
+}
+
+  copy_review_gap_scaffold
```

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only - no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift, surface it under **Advisory Observations**
with the specific file, the conflicting claim, and what the diff actually does. The
primary agent decides whether to patch docs or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

### Required output format

After completing your review, report in this exact structure (prose only - no file edits).
The structure is canonical and machine-parsed by downstream tooling:

**Invariant results**
For each invariant: `[held | broken | untested]` - one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A - <reason> | blocked - missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
