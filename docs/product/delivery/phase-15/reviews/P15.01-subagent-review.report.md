**Invariant results**
1. [broken] `appendSoaEvent` absorbs filesystem failures and circular event serialization, but `config.codogotchi?.enabled` is evaluated before the `try/catch`; `appendSoaEvent(null, ...)` throws `TypeError`.
2. [held] With `codogotchi: { enabled: false }`, the function returns before `mkdirSync`, so no `.soa/` directory or `events.ndjson` file is created.
3. [held] `resolveOrchestratorConfig({}, cwd)` returns `codogotchi: { enabled: true }`.

**Surface results**
1. [probed] `appendSoaEvent` gate condition: `undefined` and `{ enabled: true }` write; `{ enabled: false }` skips. The valid typed cases behave correctly.
2. [probed] `parseCodogotchiConfig`: `enabled: null`, `0`, and `"true"` throw validation errors; `false` and `true` are preserved. Explicit `undefined` defaults to `true` in direct JS object input; JSON config cannot represent it.
3. [probed] `resolveOrchestratorConfig` default: absent `codogotchi` becomes `{ enabled: true }`; parsed object values pass through. Non-object falsy raw values are rejected by `loadOrchestratorConfig` before resolve.
4. [probed] `appendSoaEvent` catch scope: `mkdirSync`, `open`, `write`, `JSON.stringify`, and `close` are inside the catch, but the config gate is outside it. Close failures are swallowed per spec; no retry or double-write path exists in this function.
5. [probed] NDJSON line format: circular event objects are caught and do not throw to the caller. Normal lines are `JSON.stringify(event) + '\n'`.

6. [probed] Output stability across schema-version drift: persisted `DeliveryState` stores `runPolicy`, not the whole resolved config, and searched write paths did not show resolved config serialized verbatim into state/ledger artifacts.
7. [N/A — no new flags; `codogotchi` is config-file only]
8. [probed] Error-class breadth in catch blocks: the writer intentionally swallows broad write/serialization errors and logs nothing. This matches the ticket contract; the only gap is the pre-catch config access.
9. [probed] Defensive layering at module boundaries: runtime-invalid `config` can throw before the catch; invalid/circular event values after the gate are absorbed.
10. [probed] Cross-file atomicity windows: each call opens with append mode and performs one write. For local POSIX-style filesystems this is the expected minimal append pattern; no ticket-current call sites add concurrent emit pressure.
11. [probed] Test-contract strength: tests cover default-enabled resolve and writer behavior, but they do not cover `parseCodogotchiConfig` invalid values or malformed runtime `config`.
12. [probed] Doc-vs-code drift: the ticket Outcome in `docs/product/delivery/phase-15/ticket-01-soa-event-feed-writer-and-codogotchi-config-gate.md` says `appendSoaEvent(projectRoot, event)`, while Green/Rationale and implementation use `appendSoaEvent(config, projectRoot, event)`. The orchestrator export matches the implementation.

**Actionable findings**
- `tools/delivery/soa-event-feed.ts`: `appendSoaEvent` can throw before its swallow-all catch when passed a runtime-invalid `config` such as `null`, because the gate check is outside the `try`. This breaks invariant 1 and the defensive-layering surface. Concrete fix: move the gate check inside the `try` block, or otherwise guard it with runtime-safe object access before returning.

**Findings for human review**
- `docs/product/delivery/phase-15/ticket-01-soa-event-feed-writer-and-codogotchi-config-gate.md`: Outcome line claims `appendSoaEvent(projectRoot, event)`, but Green/Rationale and code use `appendSoaEvent(config, projectRoot, event)`. This is doc-vs-code drift, not a blocking implementation bug because the later ticket text explicitly calls for threading `ResolvedOrchestratorConfig`.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: Review completed against the requested invariants and attack surfaces.
