**Invariant results**
1. `[held]` Probed `emitSoaEventsForTransitions`; `pending → in_progress` emits one `ticket_started` with `plan_key` from `nextState.planKey` and `ticket_id` from the matched next ticket.
2. `[held]` Probed gate placement in `appendSoaEvent`; `config.codogotchi?.enabled === false` returns before `.soa/` creation.
3. `[held]` Probed dual transition path; for normal orchestrator ticket order, the loop emits `ticket_completed` for A before `ticket_started` for B, matching `eventsForAdvanceCommand`.

**Surface results**
1. `[probed]` Undefined `nextTicket`: optional chaining makes both transition checks false. No throw and no spurious event.
2. `[probed]` Start previous state: `stateForStart` only re-stamps `runPolicy`; ticket statuses are unchanged, so `pending → in_progress` remains visible.
3. `[probed]` Advance cook mode: `applyAdvanceBoundaryMode` starts the next pending ticket inside `nextState`, and the single state diff from original `state` to final `nextState` captures both transitions once.
4. `[probed]` Absent `codogotchi`: `appendSoaEvent` writes when the field is absent, and `resolveOrchestratorConfig` defaults it to `{ enabled: true }`.
5. `[probed]` Per-ticket event ordering: a single ticket cannot satisfy both `nextTicket.status === 'done'` and `nextTicket.status === 'in_progress'`; no double-emission path found for one transition.
6. `[probed]` Output stability: `buildSoaEventLine` preserves P15.01 field names `{ name, ts, plan_key, ticket_id, payload }`; P15.02 passes `plan_key` and `ticket_id` for both events.
7. `[N/A — no new flags in this diff]` CLI flag/arg symmetry has no new flag behavior to inspect.
8. `[probed]` Catch breadth: `appendSoaEvent` catches all errors, not just I/O failures. This matches the current best-effort behavior, but would also hide programmer errors if invalid values were passed.
9. `[probed]` Defensive layering: `plan_key` and `ticket_id` are passed without runtime validation. With normal typed `DeliveryState`, both are strings; malformed runtime state could omit `plan_key`, causing `buildSoaEventLine` to omit the field.
10. `[probed]` Atomicity window: events emit after `saveState` and primary sync. A process kill between state save and event append can leave state advanced with no event; this is permitted by best-effort semantics.
11. `[probed]` Test-contract strength: tests cover helper-level start, completion, dual-transition order, and disabled gate for start plus advance. They do not invoke full CLI command paths or assert `ts` in P15.02, though P15.01 covers `ts`.
12. `[probed]` Doc-vs-code drift: ticket rationale and review focus match code. The gate is inside `appendSoaEvent`; call sites do not branch on `codogotchi.enabled`.

**Actionable findings**
None.

**Findings for human review**
P15.02 tests exercise the extracted helper, not full `deliver start` / `deliver advance` command execution. The implementation call sites look correct, but the Outcome is phrased at command level, so this is a coverage gap rather than a demonstrated behavior break.

`appendSoaEvent`’s broad `catch {}` also hides non-I/O programmer errors. That matches the current best-effort posture, but if future debugging needs distinguish write failures from malformed event construction, this will make silent event loss harder to diagnose.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: Review completed by static inspection of the touched implementation, tests, ticket rationale, and directly related config/event code.
