**Invariant results**
1. `[held]` I checked `emitSubagentInvoked` in `tools/delivery/soa-event-feed.ts`: it builds `name: 'subagent_invoked'`, forwards `planKey`/`ticketId`, and sets `payload: { runnerKind }` from the function argument.
2. `[held]` I checked the disabled gate in `appendSoaEvent`: `config.codogotchi?.enabled === false` returns before `mkdirSync`, so the emitter does not create or write under `${worktreePath}/.soa/`.
3. `[held]` I checked `tools/delivery/cli-runner.ts`: `void emitSubagentInvoked(...)` is inside the `(runner) => { ... }` callback passed to `runSubagentWithFallback`, immediately before `spawnSync`, and receives the closure-local `runner`.

**Surface results**
1. `[probed]` `runnerKind` forwarding: the call passes the closure-local `runner`, typed as `'claude-cli' | 'codex-cli'`; it does not pass `subagentSelection.kind`, `usedRunner`, `'skipped'`, or `'operator-recorder'`.
2. `[probed]` `void` fire-and-forget: `appendSoaEvent` wraps gate, mkdir, open, write, and close in a broad `try/catch`; I did not find a normal write-failure path that escapes into an unhandled rejection.
3. `[probed]` Placement: the emit is inside the fallback attempt closure, before `spawnSync`, so fallback attempts independently execute it.
4. `[probed]` `worktreePath` vs `cwd`: the call passes `worktreePath`, defined as `subagentTarget.worktreePath`; `spawnSync` also uses `cwd: worktreePath`.
5. `[probed]` `state.planKey` and `subagentTarget.id`: both are captured from the active `subagent-review` handler scope and match the same target used for prompt resolution, spawn cwd, and artifact paths.
6. `[probed]` Output stability across schema-version drift: I found no in-repo reader that exhaustively rejects unknown `.soa/events.ndjson` event names; docs describe passthrough consumer validation and include `subagent_invoked`.
7. `[N/A — no new flags]` CLI flag/arg symmetry: no new flags or args were added.
8. `[probed]` Error-class breadth: the existing bare `catch {}` catches all thrown/rejected values from the writer path, including non-`Error` throws and I/O errors such as `EMFILE`.
9. `[probed]` Defensive layering: runtime validation is TypeScript-only at the emitter boundary, but the actual call site’s `runner` comes from `runSubagentWithFallback`’s typed attempt parameter and current selection parsing only permits the two spawning runner kinds.
10. `[probed]` Cross-file atomicity windows: current fallback flow is serial because each attempt calls `spawnSync`; concurrent writes are not part of the current path. Each event append is a single `fh.write(JSON.stringify(event) + '\n')`.
11. `[probed]` Test-contract strength: tests parse JSON, assert exact payload object shape for both runner kinds, assert two-line fallback simulation, assert `ts`, and assert disabled suppression. They do not exercise the full CLI closure placement or real fallback path.
12. `[probed]` Doc-vs-code drift: the ticket Rationale is still placeholder-only, so there are no rationale claims to drift from. The ticket Outcome says to call `appendSoaEvent(...)` directly, while implementation wraps it in `emitSubagentInvoked(...)`; behavior matches the stated outcome and the prompt’s “New exports” contract, but the literal implementation detail differs.

**Actionable findings**
None.

**Findings for human review**
The P15.05 tests validate the emitter helper and simulate fallback by calling it twice, but they do not lock the actual `cli-runner.ts` placement before `spawnSync` or prove real `runSubagentWithFallback` emits once per attempted runner. The code currently satisfies that contract, but this regression would not be caught by the present P15.05 tests alone.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: completed the targeted review against the named invariants, attack surfaces, diff files, related runner code, tests, and ticket docs.
