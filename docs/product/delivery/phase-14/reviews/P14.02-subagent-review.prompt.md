```
You are conducting an adversarial review of a code change. Your job is not a general
code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P14.02 — Runner selection, naming, and classification fidelity (5 points, feat).

Outcome (paraphrased):

- Rename `codex-exec` → `codex-cli` across runtime, tests, docs, scaffold. No dual-name fallback.
- New CLI flag `--subagent <claude-cli|codex-cli>` (strict enum) replaces removed `--preferred-runner`. Config field `subagentRunner` is the secondary source. Missing both is a hard error.
- New CLI flag `--primary <free-form>` (config field `primaryAgent`) records executor identity on every ledger row. Default `"unknown"`.
- Codex-cli runner classification trusts a `runnerStatus: <value>` trailer from the model. `runnerSelfReport` is recorded. Authentic rate-limit is structured-signal only (exit code / JSON token); stderr noise resembling rate-limit prose is `completed`, not `skipped`.
- Fallback chain: operator-specified runner first, other on unavailability; row records `runnerKind` (what ran) plus `fallbackFrom: <originally-requested>`. Both unavailable → `skipped` with `fallbackFrom` preserved.
- Consumer scaffold (`scripts/soa-sync.sh` HEREDOC) ships `primaryAgent: "unknown"` but deliberately omits `subagentRunner`, so first `/soa execute` errors out with the documented contract before any review runs.
- `docs/template/delivery/delivery-orchestrator.md` sample JSON shows the new fields plus a precedence paragraph.

Rationale (already written in the ticket file):

- codex-cli authentic-rate-limit predicate is structured-only (exit code 7 OR quoted JSON tokens like `{"error":"rate_limited"}`).
- `runSubagentWithFallback` always preserves the originally-requested kind in `fallbackFrom` on the `failed_all` path so the skipped row is auditable.
- Per-runner authentic-rate-limit predicate is currently codex-cli-only; claude-cli still uses the prose-based `classifyRunnerTermination` heuristic. P14 had no claude-cli mislabel evidence to motivate matching changes. Captured as a deferred follow-up.

### Files touched

Implementation:
- tools/delivery/subagent-runner.ts
- tools/delivery/cli.ts
- tools/delivery/cli-runner.ts
- tools/delivery/config.ts
- scripts/soa-sync.sh

Tests:
- tools/delivery/test/subagent-runner.test.ts
- tools/delivery/test/cli-runner.test.ts (new)
- tools/delivery/test/p7-02.test.ts
- tools/delivery/test/p7-05.test.ts
- tools/delivery/test/p10-01.test.ts
- tools/delivery/test/p10-02.test.ts
- tools/delivery/test/p10-03.test.ts
- tools/delivery/test/p11-01.test.ts
- tools/delivery/test/p13-01.test.ts
- tools/delivery/test/p13-03.test.ts
- tools/delivery/test/p13-04-sidecar-artifacts.test.ts
- tools/delivery/test/ticket-flow.test.ts
- tests/fixtures/legacy-subagent-runner/codogotchi-p1-{01,18,20}.json

Docs / agent prose:
- docs/template/delivery/delivery-orchestrator.md
- docs/template/delivery/son-of-anton.md
- docs/template/delivery/tdd-workflow.md
- docs/template/overview/start-here.md
- docs/product/delivery/phase-14/ticket-02-runner-selection-naming-and-classification-fidelity.md
- README.md, AGENTS.md, AGENTS.soa.md, CLAUDE.soa.md
- .agents/skills/son-of-anton-ethos/SKILL.md

### Invariants to hold

1. `resolveSubagentSelection({ flag: undefined, configField: undefined })` MUST throw an Error whose message mentions both `--subagent` and `subagentRunner`. SoA ships no silent default.
2. `coerceCodexCliClassification({ exitCode: 0, stdout: "<no runnerStatus trailer>", stderr: "you have hit your rate limit" })` MUST return `outcome: 'clean', terminatedReason: 'completed'`. Stderr prose alone never produces `skipped` for codex-cli.
3. `runSubagentWithFallback(requested, attempt)` MUST set `fallbackFrom: requested` whenever the requested runner was unavailable — both on the `'fallback'` row when the other runner succeeded AND on the `'failed_all'` row when both were unavailable.
4. Every ledger row written by `subagent-review` (runner path AND operator-recorder path) MUST include `schemaVersion: 1`, a non-empty `primaryAgent`, and an explicit `runnerSelfReport` field (string or `null`) and explicit `fallbackFrom` field (kind or `null`).

### Attack surfaces to probe

1. `resolveSubagentSelection` in `tools/delivery/subagent-runner.ts` — error-message contents: does the thrown Error mention both `--subagent` AND `subagentRunner`? Does flag take precedence over config?
2. `coerceCodexCliClassification` rate-limit predicate (`isCodexCliAuthenticRateLimit`) — does it accept ONLY structured tokens (exit code 7 or quoted JSON `"rate_limited"|"rate_limit_exceeded"|"RATE_LIMIT(_EXCEEDED)?"` keyed by `error|status|code|type`)? Will prose like `you may have hit your rate limit on prior call` slip through and produce `skipped`?
3. `runSubagentWithFallback` in `subagent-runner.ts` — when both runners are unavailable, is `fallbackFrom` set to the originally-requested kind on the skipped row? When the requested runner succeeds on the first try, is `fallbackFrom` `null`?
4. `cli-runner.ts` runner-loop: does the runner artifact written by `appendInvocationToArtifact` carry `primaryAgent`, `runnerSelfReport`, `fallbackFrom`, and `schemaVersion` on BOTH the programmatic-runner path and the operator-recorder path? (The recorder branch fires when `--subagent` is absent but a `clean|patched <sha>` outcome is positional.)
5. CLI parser in `cli.ts` — does `--preferred-runner <anything>` throw a hard-error pointing at `--subagent` rather than being silently treated as an unknown flag and absorbed into `flags`? Does `--primary --subagent claude-cli` reject (next-token-starts-with `--`)? Does `--primary ""` reject (trim empty)?
6. Consumer scaffold in `scripts/soa-sync.sh` — does the HEREDOC ship `primaryAgent: "unknown"` AND NOT `subagentRunner`, so that first-run `/soa execute` errors out with the contract message? Is the JSON syntactically valid (trailing-comma free)?
7. `runtime-config` plumbing for `subagentRunner` and `primaryAgent`: do these resolved values reach `context.config` so `cli-runner.ts` can read `context.config.subagentRunner` / `context.config.primaryAgent` (i.e., is the `ResolvedOrchestratorConfig` type extended AND the resolver writing them through)?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — Legacy `tests/fixtures/legacy-subagent-runner/codogotchi-p1-{01,18,20}.json` were rewritten to use `codex-cli`. Does this break the legacy-shape test in `p11-01.test.ts`? Does any reader still need to parse the old `codex-exec` literal from committed phase-1[0-3] reviews under `docs/product/delivery/phase-*/reviews/*-subagent-runner.json`? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
2. **CLI flag/arg symmetry** — For both `--subagent` and `--primary`: is the parser, USAGE help text, removal error (`--preferred-runner`), `ParsedCliArgs` type, parser return, downstream consumer in `cli-runner.ts`, and persisted ledger row all updated together? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
3. **Error-class breadth in `catch` blocks** — No new catch blocks were added in this ticket. Surface should be N/A unless missed. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
4. **Defensive layering at module boundaries** — `runSubagentWithFallback` accepts an `attempt` callback. Is the result type properly enforced (so future callers can't accidentally return `{ status: 'ran' }` without `outcome`)? Does `resolvePrimaryAgent` trim whitespace symmetrically for flag and config field? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
5. **Cross-file atomicity windows** — The artifact + sidecar + state.json writes still flow through the existing `commitDeliveryArtifactAndPush` plus `saveState` ordering. Did this ticket alter the ordering? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
6. **Test-contract strength** — Do the new tests in `subagent-runner.test.ts` and `cli-runner.test.ts` assert structural shape (`{ kind, source }`, `fallbackFrom`, `runnerSelfReport`) rather than just prose? Are both the flag-supplied and config-supplied branches covered for `resolveSubagentSelection` / `resolvePrimaryAgent`? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
7. **Doc-vs-code drift in the ticket Rationale** — The Rationale claims `subagentRunner` is "passed through" by `resolveOrchestratorConfig` with no resolved default. Verify line 222 of `config.ts` and the corresponding line in `resolveOrchestratorConfig` actually do this. The Rationale also claims claude-cli classification is unchanged (still prose-based `classifyRunnerTermination`); verify cli-runner.ts only invokes `coerceCodexCliClassification` for `runner === 'codex-cli'`. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

### Diff context

The bulk of the change is a mechanical rename (`codex-exec` → `codex-cli`, `--preferred-runner` → `--subagent`, `preferredRunner` → `subagent`) across `tools/delivery/{subagent-runner,cli,cli-runner}.ts`, the test suite, fixtures, scaffold, and active docs. The substantive additions live in `tools/delivery/subagent-runner.ts`: `resolveSubagentSelection`, `resolvePrimaryAgent`, `coerceCodexCliClassification` (with `parseRunnerStatusTrailer` and `isCodexCliAuthenticRateLimit` helpers), and `runSubagentWithFallback`. `config.ts` adds `subagentRunner` and `primaryAgent` config fields. `cli-runner.ts` swaps the old hand-rolled runner-order loop for `runSubagentWithFallback`, calls `coerceCodexCliClassification` for the codex-cli branch only, and stamps `primaryAgent`/`runnerSelfReport`/`fallbackFrom`/`schemaVersion` on both the programmatic-runner and operator-recorder invocations.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path. Keep added surfaces tied to the ticket behavior; do not turn
this into broad style, cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
```
