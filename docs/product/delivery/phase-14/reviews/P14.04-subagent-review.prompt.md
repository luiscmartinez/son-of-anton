You are conducting an adversarial review of a code change. Your job is not a general
code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P14.04 Artifact triplet rename (refactor; Red: skip).

Outcome:

- The three subagent-review artifact files are written under new names:
  - `*-subagent-adversarial-prompt.md` → `*-subagent-review.prompt.md`
  - `*-subagent-review-outcome.md` → `*-subagent-review.report.md`
  - `*-subagent-runner.json` → `*-subagent-review.ledger.json`
- The orchestrator and CLI write only the new names. No dual-name fallback.
- All consumer-repo-discoverable references to the old triplet names are updated:
  orchestrator writers, file-resolution probes, retrospective templates, agent-facing
  skill prose, README, `docs/template/**` references, `.gitignore` template snippets,
  example/fixture file paths in tests.
- The `runnerKind` field name inside the ledger row is unchanged — only the artifact
  _filename_ changes.
- Existing flow tests are updated to reference new names; no behavior regression.
- Green test target: `bun test` (full suite) passes after the rename sweep.

Rationale (committed in this ticket): single-sweep rename across writers, probes, tests,
docs/template, son-of-anton-ethos skill, plus `git mv` of existing P14.01-P14.03 phase-14
artifact files and lockstep migration of internal path references inside the renamed
`.ledger.json` files and the (gitignored) `state.json`. Pre-Phase-14 ledger artifacts
under `docs/product/delivery/phase-13/reviews/` stay byte-identical per the phase-14 plan
deferral. Historical plan/retrospective/ticket prose that names the old triplet as the
renamed-from value is preserved as narrative.

### Files touched

Implementation:
tools/delivery/cli-runner.ts
tools/delivery/subagent-prompt.ts
tools/delivery/subagent-runner.ts

Tests:
tools/delivery/test/orchestrator.test.ts
tools/delivery/test/p10-02.test.ts
tools/delivery/test/p11-01.test.ts
tools/delivery/test/p11-03.test.ts
tools/delivery/test/p13-01.test.ts
tools/delivery/test/p13-02.test.ts
tools/delivery/test/p13-03.test.ts
tools/delivery/test/p13-04-sidecar-artifacts.test.ts
tools/delivery/test/reconciliation.test.ts
tools/delivery/test/ticket-flow.test.ts

Docs / skills / committed artifacts (renamed and/or content-updated):
.agents/skills/son-of-anton-ethos/SKILL.md
docs/template/delivery/delivery-orchestrator.md
docs/product/delivery/phase-14/ticket-04-artifact-triplet-rename.md
docs/product/delivery/phase-14/reviews/P14.0{1,2,3}-subagent-review.{prompt.md,report.md,ledger.json}  (git-renamed; .ledger.json content also updated to new names internally)

### Invariants to hold

1. After this commit, the orchestrator writer code paths in `tools/delivery/subagent-prompt.ts`, `tools/delivery/subagent-runner.ts`, and `tools/delivery/cli-runner.ts` compose artifact paths whose suffixes are exclusively `-subagent-review.prompt.md`, `-subagent-review.report.md`, and `-subagent-review.ledger.json` — there is no remaining code path (writer or reader) that probes any of the three old suffixes.
2. The exported public symbol names whose semantics describe the new triplet (`SUBAGENT_ADVERSARIAL_PROMPT_SUFFIX`, `SUBAGENT_REVIEW_OUTCOME_SUFFIX`, `isSubagentAdversarialPromptReference`, helpers in `cli-runner.ts`) carry the new path values, and every test exercising these helpers asserts against the new path strings.
3. The `runnerKind` schema-field identifier inside `SubagentRunnerInvocation` is byte-identical to its pre-rename value (only artifact _filenames_ moved; the JSON shape and field names are unchanged).

### Attack surfaces to probe

1. `SUBAGENT_ADVERSARIAL_PROMPT_SUFFIX` constant in `tools/delivery/subagent-prompt.ts` and `SUBAGENT_REVIEW_OUTCOME_SUFFIX` / `isSubagentAdversarialPromptReference` in `tools/delivery/subagent-runner.ts` — silent-fallback / stale-name class. Confirm the constant strings and the `endsWith` check all match the new suffixes exactly and no helper still composes the old form.
2. The four artifact path compositions in `tools/delivery/cli-runner.ts` (around the `record-deferred`, `subagent-review` recorder branch, the `buildPrPostVerifyContext` artifact path, and the `outcomePath` near the PR-body builder) — silent-fallback / mixed-name class. Confirm every `${state.reviewsDirPath}/${ticket.id}-...` site uses the new `.ledger.json` / `.report.md` suffix; a single forgotten site would let `open-pr` write under one name and read under another.
3. Renamed phase-14 ledger files (`P14.0{1,2,3}-subagent-review.ledger.json`) — internal-reference drift class. Each ledger row's `filledPrompt` and `rawOutput` fields are repo-relative paths that previously pointed at the now-renamed sidecar files. Confirm the JSON now points to the new `.prompt.md` / `.report.md` sidecars; a stale pointer would break the artifact triplet's audit trail.
4. `.agents/skills/son-of-anton-ethos/SKILL.md` and `docs/template/delivery/delivery-orchestrator.md` — agent-prose drift class. The skill drives execution behavior for `son-of-anton-ethos`-aware agents; if it still mentions the old triplet anywhere the agent will write/read under the old name and silently desync.
5. Diff-derived attack class — **test-contract strength**: do the updated tests still assert behavior (file existence/content) rather than just the new string literals? A blind sed of test fixtures could pass coverage while testing nothing real if assertions are only on filename matching.
6. Cross-file atomicity at rename boundary: the writers and the `git mv` of the existing P14.01-03 artifacts must agree. If a writer site missed the rename, the next subagent-review or open-pr from a child ticket would resurrect the old name on disk; if a `.ledger.json` internal path missed the rename, `open-pr` reads from the renamed file but follows a stale pointer.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `[probe]` Schema version inside `SubagentRunnerInvocation` is unchanged; the only on-disk change is the filename. Confirm that no field name moved, no field value reshaped, and that pre-Phase-14 fixtures (e.g. `tools/delivery/test/p11-01.test.ts` legacy-shape tests) still parse.
2. **CLI flag/arg symmetry** — `[probe]` This ticket touches no CLI flag definitions; confirm `parseCliArgs` and help text are unchanged in the diff.
3. **Error-class breadth in `catch` blocks** — `[probe]` Confirm no `catch` block was added or modified by the rename sweep; rename-only diffs should not introduce new error-handling logic.
4. **Defensive layering at module boundaries** — `[probe]` New cross-module callees, if any. Rename sweep should add zero new boundaries. Verify nothing was inlined or rebound while editing.
5. **Cross-file atomicity windows** — `[probe]` Already enumerated above (surface 6).
6. **Test-contract strength** — `[probe]` Already enumerated above (surface 5).
7. **Doc-vs-code drift in the ticket Rationale** — `[probe]` Read this ticket's `## Rationale` block (committed in this diff). It claims (a) writers write only the new names; (b) renamed `.ledger.json` internal path refs were migrated in lockstep; (c) historical phase-13 artifacts and historical narrative prose are preserved. Verify the diff matches these claims; surface drift under **Findings for human review** rather than patching the ticket doc.

### Diff context

The diff is dominated by string replacements of three suffixes plus the corresponding `git mv` of nine review artifacts under `docs/product/delivery/phase-14/reviews/`. The renamed `.ledger.json` files are also edited to update their internal `filledPrompt` / `rawOutput` path references. `tools/delivery/subagent-prompt.ts` collapses a two-line constant to one line (prettier reformat). No control flow changes, no new functions, no new tests, no schema field changes.

Reviewer focus: completeness of the sweep (any old suffix surviving in writer/reader/test/active-doc surface is a stop-condition per the implementation plan) and lockstep migration of references inside the renamed `.ledger.json` files. The runtime test suite passing (538 pass / 0 fail) is necessary but not sufficient — a forgotten code path that only fires at runtime in a real consumer-repo orchestrator run would slip through the test suite.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production.

Do not report style, preference, or hypothetical future requirements as blocking findings.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

**Invariant results** — `[held | broken | untested]` per invariant, one-line explanation each.

**Surface results** — `[probed | N/A — reason | blocked — missing-input]` per surface (both ticket-spec-derived and the seven diff-derived classes); one-to-three sentences on what you tried and found when probed.

**Actionable findings** — file/path, what is wrong, which invariant or finding-discipline clause applies, concrete fix. If none: "None."

**Findings for human review** — outside-discipline observations including any doc-vs-code drift in this ticket's Rationale. If none: "None."

**Runner termination** — `runnerStatus`: `completed | rate_limit | sandbox_denied | runner_unavailable`. `terminatedReason`: one short sentence.
