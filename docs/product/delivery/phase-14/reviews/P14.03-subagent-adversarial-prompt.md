```
You are conducting an adversarial review of a code change. Your job is not a general
code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P14.03 — Outcome derivation and PR-open reconciliation (4 points, feat).

Outcome (paraphrased):

- New helper module `tools/delivery/reconciliation.ts` exporting `detectLabeledCommits`, `parseActionableFindings`, `reconcileReview`, `recordDeferred`, `recordAcknowledgment`, `ReconciliationBlockedError`, and stable message constants `RECONCILIATION_BLOCKED_MESSAGE_A` / `_B`.
- New CLI command `reconcile-subagent-review` runs the gate and exits non-zero with the documented Condition A or B message on silent-lie conditions.
- New CLI subcommand `subagent-review record-deferred --reason "<text>"` appends a `deferred` ledger row (empty/whitespace reason rejected).
- New flag `--ack-reconciliation <patched|deferred|clean>` on `open-pr` with companion `--commit` and `--reason` flags. Each variant appends an `operator-recorder` row of the matching outcome.
- The reconciliation gate also runs implicitly inside `open-pr` before publish, so a forgotten explicit `reconcile-subagent-review` invocation cannot let a silent-lie PR ship.
- Patched rows append (do not replace) — multi-commit reconciliation is recorded with the full SHA array in `patches: [...]`.
- `reviewedPaths` is derived from `git diff --name-only <baseBranch>..<reviewedHeadSha>` at gate time. No extra state is added.

### Files touched

Implementation:
- tools/delivery/reconciliation.ts (new)
- tools/delivery/cli.ts (new flags + USAGE text)
- tools/delivery/cli-runner.ts (record-deferred subcommand, reconcile-subagent-review command, --ack-reconciliation handler, runReconciliationGate inside open-pr)

Tests:
- tools/delivery/test/reconciliation.test.ts (new — 20 tests covering all six helpers and both blocking conditions)

Docs:
- docs/product/delivery/phase-14/ticket-03-outcome-derivation-and-pr-open-reconciliation.md (Rationale appended)

### Invariants to hold

1. `RECONCILIATION_BLOCKED_MESSAGE_A` and `RECONCILIATION_BLOCKED_MESSAGE_B` are exported as stable string constants. The Condition A message MUST begin with `reconcile-subagent-review: Condition A —` and the Condition B message MUST begin with `reconcile-subagent-review: Condition B —`. Both MUST list all three resolution paths (amend, record-deferred, --ack-reconciliation).
2. `recordDeferred({ reason: '' })` and `recordDeferred({ reason: '   ' })` MUST throw an Error whose message contains `reason`. `recordAcknowledgment({ variant: 'deferred', reason: '' })` and `recordAcknowledgment({ variant: 'clean', reason: '' })` MUST also throw.
3. `recordAcknowledgment({ variant: 'patched' })` without `commitSha` MUST throw an Error whose message contains `commit`. With a non-empty `commitSha` it MUST append a row whose `patches` array contains the operator-supplied SHA verbatim — without re-running `detectLabeledCommits` (squash-merge handling).
4. `reconcileReview` MUST return `{ kind: 'patched', commitShas: [...] }` enumerating ALL labeled commits whose changes intersect `reviewedPaths` (not just the first or last). When two `[subagent-review]` commits each touch a reviewed file, both SHAs appear in the returned array in commit order.

### Attack surfaces to probe

1. `parseActionableFindings` heading parser — does it correctly handle the section being the LAST section in the markdown (no trailing `**` heading)? Does it reject "no findings" prose phrased without exactly `None.`? What about `None` (no period) or `None.\nSomething else`? Verify the heading regex and the normalization step.
2. `detectLabeledCommits` regex `/\[subagent-review\]/i` — does it match `[Subagent-Review]` (case)? Does it falsely match nested brackets like `chore: rename [foo][subagent-review][bar]`? Does it filter labeled commits whose change-set has NO intersection with `reviewedPaths`?
3. `reconcileReview` precedence — when BOTH a labeled commit AND actionable findings exist, does the function return `patched` (the labeled commits take precedence)? Verify the function does not block on Condition B when labeled commits have already been detected.
4. `reconcileReview` deferred-row scope — does the `hasDeferredRowForSha` lookup require the deferred row's `reviewedHeadSha` to MATCH the current `reviewedHeadSha`? A deferred row for an older review should NOT bypass the gate for a fresh review SHA.
5. `runReconciliationGate` in `cli-runner.ts` — when the only invocation row is `outcome: skipped`, does the gate correctly return without applying reconciliation? When no artifact exists at all, does the gate fall through (the existing open-pr `requires_runner_review` gate handles that case)?
6. `--ack-reconciliation patched` appending — does the appended row include `schemaVersion: 1`, `primaryAgent`, and the explicit `runnerSelfReport: null` / `fallbackFrom: null` fields required by P14.01's ledger schema?
7. `subagent-review record-deferred` ticket lookup — when no ticket is at `subagent_review_complete` (e.g. all are `verified` or already `in_review`), does the command emit a clear error rather than silently writing to the wrong ticket's artifact?
8. The `git log --pretty=%H%x09%s` parser in `gitListCommitsInRange` — does it correctly split on a TAB even when the commit subject itself contains a tab character? (The parser uses `split('\t')` and joins all remaining parts back with `\t` — verify this round-trips.)

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — Reconciliation appends new row variants (`deferred`, `patched` from operator-recorder, `clean` with `acknowledgment` field). Does the existing `validateRunnerArtifact` in `subagent-runner.ts` accept these rows? The validator was extended in P14.01 to accept all four outcomes; verify it tolerates the extra `acknowledgment` and `reason` fields without rejecting the artifact. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
2. **CLI flag/arg symmetry** — `--ack-reconciliation`, `--commit`, `--reason`: are all three parsed in `cli.ts`, exposed on `ParsedCliArgs`, documented in USAGE, and consumed in `cli-runner.ts`? Are missing-value cases all caught with named-flag errors? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
3. **Error-class breadth in `catch` blocks** — The `try { ... } catch { /* ignore */ }` blocks inside `loadReconciliationContext` swallow JSON-parse failures silently. Is that the intended behavior, or could it mask a genuinely broken artifact file? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
4. **Defensive layering at module boundaries** — `reconciliation.ts` uses dependency-injected git adapters. Does the CLI glue (`gitListCommitsInRange`, `gitListChangedPaths`, `gitListCommitFiles`) handle the case where `spawnSync` fails (e.g. ENOENT for `git`)? Empty stdout would currently be treated as "no commits / no files" — is that safe? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
5. **Cross-file atomicity windows** — `runReconciliationGate` appends a `patched` row to the artifact, then calls `commitDeliveryArtifactAndPush`. If the commit-and-push fails after the file write, the local artifact and the branch diverge. Does the next reconciliation see the orphaned row? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
6. **Test-contract strength** — Do the 20 new tests assert structural shape (`{ kind, condition, message }`, `{ kind, commitShas }`) AND the exported message constants by reference (not by re-typed string)? Both omitted-hook and supplied-hook paths covered for the DI? Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
7. **Doc-vs-code drift in the ticket Rationale** — The Rationale claims `reviewedPaths` is derived from `git diff --name-only <baseBranch>..<reviewedHeadSha>`. Verify `loadReconciliationContext` in `cli-runner.ts` actually computes this from `ticket.baseBranch`. The Rationale also claims `runReconciliationGate` runs inside `open-pr` — verify the `case 'open-pr'` branch calls it before `openPullRequest`. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

### Diff context

The bulk of the change is the new `tools/delivery/reconciliation.ts` module (≈270 lines) containing pure DI-friendly helpers. `cli.ts` adds three new flags (`--ack-reconciliation`, `--commit`, `--reason`) and updates USAGE. `cli-runner.ts` gains: a `record-deferred` subcommand inside the `subagent-review` case, a new top-level `reconcile-subagent-review` case, an `--ack-reconciliation` handler in the `open-pr` case, the `runReconciliationGate` call inside `open-pr` before publish, and the supporting `gitList*` adapters plus `loadReconciliationContext` / `runReconciliationGate` / `runReconcileSubagentReview` / `runAckReconciliation` helpers at the bottom of the file. The new test file `reconciliation.test.ts` exercises every helper end-to-end with mocked git adapters and freshly-written temp artifacts.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path. Keep added surfaces tied to the ticket behavior; do not turn
this into broad style, cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
```
