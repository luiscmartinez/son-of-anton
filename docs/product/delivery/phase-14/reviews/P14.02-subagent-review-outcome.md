stdout:
All tests pass. Now I have enough to write the full review.

---

**Invariant results**

**Invariant 1** — `[held]` — `resolveSubagentSelection({ flag: undefined, configField: undefined })` throws. The error message at `subagent-runner.ts:128–131` reads: `"No subagent selected. Pass --subagent <claude-cli|codex-cli> or set \`subagentRunner\` in orchestrator.config.json."` Both `--subagent` and `subagentRunner` are present.

**Invariant 2** — `[held]` — Traced manually. With `exitCode: 0`, no structured JSON rate-limit token, and stderr prose `"you have hit your rate limit"`: `isCodexCliAuthenticRateLimit` returns false (exitCode ≠ 7, blob contains no `"key":"value"` JSON token), `runnerSelfReport` is null (no `runnerStatus:` trailer), `exitCode !== 0` is false, output is non-empty → returns `{ outcome: 'clean', terminatedReason: 'completed', runnerSelfReport: null }`.

**Invariant 3** — `[held]` — `runSubagentWithFallback` (`subagent-runner.ts:282–288`): when both runners are unavailable, the function returns `{ fallbackFrom: requested, fallbackLevel: 'failed_all' }`. When the requested runner succeeds on index 0, `fallbackFrom` is `index === 0 ? null : requested` = `null`. Both paths verified in tests that pass.

**Invariant 4** — `[held]** — Both write paths in `cli-runner.ts` explicitly pass `schemaVersion: SUBAGENT_LEDGER_SCHEMA_VERSION`, `primaryAgent`, `runnerSelfReport`, and `fallbackFrom` to `buildRunnerInvocation`. The operator-recorder path (line 698–709) passes all four; the programmatic-runner path (line 940–949) passes all four. `buildRunnerInvocation` conditionally spreads only when the option is not `undefined` — since all four are passed explicitly (including `null` values for `runnerSelfReport` and `fallbackFrom`), they all land in the invocation object.

---

**Surface results**

**1. `resolveSubagentSelection` error message** — `[probed]` — Error at `subagent-runner.ts:128` names both `--subagent` and `subagentRunner`. Flag takes strict precedence: `if (input.flag)` is checked before `if (input.configField)`. Clean.

**2. `coerceCodexCliClassification` rate-limit predicate** — `[probed]` — `isCodexCliAuthenticRateLimit` (`subagent-runner.ts:226–236`) accepts exit code 7 OR quoted JSON matching `/"(?:error|status|code|type)"\s*:\s*"(?:rate_limited|rate_limit_exceeded|RATE_LIMIT(?:_EXCEEDED)?)"`. The regex requires both key and value to be quoted, so bare prose like `you may have hit your rate limit` does not match. Verified via Invariant 2 trace.

**3. `runSubagentWithFallback` `fallbackFrom` preservation** — `[probed]` — `failed_all` path sets `fallbackFrom: requested` unconditionally (line 287). The `'fallback'` row returns `fallbackFrom: requested` when `index === 1`. The first-try success path returns `fallbackFrom: null`. All three cases have passing test coverage in `subagent-runner.test.ts`.

**4. Ledger rows carry required P14.02 fields on both paths** — `[probed]** — Runner path (`cli-runner.ts:940`) and operator-recorder path (`cli-runner.ts:698`) both explicitly pass `schemaVersion`, `primaryAgent`, `runnerSelfReport`, and `fallbackFrom`. The doc-only auto-skip path (line 622) writes no invocation row, so the invariant vacuously holds there.

**5. CLI parser — `--preferred-runner` hard error, `--primary` next-token-starts-with-`--`, `--primary ""`** — `[probed]` — `--preferred-runner` triggers a hard `throw` at `cli.ts:254–258`. `--primary` rejects `raw.startsWith('--')` (line 243) and `raw.trim() === ''` (line 243). `--subagent` rejects `raw.startsWith('--')` (line 228). Tests in `cli-runner.test.ts` cover all three. Clean.

**6. Consumer scaffold in `scripts/soa-sync.sh`** — `[probed]` — HEREDOC (lines 224–238 of soa-sync.sh) emits `"primaryAgent": "unknown"` as the final key with no trailing comma. `subagentRunner` is absent. JSON is syntactically valid (no trailing commas, balanced braces). First-run `subagent-review` will hit `resolveSubagentSelection` with both inputs undefined and throw the contract error. ✓

**7. `runtime-config` plumbing for `subagentRunner` and `primaryAgent`** — `[probed]` — `config.ts:221–222` passes both through in `resolveOrchestratorConfig` with no resolved default (undefined when absent). `ResolvedOrchestratorConfig` type (`config.ts:51–61`) declares both as optional fields. `cli-runner.ts:753–758` reads `context.config.subagentRunner` and `context.config.primaryAgent` as the `configField` arguments to both resolvers. ✓

---

**Diff-derived surface results**

**1. Output stability across schema-version drift (legacy fixtures)** — `[probed]` — The three fixtures in `tests/fixtures/legacy-subagent-runner/` were updated from `codex-exec` to `codex-cli`. The p11-01 test passes (confirmed by test run). However: committed structured-format artifacts `docs/product/delivery/phase-12/reviews/P12.01-subagent-runner.json` and `docs/product/delivery/phase-13/reviews/P13.01-subagent-runner.json` still contain `"runnerKind": "codex-exec"`. These are the new `invocations[]` array format (not legacy flat), so `isLegacyShape` returns false and `validateRunnerArtifact` validates `runnerKind` against `VALID_RUNNER_KINDS`. `codex-exec` is no longer in that list, so `validateRunnerArtifact` returns `null` and `readSubagentRunnerArtifact` throws. No test currently exercises reading those specific files, so tests pass — but any runtime path that loads them would fail. See **Actionable findings**.

**2. CLI flag/arg symmetry** — `[probed]` — `--subagent` and `--primary` are both present in: `ParsedCliArgs` type (`cli.ts:25–26`), the parser (`cli.ts:223–251`), USAGE text (`cli.ts:109, 126`), `cli-runner.ts` downstream consumer (lines 752–758 and 693–694), and persisted ledger rows. `--preferred-runner` hard-errors with message pointing at `--subagent`. Complete.

**3. Error-class breadth in `catch` blocks** — `[N/A — no new catch blocks were added in this ticket]`

**4. Defensive layering at module boundaries** — `[probed]` — `RunnerAttemptResult` type (`subagent-runner.ts:94–102`) has a discriminated union: `'ran'` requires `outcome` and `terminatedReason`, `'unavailable'` and `'timeout'` have no extra fields. Callers using wrong shape get a TypeScript compile error. `resolvePrimaryAgent` trims whitespace symmetrically for both flag and config inputs (`subagent-runner.ts:143, 148`). Clean.

**5. Cross-file atomicity windows** — `[probed]` — The commit ordering in `cli-runner.ts` for the runner path is: (1) `appendInvocationToArtifact` (writes JSON), (2) `recordSubagentReview` (state update), (3) `commitDeliveryArtifactAndPush` (git commit + push), (4) `saveState`. This ordering matches the pre-existing pattern and was not altered by this ticket. Clean.

**6. Test-contract strength** — `[probed]` — `subagent-runner.test.ts` asserts structural shape (`{ kind, source }`, `fallbackFrom`, `runnerSelfReport`, `fallbackLevel`), not just prose. Both flag-supplied and config-supplied branches are covered for `resolveSubagentSelection` and `resolvePrimaryAgent`. The `runSubagentWithFallback` suite covers all three result buckets (preferred, fallback, failed_all). Clean.

**7. Doc-vs-code drift in the ticket Rationale** — `[probed]` — (a) Rationale claims `subagentRunner` is passed through by `resolveOrchestratorConfig` with no resolved default: confirmed at `config.ts:221` (`subagentRunner: raw.subagentRunner`). ✓ (b) Rationale claims claude-cli still uses prose-based `classifyRunnerTermination`: confirmed in `cli-runner.ts:841` — `coerceCodexCliClassification` is called only when `runner === 'codex-cli'`; the claude-cli branch returns a plain `SpawnResult` without `terminatedReason`, so `tryRunner` falls through to `classifyRunnerTermination`. ✓

---

**Actionable findings**

**Finding 1**

- **Files:** `docs/product/delivery/phase-12/reviews/P12.01-subagent-runner.json` and `docs/product/delivery/phase-13/reviews/P13.01-subagent-runner.json`
- **What is wrong:** Both artifacts are in the new structured `{ ticket, invocations[] }` format and contain `"runnerKind": "codex-exec"`. Since `codex-exec` was removed from `VALID_RUNNER_KINDS`, `validateRunnerArtifact` returns `null` for these files and `readSubagentRunnerArtifact` throws a hard error. Any runtime path that loads either artifact (e.g., `open-pr` for a state that references these paths, or a future audit command) fails with an unhandled exception.
- **Clause:** Finding-discipline clause 2 — demonstrable correctness gap.
- **Fix:** Update `"runnerKind": "codex-exec"` → `"runnerKind": "codex-cli"` in both JSON files. This is the same mechanical rename applied to the three test fixtures. The rename is data-only and requires no logic change.

---

**Findings for human review**

None beyond Finding 1 above, which is actionable.

---

**Runner termination**

`runnerStatus`: `completed`
`terminatedReason`: Review completed in full per template — all invariants probed, all attack surfaces reported, one actionable finding with a concrete fix.
