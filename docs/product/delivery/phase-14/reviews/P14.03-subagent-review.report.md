stdout:
I have enough context to write the full review. Here is the adversarial report:

---

**Invariant results**

**Invariant 1** — `[held]` — Both message constants begin with the required prefixes (`reconcile-subagent-review: Condition A —` and `reconcile-subagent-review: Condition B —`) and all three resolution paths (amend, record-deferred, --ack-reconciliation) appear in both messages. Verified against `reconciliation.ts:22-36`.

**Invariant 2** — `[broken — partially]` — `recordDeferred({ reason: '' })`, `recordDeferred({ reason: '   ' })`, and `recordAcknowledgment({ variant: 'deferred', reason: '' })` all throw with messages containing "reason" (`reconciliation.ts:187-190`, `249-252`). `recordAcknowledgment({ variant: 'clean', reason: '' })` also throws correctly (`reconciliation.ts:261-265`). However, the test suite covers the `deferred` case (`reconciliation.test.ts:359-371`) but contains no test for `recordAcknowledgment({ variant: 'clean', reason: '' })`. The implementation holds the invariant; the test contract does not fully assert it.

**Invariant 3** — `[held]` — `recordAcknowledgment({ variant: 'patched' })` without `commitSha` throws "…requires --commit…" (`reconciliation.ts:234-238`). With a non-empty SHA, the appended row carries `patches: [input.commitSha.trim()]` and does not call `detectLabeledCommits` (`reconciliation.ts:239-244`).

**Invariant 4** — `[held]` — `detectLabeledCommits` iterates all commits and appends every qualifying SHA; there is no early-return after the first match (`reconciliation.ts:64-71`). The `reconcileReview` test at line 192-201 confirms both `fix1` and `fix2` appear in the returned array.

---

**Surface results**

**1. `parseActionableFindings` last-section and edge cases** — `[probed]` — The regex lookahead `(?=\n\s*\*\*[A-Za-z]|$)` uses `$` without the `m` flag, so it matches end-of-string when the section is last; the lazy `[\s\S]*?` captures all remaining content correctly. `None` (no period) is treated as no-findings because `\.?` makes the period optional — this is a deliberate leniency, not a bug. `None.\nSomething else` normalizes to a multi-line string that does not match the anchored `/^none\.?$/i` test and correctly returns `true`. The "missing section" path returns `false`. No issue found.

**2. `detectLabeledCommits` regex edge cases** — `[probed]` — `/\[subagent-review\]/i` matches `[Subagent-Review]` (case-insensitive). Nested brackets like `[foo][subagent-review][bar]` also match — this is a potential false positive but the label is unusual enough that it is an acceptable known trade-off. Commits labeled but not touching `reviewedPaths` are correctly filtered by the `reviewedSet.has(f)` check (`reconciliation.ts:67`). No blocking issue.

**3. `reconcileReview` precedence (patched vs. Condition B)** — `[probed]` — `detectLabeledCommits` is called and its result checked first (`reconciliation.ts:118-127`). If `labeledShas.length > 0`, the function returns `{ kind: 'patched' }` before reaching the Condition B check at line 150. Precedence is correct.

**4. `reconcileReview` deferred-row SHA scope** — `[probed]` — The `hasDeferredRowForSha` check at `reconciliation.ts:129-133` requires both `outcome === 'deferred'` AND `reviewedHeadSha === input.reviewedHeadSha`. A deferred row from an older review with a different SHA does not bypass the gate. Correct.

**5. `runReconciliationGate` with only-skipped rows and missing artifact** — `[probed]` — The `hasNonSkipped` guard at `cli-runner.ts:2313` returns early when all rows are `skipped`. The outer `try/catch` at `cli-runner.ts:2303-2310` returns early (without error) when `loadReconciliationContext` throws, which covers both the no-artifact and no-`reviewedHeadSha` cases. Both paths confirmed.

**6. `--ack-reconciliation patched` row schema fields** — `[probed]` — The implicit patched row appended by `runReconciliationGate` (`cli-runner.ts:2339-2352`) includes `schemaVersion: SUBAGENT_LEDGER_SCHEMA_VERSION` (which equals `1`, matching `SCHEMA_VERSION` in `reconciliation.ts`), `primaryAgent: 'unknown'`, `runnerSelfReport: null`, and `fallbackFrom: null`. All required schema fields are present. Note: `primaryAgent` is hardcoded to `'unknown'` rather than resolved via `resolvePrimaryAgent` — this is a gate-auto-triggered path so there is no operator-supplied `--primary` flag available; acceptable by design.

**7. `subagent-review record-deferred` ticket lookup on wrong status** — `[probed]` — At `cli-runner.ts:608-614`, when no ticket is at `subagent_review_complete` and no explicit ticket ID is passed, `deferTarget` is `undefined` and the command throws `"record-deferred requires a ticket at subagent_review_complete (or pass an explicit ticket id)."` — a clear error, no silent write. However, note that `loadReconciliationContext` (`cli-runner.ts:2234-2236`) does fall back to `verified` and `in_review` statuses while the `record-deferred` command does not. This asymmetry means `record-deferred` rejects a `verified` ticket without an explicit ID even though the gate would accept it. This is a usability gap but not a correctness bug.

**8. `gitListCommitsInRange` tab in commit subject** — `[probed]` — `cli-runner.ts:2202-2203`: `const [sha, ...rest] = line.split('\t'); return { sha: sha ?? '', subject: rest.join('\t') }`. If the subject contains a tab, `rest` has multiple segments and `rest.join('\t')` reconstructs the original. This round-trips correctly.

---

**Diff-derived attack surfaces**

**1. `validateRunnerArtifact` compatibility with new row variants** — `[probed]` — `VALID_OUTCOMES` includes `'deferred'` (already present from P14.01). `VALID_RUNNER_KINDS` includes `'operator-recorder'`. The new `acknowledgment` and `reason` fields on `clean` rows are unrecognized by the validator and are silently dropped when rows pass through `readSubagentRunnerArtifact`. The reconciliation gate reads artifacts via raw `JSON.parse` (`cli-runner.ts:2251-2260`), not via `validateRunnerArtifact`, so the gate itself is unaffected. Code paths that go through the validator (poll-review, record-review display) would lose `acknowledgment` and `reason` from operator-recorder `clean` rows. This is a silent lossy round-trip for downstream readers.

**2. CLI flag/arg symmetry** — `[probed]` — All three flags (`--ack-reconciliation`, `--commit`, `--reason`) are parsed in `cli.ts:306-341`, present on `ParsedCliArgs`, documented in USAGE at `cli.ts:115,130-132`, and consumed in `cli-runner.ts:1063-1064,2385-2424`. Missing-value errors are all named. Symmetry is complete.

**3. Error-class breadth in `catch` blocks** — `[probed]` — The `try/catch { /* ignore */ }` blocks in `record-deferred` (`cli-runner.ts:627-634`) and `loadReconciliationContext` (`cli-runner.ts:2258-2260`) swallow JSON parse failures. In `loadReconciliationContext` this causes `rows` to remain `[]` and `reviewedHeadSha` to remain `''`, which then triggers the explicit `"No reviewedHeadSha recorded"` error at line 2263 — a misleading but still visible error for a corrupt artifact. In `record-deferred`, a corrupt artifact silently causes `reviewedHead` to remain `'unknown'` and the deferred row is written with `reviewedHeadSha: 'unknown'`. A later reconciliation gate would then find no deferred row matching the real SHA, leaving the gate open when it should potentially be satisfied. This is a correctness risk under a corrupt-artifact scenario.

**4. Defensive layering — `spawnSync` ENOENT handling** — `[probed]` — `gitListChangedPaths` and `gitListCommitsInRange` use `r.stdout ?? ''` to handle null stdout, so a failing `git` command returns an empty list. This would cause `reviewedPathTouched = false` and `labeledShas = []`, making the gate return `clean` when git is unavailable — a false-negative silent pass. The `headSha` assignment at `cli-runner.ts:2267-2270` uses `.stdout.trim()` directly without the `?? ''` guard; if `spawnSync` fails (ENOENT), `stdout` is `null` and `.trim()` throws `TypeError: Cannot read properties of null`. This crashes the gate before the false-negative path is reached, which is actually safer than silent pass — but the crash message is undiagnostic.

**5. Cross-file atomicity windows** — `[probed]` — `runReconciliationGate` writes the artifact (`cli-runner.ts:2353-2357`) and then calls `commitDeliveryArtifactAndPush` (`cli-runner.ts:2358-2366`). If the push fails after the write, the local artifact already carries the `patched` row. On the next `open-pr` invocation the gate re-runs: `loadReconciliationContext` reads the file, finds the `patched` row as the last row, uses its `reviewedHeadSha`, then `detectLabeledCommits` finds the same labeled commits again and the gate appends another `patched` row for the same SHAs. There is no dedup guard. Repeated push failures accumulate redundant `patched` rows indefinitely.

**6. Test-contract strength** — `[probed]` — Tests assert structural shape (`kind`, `condition`, `message`, `commitShas`) and use the exported constants by reference (`RM.RECONCILIATION_BLOCKED_MESSAGE_A/B` at lines 215,228). Both mock-DI paths (commit subjects, commit files, changed paths) are exercised. Gap: `recordAcknowledgment({ variant: 'clean', reason: '' })` throwing is not tested (see Invariant 2 above).

**7. Doc-vs-code drift in ticket Rationale** — `[probed]` — The Rationale claims `reviewedPaths` from `git diff --name-only <baseBranch>..<reviewedHeadSha>`. `loadReconciliationContext` at `cli-runner.ts:2273-2277` calls `gitListChangedPaths(ticket.worktreePath, ticket.baseBranch, reviewedHeadSha)` which runs `git diff --name-only ${from}..${to}` — matches exactly. The Rationale claims `runReconciliationGate` runs inside `open-pr` before publish. `cli-runner.ts:1067` is the gate call; `cli-runner.ts:1068` is `openPullRequest` — gate runs first. No drift found.

---

**Actionable findings**

**Finding 1 — Missing test for `recordAcknowledgment({ variant: 'clean', reason: '' })` throw**
File: `tools/delivery/test/reconciliation.test.ts`
Invariant 2 specifies this must throw. The implementation does throw. The test file covers `deferred` empty-reason but omits the parallel `clean` empty-reason test. This leaves the invariant partially unverified by the test suite.
Fix: add a test in the `recordAcknowledgment` describe block mirroring the `deferred` empty-reason case (lines 359-371) with `variant: 'clean'` and `reason: '   '`, asserting `.toThrow(/reason/)`.

**Finding 2 — `runReconciliationGate` accumulates duplicate `patched` rows on push retry**
File: `tools/delivery/cli-runner.ts` (`runReconciliationGate`, lines 2333-2370)
When `commitDeliveryArtifactAndPush` throws after the local artifact write, the next gate invocation detects the same labeled commits and appends another `patched` row for the same SHAs. This is a spec-permits-real-bug case: the ticket spec says "patched rows append (do not replace)" without requiring dedup, but the behavior in production is unbounded row accumulation under repeated push failures. The `patches` array in each duplicate row contains the same SHAs.
Fix: before appending, check whether any existing row has `outcome: 'patched'` with a `patches` array that is a superset of `decision.commitShas`; if so, skip the write.

**Finding 3 — `record-deferred` swallows JSON parse failure and writes `reviewedHeadSha: 'unknown'`**
File: `tools/delivery/cli-runner.ts`, lines 626-634
A corrupt (but existing) artifact silently causes `reviewedHead` to remain `'unknown'`. The deferred row is written with `reviewedHeadSha: 'unknown'`. A later reconciliation gate checks `row.reviewedHeadSha === input.reviewedHeadSha` — if the real SHA is not `'unknown'`, the deferred row does not match and the gate is not satisfied. The corrupt artifact is now silently "healed" with a bad deferred row. This is a correctness gap (discipline clause 2): the corrupt-artifact condition should surface as an error, not silently produce a sentinel-valued ledger entry.
Fix: change the `catch` at line 632 to `catch (e) { throw new Error(\`Failed to read artifact at ${artifactAbs}: ${String(e)}\`); }`rather than swallowing and writing`reviewedHeadSha: 'unknown'`.

---

**Findings for human review**

1. `validateRunnerArtifact` silently drops `acknowledgment` and `reason` fields from operator-recorder `clean` rows (diff-derived surface 1). Code paths that consume artifacts through `readSubagentRunnerArtifact` lose these fields. The reconciliation gate is safe because it reads raw JSON. This is a pre-existing validator design choice (forward-compat: "read what you can; don't enforce"), not a gate correctness issue. Worth noting for any future display or audit tooling that depends on the validator.

2. `record-deferred` command only accepts tickets at `subagent_review_complete` without an explicit ID, while `loadReconciliationContext` also falls back to `verified` and `in_review`. This asymmetry is not a bug but may confuse operators who need to defer on a `verified`-status ticket without knowing to pass an explicit ticket ID.

3. Dead code at `cli-runner.ts:620-622`: `const reviewedHeadShaForDefer = readFileSync('/dev/null', 'utf-8') ? 'unknown' : 'unknown'` — both branches evaluate to `'unknown'`. The value is immediately overwritten on the next line. This is a leftover placeholder with no behavioral effect.

---

**Runner termination**
`runnerStatus`: `completed`
`terminatedReason`: Full review of all specified invariants and attack surfaces was completed by reading the implementation files directly; no surfaces were skipped.
