You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `apps/menubar/Sources/CodogotchiPet.swift` created: 24 cols × 9 rows grid, `rowMap` for 9 SoA-owned states (celebrating/hyped/focused/nervous/ascended/callingForBackup/panicking/reviewing/pushing) each at 24 frames.
- `MenubarRenderer` updated: composite resolution — Codex sheet first, codogotchi sheet second, idle fallback third. `SpriteSource` enum drives per-frame interval selection.
- `MaliPet.frameInterval` added: `1.0 / Double(gridColumns)` = `0.125` s/frame.
- `CodogotchiPet.frameInterval` = `167.0 / 1000.0` ≈ `0.167` s/frame.
- `EXPECTED_STATE_SCHEMA_VERSION` bumped from `1` to `2` in `StateJsonReader.swift`.
- Missing codogotchi sheet → soft degrade (`CodogotchiPet.init` succeeds, `frames(for:)` returns `[]`).
- Malformed codogotchi sheet (grid not divisible by 24×9) → throws `MaliPetLoadError.spritesheetIncompatibleGrid`.

### Files touched

Implementation:
apps/menubar/Sources/CodogotchiPet.swift
apps/menubar/Sources/MaliPet.swift
apps/menubar/Sources/MenubarRenderer.swift
apps/menubar/Sources/StateJsonReader.swift
apps/menubar/Sources/MenubarApp.swift
apps/menubar/Menubar.xcodeproj/project.pbxproj

Tests:
apps/menubar/Tests/MenubarTests/CodogotchiPetTests.swift
apps/menubar/Tests/MenubarTests/MenubarRendererTests.swift
apps/menubar/Tests/MenubarTests/StateJsonReaderTests.swift
apps/menubar/Tests/MenubarTests/LivePollingTests.swift

### Invariants to hold

1. `CodogotchiPet.rowMap` must contain exactly 9 entries, all with `frameCount == 24`, with row indices matching the contract's Codogotchi Sheet table (celebrating→0, hyped→1, focused→2, nervous→3, ascended→4, callingForBackup→5, panicking→6, reviewing→7, pushing→8).
2. Composite resolution order: `MaliPet.rowMap[state]` first → `CodogotchiPet.rowMap[state]` second → idle fallback third. No state mapped in both sheets today, but the resolution code must implement the correct priority.
3. Missing codogotchi spritesheet → `CodogotchiPet.init` succeeds, `frames(for:)` returns `[]` for all states. Malformed spritesheet → `init` throws `MaliPetLoadError.spritesheetIncompatibleGrid`.
4. `EXPECTED_STATE_SCHEMA_VERSION == 2`. A `schema_version: 3` payload surfaces `.schemaNewer(got: 3, expected: 2)`.
5. Frame interval for Codex-sourced frames uses `1.0 / Double(frameCount)` (variable per state). Codogotchi-sourced frames use `CodogotchiPet.frameInterval` (~167 ms/frame).

### Attack surfaces to probe

1. **`CodogotchiPet.rowMap` completeness and correctness** — Row indices must match the contract's Codogotchi Sheet table. Probe: read `CodogotchiPet.swift` rowMap literal and verify all 9 entries exist with correct rowIndex/frameCount values. Check for off-by-one in row indices.

2. **Grid invariant gate — 24×9** — `CodogotchiPet` checks `cg.width % 24 == 0 && cg.height % 9 == 0`. Probe: does a sheet that is exactly 24 px wide and 9 px tall pass? Does a 1×1 PNG correctly trigger the incompatible-grid error? Does the check also require `cg.width >= 24` and `cg.height >= 9`?

3. **Soft-degrade completeness** — When `cgSheet == nil` (missing spritesheet), `frames(for:)` must return `[]` for all states in `rowMap`. Probe: does `frames(for:)` guard on `cgSheet` before consulting `rowMap`? Could a state with a rowMap entry bypass the nil check?

4. **Composite resolution race or short-circuit** — `MenubarRenderer.resolveFrames(for:)` must ask MaliPet first and return early if non-empty. Probe: if both sheets have an entry for the same state (hypothetically), does MaliPet win? Read `resolveFrames` implementation.

5. **`SpriteSource` → frame interval mapping** — The `restartTimer()` switch must route `.codogotchi` to `CodogotchiPet.frameInterval` and `.codex`/`.idleFallback` to the frame-count-based interval. Probe: does `.idleFallback` use `CodogotchiPet.frameInterval` or the Codex-style calculation? If idle fallback uses codogotchi interval, the idle animation would run at 167 ms/frame (wrong).

6. **`MenubarApp` nil codogotchi pet** — `try? CodogotchiPet()` produces `nil` if pet.json is absent at the default path. The renderer then receives `nil` for `codogotchiPet`. Probe: does `MenubarRenderer.resolveFrames` correctly handle `nil` (optional chaining `codogotchiPet?.frames(for:)`)?

#### Diff-derived attack surfaces

1. **`MaliPet.frameInterval` = `1.0 / Double(gridColumns)`** — `gridColumns = 8`. `1.0 / 8.0 = 0.125`. But the renderer's `restartTimer` for `.codex`/`.idleFallback` uses `1.0 / Double(max(currentFrames.count, 1))` — not `MaliPet.frameInterval`. Probe: is `MaliPet.frameInterval` actually used anywhere in the renderer, or is it dead code? If dead code, is this intentional (the renderer dynamically adapts to actual frame count per state)?

2. **`CodogotchiPet` init does NOT throw for missing `pet.json` when it should** — `pet.json` absence throws `petJsonNotFound`. Missing spritesheet does NOT throw. Probe: in `MenubarApp`, if `~/.codogotchi/pets/maew/pet.json` doesn't exist, `CodogotchiPet()` throws, `try?` silently returns nil, and the renderer soft-degrades. Is this correct per the ticket's "Missing codogotchi sheet is not a hard load failure" clause, or is this conflating two different failure types?

3. **`EXPECTED_STATE_SCHEMA_VERSION` bump regression** — `LivePollingTests.testSchemaNewerRendersIdleDesaturatedWithInterpolatedSchemaNewerTooltip` was updated from `v1` to `v2`. Probe: does the `schema-newer.json` fixture still have `schema_version: 99`? With `EXPECTED_STATE_SCHEMA_VERSION = 2`, a payload with `schema_version: 2` must parse successfully (not fail as schemaNewer). Verify no existing fixture that relied on v1 acceptance now fails under v2.

4. **Frame index reset on codogotchi→codex transition** — When state changes from `.panicking` (codogotchi, 24 frames) to `.implementing` (codex, 6 frames), the frame index is reset to 0. Probe: does `resolveFrames` reset `frameIndex` before or after updating `currentFrames`? (frameIndex = 0 happens in `update()` after `resolveFrames` — verify the order is correct and no off-by-one can occur on the first tick after transition).

5. **`CodogotchiPet.frames(for:)` vs `MaliPet.Frame` type** — `CodogotchiPet.frames(for:)` returns `[MaliPet.Frame]`. The `Frame` struct is defined inside `MaliPet`. Probe: does Swift allow `CodogotchiPet` to instantiate and return `MaliPet.Frame` instances directly? (It should — `MaliPet.Frame` is a public struct with no access restrictions, accessible from other files in the same module.)

6. **`testStateTransitionResetsFrameIndexToZero` update** — The test now expects `.celebrating` to resolve to `codogotchiPet.frames(for: .celebrating).count` (24) after P3.04. Probe: does the `makeRenderer()` helper in the updated `MenubarRendererTests` correctly load the `maew` fixture for codogotchiPet, or does it use a degraded (nil-sheet) instance?

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
docs as part of probing the diff-derived surfaces above.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the six diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
