**Invariant results**
1. [held] Verified all 15 `ActivityState` raw values against `packages/contracts/src/animation-state.ts`; unknown strings still decode to `.idle` via `init(from:)`.
2. [broken] `MaliPet.rowMap` contains exactly the six required keys and excludes `.celebrating`, but unmapped codogotchi-owned states do not actually render as `.idle`; renderer stores an empty frame array and emits nothing.
3. [held] New rows are in bounds and `frameCount: 8`; `xcodebuild test` passed, and `frames(for:)` loops over the declared frame count.

**Surface results**
1. [probed] ActivityState raw values match contract strings, including `running-tests`, `calling_for_backup`, and `requesting_input`.
2. [probed] `CaseIterable` synthesis is sound here; Swift raw-value enums cannot compile with duplicate raw values, and `ActivityState.allCases.count == 15` passes.
3. [probed] `rowMap` literal has six keys exactly: `.idle`, `.implementing`, `.runningTests`, `.waiting`, `.requestingInput`, `.errored`; new row/frame specs match ticket.
4. [probed] `.celebrating` is absent and `MenubarRendererTests.testStateTransitionResetsFrameIndexToZero` passes, but it passes by accepting zero frames, exposing the missing idle-render fallback.
5. [probed] `unknown-state.json` now uses `future_unknown_state`; fallback test passes. This is not guaranteed future-proof if that exact string is later added as a valid case.
6. [probed] Rows `3`, `5`, and `6` are within `gridRows = 9`; `frameCount = 8` fits `gridColumns = 8`.

7. [probed] Schema-version behavior unchanged: `EXPECTED_STATE_SCHEMA_VERSION = 1`, schema-newer fixture still asserts `got == 99`, `expected == 1`, and test passes.
8. [N/A — no CLI changes]
9. [N/A — no new catch blocks]
10. [probed] `JSONDecoder.keyDecodingStrategy = .convertFromSnakeCase` only maps JSON keys; enum raw-value decoding reads the string payload directly and is unaffected.
11. [N/A — no multi-step writes]
12. [probed] Tests cover `requesting_input`, `errored`, count 15, row indices, `.celebrating` nil, waiting frame count, and idle regression. They do not directly assert every raw value or frame counts for `.requestingInput` and `.errored`.
13. [probed] Ticket rationale matches the unknown-state fixture update, celebrating removal, and rowMap additions, except for one review-focus drift noted below.

**Actionable findings**
[apps/menubar/Sources/MenubarRenderer.swift:69](/Users/cesar/code/codogotchi_p3_03/apps/menubar/Sources/MenubarRenderer.swift:69): Unmapped codogotchi-owned states do not fall back to idle rendering. `update(state:)` assigns `currentFrames = pet.frames(for: state)`, and `MaliPet.frames(for:)` returns `[]` for `.celebrating`, `.hyped`, etc.; `paintCurrent()` then emits no image. This breaks the ticket outcome / invariant that the nine codogotchi-owned states continue to fall back to `.idle` rendering. Fix by preserving `currentState = state` but substituting `pet.frames(for: .idle)` for rendering when the requested state has no mapped frames, and add a renderer test that transitioning to `.celebrating` yields idle frame count and emits an image.

**Findings for human review**
`docs/product/delivery/phase-03/ticket-03-swift-enum-and-codex-sheet-expansion.md:50` says the Codex `rowMap` has exactly seven entries, but the ticket outcome and implementation require six. The line appears stale.

The added tests do not assert all 15 raw-value identities or frame counts for `.requestingInput` and `.errored`; current code is correct, but the coverage is thinner than the attack surface.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: Review completed; targeted source reads and `xcodebuild test -project apps/menubar/Menubar.xcodeproj -scheme Menubar -destination 'platform=macOS'` succeeded.
