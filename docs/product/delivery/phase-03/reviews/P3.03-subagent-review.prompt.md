You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `ActivityState.swift` is a closed enum with all 15 cases: idle, implementing, runningTests, reviewing, pushing, hyped, focused, nervous, waiting, celebrating, ascended, callingForBackup, panicking, requestingInput, errored.
- The decoder's "unknown string → `.idle`" fallback still applies.
- `MaliPet.rowMap` extended with `.waiting → row 6 (8 frames)`, `.requestingInput → row 3 (8 frames)`, `.errored → row 5 (8 frames)`.
- `.celebrating` removed from rowMap (honest intermediate; codogotchi sheet owns it in P3.04).
- Nine codogotchi-owned states not in rowMap continue to fall back to `.idle` rendering.
- Fixture `unknown-state.json` updated from `"ascended"` (now a valid case) to `"future_unknown_state"`.

**Rationale notes:**
- No exhaustive switch statements over ActivityState exist in the codebase — no fallthrough handling was needed.
- `EXPECTED_STATE_SCHEMA_VERSION` not bumped (deferred to P3.04).
- `CaseIterable` added to ActivityState to support `allCases.count == 15` test.

### Files touched

Implementation:
apps/menubar/Sources/ActivityState.swift
apps/menubar/Sources/MaliPet.swift
apps/menubar/Fixtures/state-json/unknown-state.json

Tests:
apps/menubar/Tests/MenubarTests/MaliPetTests.swift

### Invariants to hold

1. `ActivityState(rawValue: s)` for any of the 15 defined raw-value strings must return the matching case (not `.idle`). For any string not in the 15, it must return `.idle` via the fallback.
2. `MaliPet.rowMap` must contain exactly six entries after this ticket: `.idle`, `.implementing`, `.runningTests`, `.waiting`, `.requestingInput`, `.errored`. `.celebrating` must be absent.
3. `MaliPet.frames(for: state)` for the three new states must return non-empty arrays with count matching their declared `frameCount` when loading from the fixture spritesheet.

### Attack surfaces to probe

1. **`ActivityState` raw-value correctness** — raw values must match the hook's emitted strings exactly. Probe: does `callingForBackup = "calling_for_backup"` (underscore) match the contracts package's `ACTIVITY_STATES` definition? Does `runningTests = "running-tests"` (hyphen) match? Read `packages/contracts/src/animation-state.ts` to verify.
2. **`ActivityState.allCases.count` via CaseIterable** — Swift's synthesized `allCases` for enums with explicit raw values enumerates all declared cases. Probe: could any case be silently de-duplicated or excluded by the Swift compiler (e.g., two cases with the same raw value)?
3. **`MaliPet.rowMap` exhaustiveness** — the rowMap comment lists six states but the actual map literal is the ground truth. Probe: does the map literal have exactly six keys? Does the row/frameCount for each new entry match the ticket spec (waiting→row6/8, requestingInput→row3/8, errored→row5/8)?
4. **`.celebrating` removal regression** — removing `.celebrating` from rowMap means `pet.frames(for: .celebrating)` returns empty. Probe: does `MenubarRendererTests.testStateTransitionResetsFrameIndexToZero` still pass when it transitions to `.celebrating` (asserts `currentFramesForTesting.count == pet.frames(for: .celebrating).count == 0`)?
5. **`unknown-state.json` fixture honesty** — the fixture was updated to `"future_unknown_state"`. Probe: is this string guaranteed to never be added as a valid case? Is the test for unknown-state fallback resilient to future enum expansion?
6. **Row index bounds against the spritesheet grid** — `MaliPet.gridRows = 9` (rows 0..8). Probe: do the new row indices (3, 5, 6) all fall within 0..8? Does the frameCount=8 for all three new states fit within `gridColumns=8`?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `StateJsonReader` still uses `EXPECTED_STATE_SCHEMA_VERSION = 1`. Probe: does the `testNewerSchemaVersionFailsWithSchemaNewer` test still assert `expected == 1`? Does the schema-newer fixture (`schema_version: 99`) still trigger the correct error? Verify that adding 11 new ActivityState cases does not change the schema version gate behavior.
2. **CLI flag/arg symmetry** — No CLI flags changed. [N/A — no CLI changes]
3. **Error-class breadth in `catch` blocks** — No new catch blocks. [N/A]
4. **Defensive layering at module boundaries** — The new enum cases must round-trip through `JSONDecoder` + `ActivityState(from:)`. Probe: does `JSONDecoder.keyDecodingStrategy = .convertFromSnakeCase` interact with the raw-value strings? (It should not — raw values are explicit strings, not derived from case names.)
5. **Cross-file atomicity windows** — No multi-step writes. [N/A]
6. **Test-contract strength** — Do the new tests assert raw-value identity for both new hook states (`requesting_input`, `errored`)? Does `testAllCasesCountIs15` provide meaningful coverage or just a count? Does `testCelebratingRemovedFromRowMap` assert nil, not just "not the old value"?
7. **Doc-vs-code drift in the ticket Rationale** — Read `docs/product/delivery/phase-03/ticket-03-swift-enum-and-codex-sheet-expansion.md` Rationale and verify the described unknown-state fixture update, rowMap entries, and celebrating-removal match the diff.

### Diff context

Key changes:
1. `ActivityState.swift`: Enum gains `CaseIterable`; 11 new cases added (waiting, requestingInput, errored, hyped, focused, nervous, ascended, callingForBackup, panicking, reviewing, pushing). `celebrating` stays but is now in the middle of the list. Raw values use exact contract strings.
2. `MaliPet.swift`: `rowMap` gains `.waiting: RowSpec(rowIndex: 6, frameCount: 8)`, `.requestingInput: RowSpec(rowIndex: 3, frameCount: 8)`, `.errored: RowSpec(rowIndex: 5, frameCount: 8)`. `.celebrating: RowSpec(rowIndex: 4, frameCount: 5)` removed.
3. `unknown-state.json`: `"activity_state": "ascended"` → `"future_unknown_state"`.
4. `MaliPetTests.swift`: `testEveryFloorStateHasNonEmptyFrames` renamed/updated to exclude `.celebrating`; new `ActivityStateEnumTests` and `MaliPetRowMapExpansionTests` classes added.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
