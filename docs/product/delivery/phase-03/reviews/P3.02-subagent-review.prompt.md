You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**Outcome:**
- `packages/cli/src/hook-binary.ts` classifies an incoming Claude Code / Codex `Stop` event whose payload indicates the agent is awaiting user input as `requesting_input`.
- The same hook classifies an incoming agent-response-failure event (rate limit, network error, or any stdin shape indicating an incomplete response cycle) as `errored`.
- SoA gate-event precedence is unchanged: a fresh SoA event still wins over the two new tool-stream classifications (they live at the heuristic tier alongside `pushing` / `running-tests` / `implementing` / `reviewing`).
- The hook writes `state.json` with `schema_version: 2` going forward (consumes `STATE_JSON_SCHEMA_VERSION` from `@codogotchi/contracts` — no hardcoded number in the hook).
- The `source_event` field continues to carry `origin`, `kind`, and `name` correctly for the two new state classifications. `kind` uses the existing `tool_use` or `session_end` enum value as appropriate; no new `kind` is introduced.
- Existing v1-classification behavior for all 13 prior states is unchanged.

**Rationale notes (from ticket):**
- `requesting_input` triggered by `hook_event_name: "Stop"` (uppercase Claude Code; Codex lowercase `"stop"` also matches via `.toLowerCase()`). Absent `stop_reason` and absent `is_error` → `requesting_input`.
- `errored` triggered by `{ hook_event_name: "Stop", stop_reason: "max_tokens" }` OR any event with `{ is_error: true }`.
- Detecting `requesting_input` broadly via `kind === "session_end"` was rejected — it would reclassify Codex `session_end` events currently mapping to `idle`.
- Deferred: `stop_reason: "stop_sequence"` / `"tool_use"`, Ctrl-C interrupt, recoverable timeouts.

### Files touched

Implementation:
packages/cli/src/hook-binary.ts

Tests:
packages/cli/src/hook-binary.test.ts

### Invariants to hold

1. `classifyEvent` must return `state: "requesting_input"` for any `HookInput` where `hook_event_name.toLowerCase() === "stop"` AND `is_error` is falsy AND `stop_reason` is not in `FAILURE_STOP_REASONS`.
2. `classifyEvent` must return `state: "errored"` for any `HookInput` where `hook_event_name.toLowerCase() === "stop"` AND (`stop_reason === "max_tokens"` OR `is_error === true`), and for any non-Stop input where `is_error === true`.
3. When a fresh SoA gate event exists in `.soa/events.ndjson` at the time `runHook` is called with a Stop input, the written `state.json` must reflect the SoA event's state (not `requesting_input` or `errored`).

### Attack surfaces to probe

1. **`classifyEvent` — SoA gate block positioning:** The new Stop/errored branches must sit below the `origin === "soa" && kind === "gate"` short-circuit. Probe: can a raw input that looks like a Stop event but has `origin: "soa"` bypass the SoA gate and land as `requesting_input`?
2. **`normalize()` + `rawHookKind()` — Stop event source_event shape:** A Stop input normalized to `{ origin: "claude_code", kind: "session_end", name: "unknown" }`. Check that `sourceEvent.kind` is `"session_end"` (existing enum) and `sourceEvent.origin` is `"claude_code"` — no new enum value introduced.
3. **`FAILURE_STOP_REASONS` set exhaustiveness:** The set currently contains only `"max_tokens"`. Probe whether any other `stop_reason` value the caller might plausibly pass (e.g. `"stop_sequence"`, `""`, `undefined`, `null`) accidentally triggers `errored` or silently falls through to `requesting_input` in an unexpected way.
4. **`isFailureStopReason` — undefined/null guard:** Called with `input.stop_reason` which is `string | undefined`. Probe: does `isFailureStopReason(undefined)` return `false` cleanly, and does `isFailureStopReason("")` return `false` (empty string not in the set)?
5. **`is_error` flag on non-Stop events:** The second new branch fires for any `input.is_error === true` regardless of `hook_event_name`. Probe: does a tool-use event with `is_error: true` (e.g. `{ tool_name: "Edit", is_error: true }`) return `errored` rather than `implementing`? Is this the intended behavior?
6. **Existing `session_end` → idle not regressed:** `{ origin: "claude_code", kind: "session_end", name: "end" }` (no `hook_event_name`) must still return `idle`. Probe: does the absence of `hook_event_name` prevent the Stop branch from firing?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `STATE_JSON_SCHEMA_VERSION` is imported from `@codogotchi/contracts` (already set to `2` since P3.01). No hardcoded version in this diff. Probe: is `schema_version: 2` written for Stop-classified events (the `writeStateAtomic` call uses the imported constant, not a literal)?
2. **CLI flag/arg symmetry** — No new CLI flags introduced. `is_error` and `stop_reason` are `HookInput` fields read by the hook binary, not CLI flags. [N/A — no CLI flag changes]
3. **Error-class breadth in `catch` blocks** — No new `catch` blocks introduced. The diff adds pure classification logic with no I/O. [N/A — no catch blocks changed]
4. **Defensive layering at module boundaries** — The new fields `is_error?: boolean` and `stop_reason?: string` are read directly from `HookInput` without schema validation. Probe: if a caller passes `is_error: "true"` (string coercion), does the check `input.is_error` evaluate truthy and produce `errored`? Is that safe?
5. **Cross-file atomicity windows** — No multi-step writes in this diff. The `writeStateAtomic` function is unchanged and already uses rename-based atomicity. [N/A — no new write paths]
6. **Test-contract strength** — Probe: do the new tests assert `sourceEvent.origin` and `sourceEvent.kind` for the `requesting_input` case (structural shape)? Do they test `errored` for both trigger paths (`stop_reason` and `is_error`)? Is the SoA override test complete (checks both `activity_state` and `source_event.origin`)?
7. **Doc-vs-code drift in the ticket Rationale** — Read the Rationale section in `docs/product/delivery/phase-03/ticket-02-hook-v2-detection.md` and verify that the described Stop-event and failure-event shapes match what the diff actually implements.

### Diff context

Key changes in `packages/cli/src/hook-binary.ts`:

1. Two new fields added to `HookInput`: `stop_reason?: string` and `is_error?: boolean`.
2. `FAILURE_STOP_REASONS = new Set(["max_tokens"])` constant added at module level.
3. `isFailureStopReason(reason: string | undefined): boolean` helper added.
4. In `classifyEvent`, after the SoA gate block, two new branches inserted before the `if (kind === "tool_use")` block:
   - If `input.hook_event_name?.toLowerCase() === "stop"`: checks `input.is_error || isFailureStopReason(input.stop_reason)` — if true returns `errored`, else returns `requesting_input` (preserving `prior.readRun`).
   - If `input.is_error === true` (non-Stop events): returns `errored`.

Key changes in `packages/cli/src/hook-binary.test.ts`:

- 4 new `classifyEvent` unit tests: Stop→requesting_input (with sourceEvent shape assertions), Stop+max_tokens→errored, Stop+is_error→errored, Edit→implementing regression.
- 3 new `runHook` integration tests: schema_version 2 assertion on Stop event, Stop→requesting_input in runHook, Stop+max_tokens→errored in runHook.
- 1 new SoA precedence test: fresh `ticket_started` event overrides Stop→requesting_input, producing `hyped`.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
