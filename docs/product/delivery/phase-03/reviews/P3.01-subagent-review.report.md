**Invariant results**

1. `[held]` Probed `parseStateJson` with `schema_version` values `1`, `2`, `3`, `0`, `-1`, `1.5`, `"2"`, `null`, missing, and `2.0`; only integer versions `1` and `2` parsed.
2. `[held]` `ACTIVITY_STATES` remains an `as const` literal tuple and `activityStateSchema = z.enum(ACTIVITY_STATES)`; the two new states are appended after all v1 entries.
3. `[held]` `STATE_JSON_SCHEMA_VERSION === 2`, and the parser ceiling is configured through `.max(STATE_JSON_SCHEMA_VERSION)` rather than a second numeric copy.

**Surface results**

1. `[probed]` `parseStateJson` / `stateJsonV1Schema`: invalid versions reject with `ZodError`; `2.0` parses as integer-valued JS number; `3` rejects with max `<=2`. No leak past the ceiling found.
2. `[probed]` `ACTIVITY_STATES`: enum order matches the contract table and closed runtime schema. However, `RELIABLE_ACTIVITY_STATES` does not include the new doc-classified reliable states; see actionable finding.
3. `[probed]` Version hardcodes: TS hook writer imports `STATE_JSON_SCHEMA_VERSION` and will now emit v2; Swift `EXPECTED_STATE_SCHEMA_VERSION` remains `1`; menubar fixtures remain v1. This matches the documented P3.04 deferral but creates release-sequencing risk if the hook is shipped before renderer support.
4. `[probed]` `state-json.test.ts`: tests use exported public surfaces, do not mutate `baseV1Payload`, assert parsed activity state for new v2 states, and the v3 test would fail if the ceiling were accidentally widened to `99`.
5. `[probed]` `package.json` CI script: `bun run test` resolves to `bun test packages convex` on Bun 1.3.14 and ran 189 tests. Current TS tests all live under `packages` or `convex`; no current `tools`/`scripts` test directory is skipped.
6. `[probed]` Forward-compat doc-vs-code: missing/non-integer versions reject uniformly at parser layer with `ZodError`; doc heading is `state.json v2 schema`.

Diff-derived classes:

1. `[probed]` Output stability across schema-version drift: no TS consumer branches on `.schema_version === STATE_JSON_SCHEMA_VERSION`; hook writer emits the imported constant. Swift renderer still accepts v1 and rejects v2 as newer, which is deferred by plan.
2. `[N/A — no new CLI flags added in this diff]`
3. `[N/A — no new catch blocks added in this diff]`
4. `[probed]` Defensive layering: `@codogotchi/contracts` remains the TS trust boundary; hook writes validate through `stateJsonV1Schema.parse`; no consumer import shows a discriminated-union break from literal-to-bounded-number widening.
5. `[N/A — pure contract/doc/test/tooling change with no multi-file runtime migration window beyond the planned phase stack]`
6. `[probed]` Test-contract strength: current tests cover constant, v2 new states, v1 acceptance, and v3 rejection; invalid version-shape cases are not in tests but passed runtime probes.
7. `[probed]` Ticket Rationale drift: behavior claims mostly match runtime, but the pre-flight commit description is inaccurate; see human-review finding.

**Actionable findings**

`packages/contracts/src/animation-state.ts:34`: `RELIABLE_ACTIVITY_STATES` omits `requesting_input` and `errored`, while `docs/contracts/animation-state-vocabulary.md:105` and `:106` classify both as `reliable`. This is a correctness gap under finding-discipline clause 2: the TS contracts package exports reliability groupings that now drift from the contract enum metadata. Fix: append `"requesting_input"` and `"errored"` to `RELIABLE_ACTIVITY_STATES` and add a focused assertion that the v2 reliable states are included.

**Findings for human review**

`docs/contracts/animation-state-vocabulary.md:7`: intro still says the doc defines the v1 `state.json` schema even though the title and schema section are v2. This is doc drift, not a parser invariant break.

`docs/product/delivery/phase-03/ticket-01-schema-v2-contract-and-ts-contracts.md:63`: Rationale says “Two structural fixes” sit on this branch as separate commits ahead of `[red]`, but `git log` shows only `2c8b90f chore(p3): include bun tests in the ci gate` ahead of `[red]`; `ffe9caa` is already on `main`. The bullet list also names only one branch-local pre-flight commit.

Release sequencing note: `packages/cli/src/hook-binary.ts:345` now writes schema v2 because it imports the bumped constant, while `apps/menubar/Sources/StateJsonReader.swift:8` still expects v1. The contract explicitly permits old renderers to refuse newer payloads, and P3.04 defers the Swift bump, so this is not a blocking finding for this ticket.

**Runner termination**

`runnerStatus`: `completed`  
`terminatedReason`: Completed the requested advisory-only review and all listed attack surfaces were either probed or marked N/A.
