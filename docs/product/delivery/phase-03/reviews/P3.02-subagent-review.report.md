**Invariant results**

1. [held] Static probe of the Stop branch shows `hook_event_name?.toLowerCase() === "stop"` returns `requesting_input` when `is_error` is falsy and `stop_reason` is absent or not `"max_tokens"`, except for the intentional SoA gate precedence path.
2. [held] Stop + `stop_reason: "max_tokens"` returns `errored`; Stop + `is_error: true` returns `errored`; non-Stop + `is_error: true` returns `errored`.
3. [held] `runHook` classifies first, then reads fresh `.soa/events.ndjson`; any mapped fresh SoA event overwrites `activityState` and `sourceEvent` before `state.json` is written.

**Surface results**

1. [probed] SoA gate block positioning: the SoA gate short-circuit is above Stop/error branches. A Stop-shaped input with `origin: "soa", kind: "gate", name: "ticket_started"` maps to `hyped`, not `requesting_input`.
2. [probed] Stop source_event shape: `{ hook_event_name: "Stop" }` normalizes to `origin: "claude_code"`, `kind: "session_end"`, `name: "unknown"`. No new `SourceEventKind` enum value is introduced.
3. [probed] `FAILURE_STOP_REASONS`: only `"max_tokens"` is treated as failure. `"stop_sequence"`, `""`, `undefined`, and runtime `null` do not trigger `errored`; they fall through to `requesting_input`, matching the ticket’s deferred scope.
4. [probed] `isFailureStopReason` guard: `undefined` returns false via the explicit guard; `""` returns false because it is not in the set. Runtime `null` also returns false through `Set.has(null)`.
5. [probed] Non-Stop `is_error`: `{ tool_name: "Edit", is_error: true }` returns `errored` before `kind === "tool_use"` can return `implementing`. This matches the documented “any event with `{ is_error: true }`” failure shape.
6. [probed] Existing `session_end` behavior: `{ origin: "claude_code", kind: "session_end", name: "end" }` has no `hook_event_name`, so the Stop branch does not fire and the fallback remains `idle`.

Diff-derived:

1. [probed] Schema-version drift: `runHook` writes `schema_version: STATE_JSON_SCHEMA_VERSION`, imported from `@codogotchi/contracts`; the contract currently exports `2`.
2. [N/A — no CLI flag changes] `is_error` and `stop_reason` are stdin payload fields on `HookInput`; no CLI arg or flag path was added.
3. [N/A — no catch blocks changed] The diff does not add or alter catch behavior in the classification path.
4. [probed] Defensive module boundary: `runHookFromStdin` parses arbitrary JSON and casts it to `HookInput`; `classifyEvent` uses truthiness for `input.is_error`, so string values like `"true"` and `"false"` both classify as `errored`.
5. [N/A — no new write paths] `writeStateAtomic` remains the single state write path and still validates then writes temp + rename.
6. [probed] Test-contract strength: new tests assert `sourceEvent.origin` and `sourceEvent.kind` for `requesting_input`; test `errored` via Stop + `max_tokens` and Stop + `is_error`; and the SoA precedence test checks `activity_state`, `source_event.origin`, and `source_event.name`.
7. [probed] Doc-vs-code drift: the ticket Rationale in `docs/product/delivery/phase-03/ticket-02-hook-v2-detection.md` matches the implementation for Stop shape, failure shape, SoA positioning, and deferred cases.

**Actionable findings**

`packages/cli/src/hook-binary.ts`: `input.is_error` is checked by truthiness in both the Stop branch and the non-Stop error branch. Because `runHookFromStdin` accepts raw JSON and casts it to `HookInput` without validation, a payload like `{ "hook_event_name": "Stop", "is_error": "false" }` or `{ "tool_name": "Edit", "is_error": "false" }` is classified as `errored`. This is a demonstrable correctness gap under finding-discipline clause 2. Recommended fix: compare strictly with `input.is_error === true` in both branches, matching the stated failure shape.

**Findings for human review**

None.

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: Review completed by static inspection only; no repository files were written or modified.
