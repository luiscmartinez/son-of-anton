You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** — anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to protect. Start from the invariants and attack surfaces below, then independently inspect the diff and directly related implementation code for missing ticket-relevant risks. You are looking for paths where the ticket's intended behavior breaks, not for general improvements.

### Ticket scope

P16.03 Add Post-Phase Advisory Observations Triage Command

Outcome:
- `bun run deliver --plan <plan-path> triage-advisory-observations` scans the selected phase's subagent-review reports.
- The command groups advisory observations by ticket and source report.
- The command writes or updates the advisory-observation triage artifact using the schema from P16.02.
- The command is post-phase and decision-recording only; it never applies source patches.
- The command supports explicit disposition input without requiring an interactive terminal.

Rationale notes:
- Red first: focused test failed because `../advisory-observation-command` did not exist.
- Why this path: add a focused command helper that scans subagent-review ledger/report sidecars, matches parsed `Advisory Observations` to explicit disposition input, and writes the phase-level triage artifact without touching source files.
- Deferred: warning on untriaged or suspicious advisory-observation evidence remains in P16.04.

### Files touched

Implementation:
- tools/delivery/advisory-observation-command.ts
- tools/delivery/cli.ts
- tools/delivery/cli-runner.ts
- tools/delivery/orchestrator.ts
- docs/product/delivery/phase-16/ticket-03-add-post-phase-advisory-observations-triage-command.md

Tests:
- tools/delivery/test/p16-03.test.ts

### Invariants to hold

1. `triage-advisory-observations` must require explicit non-interactive disposition input and must not guess a disposition for any parsed advisory observation.
2. The command must parse only `Advisory Observations` from completed subagent-review report sidecars and must not include `Actionable findings` in the advisory-observation triage artifact.
3. Re-running the command with the same disposition input must update/merge deterministically without duplicating observation entries, using the P16.02 artifact schema.

### Attack surfaces to probe

1. `runAdvisoryObservationTriage` missing-disposition matching — can source path normalization, ticket id fallback, or observation text parsing cause the command to silently skip a required operator decision?
2. `scanAdvisoryObservationGroups` ledger/report discovery — does mixing state-provided ledger paths with reviews-directory scanning miss valid completed sidecar reports or accidentally parse non-completed/invalid runner output?
3. `readAdvisoryObservationDispositionInput` and `writeAdvisoryObservationTriageArtifact` boundary — does unvalidated input reach artifact write in a way that violates P16.02 schema or produces partial/invalid JSON?
4. CLI integration in `parseCliArgs` and `runDeliveryOrchestrator` — are `--dispositions`, usage text, command dispatch, plan resolution, and worktree guard behavior symmetric and headless-safe?
5. Artifact path derivation in `deriveAdvisoryObservationTriageArtifactPath` — does deriving from `reviewsDirPath` write to the phase-level artifact path rather than under reviews or an unrelated directory?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — probe whether the new writer preserves `schemaVersion: 1`, the P16.02 observation fields, deterministic ordering, and existing artifact compatibility.
2. **CLI flag/arg symmetry** — probe `--dispositions` in help text, parser field, missing value errors, and the downstream consumer in `triage-advisory-observations`.
3. **Error-class breadth in `catch` blocks** — there are no new broad catch blocks in the command helper; still probe skipped missing ledgers/reports and JSON parsing behavior for misleading silent success.
4. **Defensive layering at module boundaries** — probe state input, disposition input, ledger JSON shape, report path normalization, and artifact writer validation.
5. **Cross-file atomicity windows** — probe whether the command writes only the single triage artifact and whether reruns recover from existing artifacts without source mutations.
6. **Test-contract strength** — probe that tests cover command presence, grouping, actionable exclusion, missing disposition failure, and idempotency.
7. **Doc-vs-code drift in the ticket Rationale** — read the ticket rationale and verify it matches the actual command behavior and deferrals.

### Diff context

This change adds `tools/delivery/advisory-observation-command.ts`, which discovers subagent-review ledger JSON files from delivery state and the reviews directory, reads completed invocation `rawOutput` report sidecars, parses `Advisory Observations` via the existing reconciliation parser, requires matching explicit disposition entries, merges them through the P16.02 artifact helper, and writes `docs/product/delivery/<phase>/advisory-observation-triage.json`. CLI parsing now recognizes `--dispositions`, usage lists `triage-advisory-observations`, and `runDeliveryOrchestrator` reads the disposition file then calls the new helper. The barrel export exposes the helper/types. Tests assert usage/parsing, grouped artifact writing, exclusion of `Actionable findings`, missing disposition failure, and idempotency.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly related code paths named in the attack surfaces. Do not expand scope beyond what the ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in the repository. Your entire deliverable is findings prose in the required output format below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**` (or anywhere else). You must still read the ticket Rationale and any referenced contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale" diff-derived surface above. If you find drift, surface it under **Findings for human review** with the specific file, the conflicting claim, and what the diff actually does.

**Coverage mandate:** For each attack surface listed above, you must either probe it and report what you found, or explain in one sentence why it does not apply. "I didn't check" is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the behavior, but that behavior is nevertheless unsafe in production (data loss, unrecoverable state, silent-failure exposure, security regression). Name which spec clause permitted the unsafe behavior so the primary agent can decide whether to update the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings. If you notice something worth flagging but it is outside these three clauses, put it in **Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes): `[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong, which invariant or finding-discipline clause applies, and a concrete fix recommendation. If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
