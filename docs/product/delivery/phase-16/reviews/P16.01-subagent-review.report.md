**Invariant results**
1. `[broken]` — `Actionable findings` and `Advisory Observations` parse correctly for exact bold headings, but the shared section extractor does not tolerate ATX Markdown headings like `## Actionable findings`, despite the review focus requiring common Markdown drift tolerance.
2. `[held]` — `None.`, empty bodies, and missing bold sections return no findings in the covered paths.
3. `[held]` — advisory observations are returned separately and do not make `parseActionableFindings()` block reconciliation.
4. `[held]` — `inspectSubagentReviewEvidence()` returns warning-shaped structured data for `clean/completed` rows with missing or whitespace-only report files, without throwing or returning reconciliation failure state.

**Surface results**
1. `parseActionableFindings` / `parseAdvisoryObservations` section extraction — `[probed]` — Exact bold headings pass. ATX headings fail, and the regex is not line-anchored, so it can match inline bold prose as a section label.
2. `parseAdvisoryObservations` bullet/prose splitting — `[probed]` — Pure bullet sections become one item per bullet; paragraph prose remains paragraph-separated. Mixed prose plus bullets becomes paragraph output, which is acceptable under the current ticket contract.
3. `None.` handling — `[probed]` — Plain `None.` and whitespace-only bodies return empty/false. I did not find a ticket-required case for bullet-form `- None.`.
4. `inspectSubagentReviewEvidence` path checks — `[probed]` — Missing `rawOutput`, missing file, and whitespace-only file are warning data. Non-clean and non-completed rows are ignored as intended.
5. `reconcileReview` interaction — `[probed]` — Existing actionable findings still block with Condition B; advisory observations do not affect blocking semantics.
6. **Output stability across schema-version drift** — `[probed]` — No persisted JSON schema is changed; new warning type is not yet wired into an artifact writer.
7. **CLI flag/arg symmetry** — `[N/A — no CLI flag or arg changed in this diff]`
8. **Error-class breadth in `catch` blocks** — `[N/A — no new or modified catch block in the changed parser/evidence code]`
9. **Defensive layering at module boundaries** — `[probed]` — The exported evidence helper accepts raw row-like objects and revalidates the relevant row fields by exact outcome/termination/path checks.
10. **Cross-file atomicity windows** — `[N/A — parser helper reads files but performs no multi-file writes]`
11. **Test-contract strength** — `[probed]` — Tests assert returned structures and blocking booleans, but they do not cover ATX heading drift or false positive inline-bold matches.
12. **Doc-vs-code drift in the ticket Rationale** — `[probed]` — Ticket Rationale matches the implementation path and deferrals. The newly added review prompt artifact is not fully filled per the template, noted below for human review.

**Actionable findings**
`tools/delivery/reconciliation.ts:156` — `extractBoldSection()` only recognizes unanchored bold labels like `**Actionable findings**`, so reports using common Markdown section headings such as `## Actionable findings` or `## Advisory Observations` are treated as missing sections. That breaks the ticket review focus requiring heading matching to tolerate common Markdown drift. It also risks accepting unrelated inline bold text as a section because the match is not anchored to a heading line. Fix: replace the extractor with a line-anchored section parser that accepts the supported heading forms, at minimum bold heading lines and ATX heading lines, and stops only at the next recognized heading line.

**Findings for human review**
`docs/product/delivery/phase-16/reviews/P16.01-subagent-review.prompt.md` is newly added but leaves the template’s “Invariants to hold” and “Attack surfaces to probe” sections as generic instructions rather than filled, ticket-specific assertions/surfaces. That conflicts with `docs/template/delivery/adversarial-review-template.md`, which says the primary execution agent fills those sections before invoking the subagent. This is outside the parser behavior itself, but it is artifact-contract drift.

**Runner termination**
`runnerStatus`: completed  
`terminatedReason`: Review completed; focused tests passed, and one ticket-relevant parser robustness gap plus one review-artifact drift note were identified.
