You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Findings for human review** — anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to protect.

### Ticket scope

P16.04 Surface Untriaged Advisory Observations After Closeout

Outcome:
- SOA can detect when a completed phase has advisory observations without recorded dispositions.
- SOA can detect suspicious subagent-review evidence, including a `clean/completed` ledger row with missing or empty report prose.
- These conditions surface as warnings in the appropriate post-phase/status/closeout pathway, not as pre-PR reconciliation blockers.
- Existing `reconcile-subagent-review` behavior for `Actionable findings` is unchanged.

Rationale notes:
- Red first: `bun test ./tools/delivery/test/p16-04.test.ts` failed because `../advisory-observation-warnings` did not exist.
- Why this path: add a focused advisory-observation warning helper that reads subagent-review ledgers/reports plus the P16.02 disposition artifact, then surface those warnings through closeout summary output without changing pre-PR reconciliation semantics.
- Deferred: documentation and `/soa` wrapper guidance remain in P16.05.

### Files touched

Implementation:
- tools/delivery/advisory-observation-warnings.ts
- tools/delivery/closeout-stack.ts
- tools/delivery/orchestrator.ts
- docs/product/delivery/phase-16/ticket-04-surface-untriaged-advisory-observations-after-closeout.md

Tests:
- tools/delivery/test/p16-04.test.ts

### Invariants to hold

1. A completed phase with parsed `Advisory Observations` and no matching P16.02 disposition artifact entry must produce non-blocking `untriaged_observation` warnings.
2. A completed phase whose advisory observations all have matching disposition entries must produce no untriaged warnings.
3. Clean/completed subagent-review ledger rows with missing or empty report prose must produce suspicious-evidence warnings without invalidating the runner ledger or changing `reconcile-subagent-review` blocking behavior.
4. Closeout summary output must include advisory-observation warnings while existing `reconcileReview` remains clean for reports that contain only advisory observations and no actionable findings.

### Attack surfaces to probe

1. `computeAdvisoryObservationWarnings` ledger discovery and parsing — can state-provided paths, directory scanning, missing ledgers, or missing reports cause silent false negatives for required warnings?
2. `readTriagedObservationKeys` matching — does keying by source report path, ticket id, and observation text match the P16.02 artifact schema exactly, without suppressing unrelated observations?
3. `inspectSubagentReviewEvidence` integration — does suspicious evidence fire only for clean/completed rows and preserve warning-only semantics?
4. `formatCloseoutSummary` wiring — does closeout include warnings without changing merge/close behavior or requiring a clean warning state before closeout proceeds?
5. `reconcileReview` boundary — verify advisory observations still do not trigger Condition A or Condition B.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — probe that no persisted triage schema changes and warning output is additive closeout text only.
2. **CLI flag/arg symmetry** — N/A unless closeout or deliver flags changed; verify no parser surface changed accidentally.
3. **Error-class breadth in `catch` blocks** — no new catches; probe missing JSON/report paths for explicit warning/skip semantics rather than swallowed corruption.
4. **Defensive layering at module boundaries** — probe ledger JSON shape, triage artifact validation, path normalization, and closeout caller integration.
5. **Cross-file atomicity windows** — probe that closeout warning computation reads artifacts only and does not create a multi-file write dependency.
6. **Test-contract strength** — verify tests cover untriaged, fully triaged, suspicious evidence, closeout formatting, and reconciliation non-blocking behavior.
7. **Doc-vs-code drift in the ticket Rationale** — verify ticket rationale accurately describes the implemented warning helper and closeout surfacing.

### Diff context

This diff adds `tools/delivery/advisory-observation-warnings.ts`, which reads subagent-review ledgers, parses completed report sidecars for `Advisory Observations`, compares those observations to `advisory-observation-triage.json`, and returns warning objects for untriaged observations and suspicious missing/empty clean completed reports. `closeout-stack.ts` now exports `formatCloseoutSummary`, computes advisory warnings during closeout, and appends formatted warning text to the closeout summary. `orchestrator.ts` exports the new helper and type. Tests cover warning computation, closeout formatting, and unchanged `reconcileReview` behavior.

---

### Your directives

**Scope:** Review only the implementation diff and directly related code paths named above.

**Advisory-only — no file writes:** You must not create, modify, or delete files. Return findings prose only.

**Read boundary for delivery docs:** Read the ticket Rationale and referenced contract docs, but do not write delivery docs. Surface doc-vs-code drift under **Findings for human review**.

**Coverage mandate:** For each attack surface listed above, either probe it and report what you found, or explain why it does not apply.

**Finding discipline:** Report a finding only if the code breaks a stated invariant, introduces a demonstrable correctness gap, or the spec permits unsafe production behavior.

---

### Required output format

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface, including the seven diff-derived classes: `[probed | N/A — <reason> | blocked — missing-input]` plus what you tried and found.

**Actionable findings**
If none: "None."

**Findings for human review**
If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
