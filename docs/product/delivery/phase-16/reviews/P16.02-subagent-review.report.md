**Invariant results**

1. `[held]` `validateAdvisoryObservationTriageArtifact` funnels every entry through `normalizeEntry`, checks `disposition` against the exact five-value `ADVISORY_OBSERVATION_DISPOSITIONS` tuple, and rejects unknown strings.
2. `[held]` Non-`patched` entries require `(rationale ?? '').trim() !== ''`, so missing, empty, and whitespace-only rationales fail validation.
3. `[held]` `writeAdvisoryObservationTriageArtifact` revalidates before writing, normalizes required identity fields and optional patch/follow-up fields, sorts observations, uses pretty JSON, and appends a trailing newline.
4. `[held]` `mergeAdvisoryObservationTriageEntries` keys entries by `sourceReportPath + ticketId + observationText`; later matching entries overwrite earlier ones and the result is sorted.

**Surface results**

1. `[probed]` `normalizeEntry` rejects non-objects, missing/blank required strings, unknown dispositions, non-string optional fields, and empty non-`patched` rationale. Whitespace-only required fields also fail via `trim()`.
2. `[probed]` Read/write round-trip revalidates through `validateAdvisoryObservationTriageArtifact`; normalized object construction plus sorted observations gives stable JSON ordering and preserves identity fields.
3. `[probed]` Merge uses the stated identity key. Existing duplicates and incoming duplicates collapse to one decision because all entries pass through the same `Map`.
4. `[probed]` `tools/delivery/orchestrator.ts` re-exports the disposition constant, merge/read/sort/validate/write helpers, and artifact/entry/disposition types.
5. `[probed]` Tests cover all five dispositions, unknown disposition rejection, missing non-`patched` rationale, patched SHA preservation, identity preservation, trailing newline, read/write round-trip, and duplicate update behavior. They do not explicitly test whitespace-only rationale or bad schema versions, but the implementation covers those paths.
6. `[probed]` Schema drift: validation accepts only `schemaVersion === 1`; older/unknown versions reject with a clear schemaVersion error. Write normalizes to `schemaVersion: 1`.
7. `[N/A — no CLI change expected]` No CLI flags or help text were added for P16.02; P16.03 owns command behavior.
8. `[N/A — no catch blocks added]` Read/write do not swallow JSON or filesystem errors.
9. `[probed]` Public validate/read/write/merge entry points revalidate caller-supplied artifacts or entries before returning/writing merged output.
10. `[probed]` Write creates parent directories recursively and writes deterministic JSON. It is not an atomic temp-file rename, but that is outside this ticket’s stated artifact contract.
11. `[probed]` Test-contract strength is mostly structural, not prose-only. Missing edge tests would improve regression coverage, but I did not find a malformed artifact that the implementation accepts in violation of the ticket.
12. `[probed]` Ticket rationale matches the implementation path, separate artifact choice, deterministic merge semantics, exported surface, and P16.03 CLI deferral.

**Actionable findings**

None.

**Findings for human review**

None.

**Runner termination**

`runnerStatus`: `completed`  
`terminatedReason`: Advisory review completed from static repo inspection without modifying files.
