You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in Advisory Observations; anything off-scope but real is welcome there.
Your job is not a general code review. It is a targeted attack on the behavior this ticket is supposed to protect.

### Ticket scope

Outcome:
- PR body ticket-file links use repo-primary `defaultBranch`.
- PR body stacked-base metadata uses the ticket's actual `baseBranch`.
- State repair and sync paths preserve configured delivery bases instead of reintroducing `defaultBranch` as the delivery base.
- Operator-facing metadata distinguishes repo-primary references from delivery-base branch references.

Rationale notes:
- Red first: metadata and state repair tests should fail before implementation.
- Metadata and repair are easy to regress after delivery-base plumbing, so they get their own focused slice.
- Existing PR refresh now sends the ticket `baseBranch` to GitHub alongside refreshed title/body so GitHub PR metadata, body metadata, and delivery state do not drift when repo-primary `defaultBranch` differs from the delivery base.

### Files touched

Implementation:
- `tools/delivery/ticket-flow.ts`

Tests:
- `tools/delivery/test/p18-03.test.ts`
- `docs/product/delivery/phase-18/ticket-03-pr-metadata-and-state-repair-branch-roles.md`

### Invariants to hold

1. `buildPullRequestBody` must link ticket files through `githubRepo.defaultBranch` while rendering stacked base metadata from `ticket.baseBranch`.
2. `openPullRequest` must refresh an existing PR's GitHub base using `target.baseBranch`, not repo-primary default branch discovery or stale GitHub state.
3. State sync/repair for a stale first ticket must restore `deliveryBaseBranch` instead of preserving or reintroducing `defaultBranch`.

### Attack surfaces to probe

1. `openPullRequest` existing-PR path in `tools/delivery/ticket-flow.ts`: confirm `editPullRequest` now receives `base: target.baseBranch` and that this does not affect new-PR creation or status transitions.
2. `buildPullRequestBody` in `tools/delivery/pr-metadata.ts`: confirm ticket-file URLs and stacked-base metadata intentionally draw from different branch roles.
3. `syncStateFromExisting` / `syncStateWithPlan` in `tools/delivery/state.ts`: confirm the first-ticket repair fallback uses `deliveryBaseBranch`, and later tickets still stack on predecessor branches.
4. `tools/delivery/test/p18-03.test.ts`: check whether tests would catch a regression where GitHub PR base refresh uses `main`, omits `base`, or mutates ticket-file links to `release-next`.

#### Diff-derived attack surfaces

1. Output stability across schema-version drift: no persisted schema version is added, but `editPullRequest` options now include `base` for existing PR refreshes. Probe whether tests or adapters expect body/title-only update options.
2. CLI flag/arg symmetry: no CLI flags or args changed. Mark N/A unless you find a hidden downstream CLI surface.
3. Error-class breadth in catch blocks: no catch blocks changed. Mark N/A unless related code turns PR base-edit failures into swallowed errors.
4. Defensive layering at module boundaries: `openPullRequest` passes a previously validated `TicketState.baseBranch` across the platform adapter boundary. Probe whether empty or missing base values can reach this call.
5. Cross-file atomicity windows: PR refresh remains a single platform call followed by state update. Probe whether adding `base` creates a partial body/base mismatch when GitHub rejects base edits.
6. Test-contract strength: new tests cover body branch-role split, existing PR base refresh, and first-ticket repair fallback. Probe whether the existing-PR test asserts the structural `base` option directly.
7. Doc-vs-code drift in the ticket Rationale: read `docs/product/delivery/phase-18/ticket-03-pr-metadata-and-state-repair-branch-roles.md` and compare the implementation note with the actual diff.

### Diff context

This ticket adds a focused P18.03 test file covering three branch-role contracts. The initially failing behavior was that `openPullRequest` refreshed an existing PR with only `body` and `title`, leaving GitHub's PR base unchanged even while the PR body rendered `- stacked base branch: release-next`. The implementation patch adds `base: target.baseBranch` to the existing-PR `editPullRequest` call. No state implementation code changed because the existing `syncStateFromExisting` path already repairs first-ticket base branch fallback to `deliveryBaseBranch`; the new test locks that behavior.

### Your directives

Scope: Conduct an adversarial review of the implementation diff and directly related code paths named above. Do not expand scope beyond the ticket outcome.

Advisory-only: do not create, modify, or delete files. Your deliverable is findings prose only.

Read boundary for delivery docs: read the ticket Rationale and referenced contract docs, but do not edit them. Surface doc drift under Advisory Observations.

Coverage mandate: For each attack surface listed above, either probe it and report what you found, or explain why it does not apply.

Finding discipline: Report a finding only when code breaks a stated invariant, introduces a demonstrable correctness gap, or the spec permits a real unsafe production bug. Put non-blocking scope drift under Advisory Observations.

Required output format:

**Invariant results**
For each invariant: `[held | broken | untested]` plus one line explaining what you tried.

**Surface results**
For each attack surface, including the seven diff-derived classes: `[probed | N/A - reason | blocked - missing-input]` plus what you tried and found.

**Actionable findings**
If none: `None.`

**Advisory Observations**
If none: `None.`

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
