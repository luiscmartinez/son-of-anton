Invariant results

1. [held] - I traced `runCloseoutStack` and found the guard calls `ensureOnCloseoutBranch(cwd, config.closeoutBranch)`; the guard compares current HEAD directly to `closeoutBranch`.
2. [held] - Fetch/reset/push helper output is built from `closeoutBranch`, and the production normal, skipped-merged, conflict-recovery, and final-push paths consume `config.closeoutBranch`.
3. [held] - `closePullRequest` receives `config.closeoutBranch` and both squash and cherry-pick comment strings name that branch.

Surface results

1. [probed] Branch guard: `tools/delivery/closeout-stack.ts:303-388` rejects `main` when `closeoutBranch` is `staging`, and `formatCloseoutBranchGuardError` names `staging`, not `main`.
2. [probed] Normal and already-merged sync paths: `tools/delivery/closeout-stack.ts:400-419` fetches `origin staging` and resets to `origin/staging` via `buildCloseoutBranchSyncCommands(config.closeoutBranch)` in both paths.
3. [probed] Squash-conflict recovery: `tools/delivery/closeout-stack.ts:432-438` resets to `origin/${config.closeoutBranch}` before cherry-picking; no `origin/main` fallback remains there.
4. [probed] Push and PR-close side effects: `tools/delivery/closeout-stack.ts:459-470` pushes `config.closeoutBranch` and passes the same branch into PR close comments.
5. [probed] Exported helper tests: `tools/delivery/test/p18-04.test.ts:10-32` constrain the helper text and command builders used by production, but they do not exercise `runCloseoutStack` end-to-end or the inline conflict reset.
6. [probed] Output stability across schema-version drift: no persisted schema path changed; PR close comment text changed intentionally. Closeout summary output does not currently name `closeoutBranch`, which is a ticket outcome gap.
7. [probed] CLI flag/arg symmetry: `parseCloseoutStackArgs` still accepts only `--plan`, and `scripts/closeout-stack.ts` still calls `runCloseoutStack(process.argv.slice(2), process.cwd())`; branch source comes from resolved config.
8. [probed] Error-class breadth in catch blocks: the top-level catch still prints `error.message`; `runProcess` still includes command text and stderr/stdout, and branch guard copy is branch-specific.
9. [probed] Defensive layering at module boundaries: exported helpers accept raw strings, but production inputs come from required non-blank config validation plus argv-free command arrays. That is acceptable for internal helpers.
10. [probed] Cross-file atomicity windows: fetch/reset/push/comment now consistently target `closeoutBranch`; remote ticket branch deletion remains intentionally `ticket.branch`, not closeout target.
11. [probed] Test-contract strength: helper tests would catch helper regressions, but not a future production regression that bypasses the helpers or changes the inline conflict reset.
12. [probed] Doc-vs-code drift in ticket Rationale: the ticket rationale accurately describes guard, sync command, recovery reset, and PR comment threading; it does not claim the summary behavior that the Outcome requires.

Actionable findings

- `tools/delivery/closeout-stack.ts:337-366` - `formatCloseoutSummary` receives `config` but never includes `config.closeoutBranch`, so the operator-facing closeout summary does not name the resolved closeout target branch. This breaks the ticket outcome and is a demonstrable correctness gap. Fix by adding an explicit summary line such as `closeout_target=${config.closeoutBranch}` or by naming the branch in the closeout summary header, then add a test covering `closeoutBranch: "staging"`.

Advisory Observations

- `tools/delivery/test/p18-04.test.ts:10-32` only tests exported helpers. That is useful but weaker than the ticket’s red-test language about command sequence behavior; a focused adapter-driven `runCloseoutStack` test would better protect the production path.

- `docs/template/delivery/delivery-orchestrator.md:742` still says closeout pushes to `main`, produces commits on `main`, and resets to `origin/main`. The implementation now targets `config.closeoutBranch`, so this contract doc is stale.

Runner termination

runnerStatus: completed
terminatedReason: Completed a read-only adversarial review of the requested closeout branch behavior surfaces.
