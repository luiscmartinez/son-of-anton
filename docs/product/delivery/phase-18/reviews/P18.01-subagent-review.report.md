Now I have enough context to write the full adversarial review report.

---

**Invariant results**

**Invariant 1** [held] — `loadOrchestratorConfig` calls `requiredNonBlankString` for both `deliveryBaseBranch` and `closeoutBranch`, throwing a field-specific error for missing, null, non-string, or blank values. `resolveOrchestratorConfig({})` applies `raw.deliveryBaseBranch?.trim() || 'main'` and `raw.closeoutBranch?.trim() || 'main'`, defaulting all three roles to `'main'` when the absent-config path passes in `{}`.

**Invariant 2** [held] — `run_migration_2` computes `fallbackBranch = normalize(config.defaultBranch) ?? 'main'`, sets missing branch-role fields to `fallbackBranch`, and preserves non-blank existing string values via `normalize(config.deliveryBaseBranch) ?? fallbackBranch`. Non-string (e.g. numeric) values in existing config are silently replaced by `fallbackBranch`, consistent with "valid explicit" preservation. Blank `defaultBranch` degrades correctly to `'main'`.

**Invariant 3** [held] — The scaffold `cat` block (lines 271–290) writes `"defaultBranch"`, `"deliveryBaseBranch"`, and `"closeoutBranch"` all set to `"main"`. Package-manager and runtime detection logic is untouched.

---

**Surface results**

**Surface 1 — `loadOrchestratorConfig` validation ordering** [probed] — Validation runs: enum checks → `defaultBranch` (optional) → `deliveryBaseBranch` (required) → `closeoutBranch` (required) → retired-key checks → `reviewPolicy` / `prReviewAgents`. A config with both a missing required branch field and a retired key surfaces the branch error first; the user must fix in two passes. Invariant is not broken, but a consumer upgrading from a retired key while simultaneously missing branch fields gets confused ordering. Not a finding — both errors are ultimately caught.

**Surface 2 — `resolveOrchestratorConfig` defaulting** [probed] — `||` (not `??`) is used for all three branch roles. Because `loadOrchestratorConfig` already throws on blank strings, the only input path reaching `resolveOrchestratorConfig` with a blank is the `{}` empty-config case, where `undefined?.trim()` is `undefined` and `undefined || 'main'` is `'main'`. Clean. Separate field preservation verified by the `merges partial config with defaults` test.

**Surface 3 — `run_migration_2` JSON rewrite, idempotency, preservation, fallback** [probed] — JSON parse errors abort the sync (no try/catch; `set -euo pipefail` propagates node exit ≠ 0). Idempotency holds at the version-gate level (migration 2 only runs from version 1 → 2); if interrupted after the config write but before the version bump, re-running migration 2 sees already-normalized values and reproduces the same output. `normalize()` correctly treats non-string values as unusable (returns `undefined`), so numeric or null branch fields in the existing config are silently replaced by `fallbackBranch` rather than preserved — this is the right behavior for invalid values.

**Surface 4 — Consumer scaffold block** [probed] — All three branch-role fields are present in the heredoc. Package-manager priority order (pnpm → yarn → npm → bun) is unchanged from pre-ticket code. No regression.

**Surface 5 — `p17-01.test.ts` canonical-root expectation** [probed] — The `canonicalGitRoot` helper uses `git rev-parse --git-common-dir` to locate the worktree root; it resolves symlinks and handles worktrees. The assertion `expect(context.repo_root).toBe(canonicalGitRoot(process.cwd()))` is ticket-irrelevant (codogotchi context, not branch config) and does not mask a config migration regression. However, see Actionable Finding below regarding omitted fields in `enabledConfig()`.

**Diff-derived Surface 1 — Schema-version drift (v0→v1→v2, v1→v2)** [probed] — The p6-01 test starting from version 0 (no version file) now expects `.soa-sync-version` to read `'2'` after a run that moves review files (migration 1) and writes branch roles (migration 2). Both migrations run sequentially in the `while` loop. The v1→v2 path is the direct target of the new migration tests; the version file is confirmed as `'2'` in those tests. No gap found.

**Diff-derived Surface 2 — CLI flag/arg symmetry** [N/A — no new flags or arguments were added in this ticket.]

**Diff-derived Surface 3 — Error-class breadth in catch blocks** [N/A — no new catch blocks were introduced; `run_migration_2` delegates failure propagation to shell exit via `set -euo pipefail`.]

**Diff-derived Surface 4 — Defensive layering at module boundary (shell → Node inline)** [probed] — The Node script validates config shape (`typeof config !== 'object' || Array.isArray(config)`) before rewriting. Malformed JSON throws before any write, aborting the sync cleanly. An array config (e.g. `[]` in `orchestrator.config.json`) would be caught. Non-existent config exits early in the shell guard `if [ ! -f "$config" ]; then return`. Boundary is sound.

**Diff-derived Surface 5 — Cross-file atomicity (config write + version bump ordering)** [probed] — `run_migration_2` writes `orchestrator.config.json`, then `apply_migrations` calls `write_soa_version` after the migration loop completes. An interruption between these two steps leaves the config migrated but the version file at `1`, so migration 2 re-runs on next sync and reproduces identical output (idempotent). No data loss path identified.

**Diff-derived Surface 6 — Test-contract strength** [probed — one gap found; see Actionable Finding below.] Migration field-derivation, `defaultBranch` preservation, explicit-value preservation, and missing-`defaultBranch` fallback are all directly asserted. Migration-2 idempotency is implicitly covered by version-gating rather than by an explicit re-run assertion, which is adequate since migration 2 cannot run twice on a successfully migrated consumer without manual version file tampering.

**Diff-derived Surface 7 — Doc-vs-code drift in ticket Rationale** [probed] — Ticket Rationale states "sync migration v2 uses Node to parse and rewrite `orchestrator.config.json` structurally, preserving existing explicit values and deriving missing branch roles from `defaultBranch` or `main`." Code does exactly this. The ticket outcome statement "Migration falls back to `main` only when the existing config has no usable `defaultBranch`" matches the `normalize(config.defaultBranch) ?? 'main'` fallback. No doc-vs-code drift found.

---

**Actionable findings**

**Finding 1 — test base-config objects omit now-required `deliveryBaseBranch` / `closeoutBranch` fields (finding-discipline clause 2: demonstrable correctness gap)**

Files:
- `tools/delivery/test/runtime-config.test.ts` line 14 — `baseConfig: ResolvedOrchestratorConfig` omits both fields
- `tools/delivery/test/ticket-flow.test.ts` line 40 — `baseConfig: ResolvedOrchestratorConfig` omits both fields
- `tools/delivery/test/p10-01.test.ts` line 25 — `baseResolvedConfig: ResolvedOrchestratorConfig` omits both fields
- `tools/delivery/test/p17-01.test.ts` line 11 — `enabledConfig(): ResolvedOrchestratorConfig` return value omits both fields

What is wrong: `ResolvedOrchestratorConfig` now declares `deliveryBaseBranch: string` and `closeoutBranch: string` as required fields. Each of the above objects satisfies the type annotation at the declaration site only because the CI `verify` script runs `format:check && lint` — it does not run `tsc`. TypeScript language servers in IDEs will report missing-property errors. More critically, any test that spreads one of these helpers into an argument consumed by code that now reads `deliveryBaseBranch` or `closeoutBranch` will receive `undefined` instead of a string at runtime. The `closeout-stack.ts` and any PR-branching code that begins consuming these fields will silently get `undefined`, which could produce an empty or invalid git branch name without a thrown error.

Fix: Add `deliveryBaseBranch: 'main'` and `closeoutBranch: 'main'` to each base/helper config object. Since the ticket explicitly lists these four files as touched, the update was incomplete — the inline JSON test fixtures within test bodies were updated, but the shared top-level helper objects were not.

---

**Advisory Observations**

- `runtime-config.test.ts` lines 279 and 283 use double casts (`as never as { deliveryBaseBranch: string }`) to access `resolved.deliveryBaseBranch` and `resolved.closeoutBranch`. Now that `ResolvedOrchestratorConfig` declares these fields, the casts are unnecessary — `resolved.deliveryBaseBranch` is directly accessible. This is cosmetic but suggests the test was updated incrementally and the casts were not cleaned up after the type was finalized. Not a correctness issue.

- `scripts/soa-sync.sh` `run_migration_2` exits with code 1 when `node` is not available. Consumer repos running in minimal environments (e.g., Alpine-based Docker images without Node) would have their entire `soa-sync` call fail, even if only skill-symlinking was desired. The error message is clear (`node is required to migrate orchestrator.config.json branch roles`), so this is not silent, but it may surprise consumers in node-less environments. Outside ticket scope — advisory only.

- The `planRoot: '   '` blank-string test at `runtime-config.test.ts` line 231 catches blanks via `optionalNonBlankString`, but the orchestrator.config.json that triggers this test does not include the now-required `deliveryBaseBranch` / `closeoutBranch`. At runtime this test actually throws on `deliveryBaseBranch` missing before reaching the `planRoot` blank-string check, so it tests the wrong error — but the regex match `/Expected a non-blank string/` happens to match both the `deliveryBaseBranch` missing error (which says "Expected a non-blank string") and the intended `planRoot` blank error. The test passes for the wrong reason. This is a test-contract weakness, not a broken invariant. Recommend adding `deliveryBaseBranch: 'main', closeoutBranch: 'main'` to that fixture so the test actually reaches the `planRoot` validation it was written to cover.

---

**Runner termination**

`runnerStatus`: completed
`terminatedReason`: All invariants and attack surfaces were probed to completion with no external blockers; one actionable finding identified and advisory observations collected.
