You are conducting an adversarial review of a code change. Your job is not a general
code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

**P2.01 — apps/menubar Xcode project skeleton + toolchain seam (Red: skip, chore, 2 points).**

Outcome:

- `apps/menubar/Menubar.xcodeproj` exists and opens in Xcode without error.
- The project contains a single executable target `Menubar` (macOS, AppKit, Swift), with `LSUIElement = true` in the bundled `Info.plist` so the app runs as a menu-bar agent without a dock icon.
- `apps/menubar/Sources/` contains a minimal `MenubarApp.swift` that registers an `NSStatusItem` with a placeholder system-symbol image (e.g., `pawprint`) and a Quit menu item.
- `apps/menubar/Tests/MenubarTests/` exists with one passing smoke test (`testAppCanInstantiate`) so the test scheme is wired and `xcodebuild ... test` succeeds end-to-end.
- `apps/menubar/Fixtures/` and `apps/menubar/Resources/` directories exist (may contain only a `.gitkeep` for now).
- `apps/menubar/README.md` documents: how to open in Xcode, raw `xcodebuild` invocations for build and test, and an explicit "this app is a dev build only, no signing/notarization" note.
- Root `package.json` gains exactly two new scripts: `mac:build` and `mac:test`, both shelling out to `xcodebuild`.
- `biome.json` excludes `apps/**` from formatting and linting.
- `cspell.json` `ignorePaths` includes Swift sources and binary assets under `apps/**` but keeps `apps/**/*.md` indexed.
- `bun run mac:test` succeeds on the owner's machine.

Rationale-as-shipped (append):

- xcodegen used instead of Xcode GUI template; `project.yml` is source of truth, `Menubar.xcodeproj` generated and committed. New dev dep (`brew install xcodegen`).
- `Info.plist` generated by xcodegen from `info.properties` block; `GENERATE_INFOPLIST_FILE: NO` prevents Xcode overwrite.
- `MenubarTests` is `bundle.unit-test` depending on `Menubar`; xcodegen wires `TEST_HOST` / `BUNDLE_LOADER`; `@testable import Menubar` reaches `@main` class.

Red: skip — no behavior test required; the smoke test exists only to wire the test scheme.

### Files touched

Implementation:
apps/menubar/project.yml
apps/menubar/Info.plist
apps/menubar/Menubar.xcodeproj/project.pbxproj
apps/menubar/Menubar.xcodeproj/project.xcworkspace/contents.xcworkspacedata
apps/menubar/Menubar.xcodeproj/xcshareddata/xcschemes/Menubar.xcscheme
apps/menubar/Sources/MenubarApp.swift
apps/menubar/README.md
apps/menubar/Fixtures/.gitkeep
apps/menubar/Resources/.gitkeep
biome.json
cspell.json
package.json
docs/product/delivery/phase-02/ticket-01-repo-skeleton.md

Tests:
apps/menubar/Tests/MenubarTests/MenubarTests.swift

### Invariants to hold

1. The built `Menubar.app` runs as a true menu-bar agent: `LSUIElement` is `true` in the bundled `Info.plist`, the app does not register a Dock icon, and no main window is created. Anything that drops `LSUIElement`, sets it to `false`, or causes Xcode to regenerate Info.plist (overwriting it) breaks this invariant.
2. `bun run mac:test` and `bun run mac:build` succeed from a fresh shell at the repo root without any extra Xcode-GUI bootstrap step. Anything that requires opening Xcode first, accepting a signing prompt, or installing tooling beyond the documented `brew install xcodegen` breaks this invariant.
3. `bun run ci:quiet` (TS-only gate: biome + cspell) is green across the repo with `apps/**` Swift/plist/binary/xcodeproj content present. Anything that causes biome to choke on `.swift`/`.plist` content or cspell to flag tokens inside the generated project files breaks this invariant.

### Attack surfaces to probe

1. **`apps/menubar/Info.plist` LSUIElement persistence vs `GENERATE_INFOPLIST_FILE`** — Info.plist is committed but `project.yml` also declares `info.path: Info.plist` with `info.properties` including `LSUIElement: true`. If `GENERATE_INFOPLIST_FILE: NO` is missing on the `Menubar` target (per-target `settings.base`), or if xcodegen still auto-generates a parallel plist that wins at build time, the committed `LSUIElement = true` may not actually reach the built bundle. Probe: does the built `Menubar.app/Contents/Info.plist` after `xcodebuild build` contain `LSUIElement = true`? Does `defaults read .../Contents/Info LSUIElement` return `1`?
2. **`MenubarApp.main` + `applicationDidFinishLaunching` window/dock side effects** — `@main` class calls `NSApplication.shared.run()` after `setActivationPolicy(.accessory)`. Without a main window in `project.yml` (no storyboard, no SwiftUI scene), is there any code path that still instantiates a window or main menu that would force the Dock icon to appear? Specifically: does `NSApplication.shared.run()` with no main menu produce a default app menu that requests Dock activation, or does `.accessory` + `LSUIElement` keep it suppressed?
3. **`MenubarTests.testAppCanInstantiate` side effects** — the test does `MenubarApp()` (instantiates the `@main` class). Does the class's `init` (inherited from `NSObject`) do anything that would register an `NSStatusItem` or kick off application lifecycle? If `applicationDidFinishLaunching` ever runs during the test, the status item leaks into the test process. Probe: does instantiation alone register anything in `NSStatusBar.system`?
4. **`cspell.json` `ignorePaths` coverage vs the spellcheck glob** — the spellcheck script is `cspell lint --no-progress "**/*.{ts,md,json,txt}"`. The added `ignorePaths` entries cover `apps/**/*.swift`, `apps/**/*.plist`, `apps/**/*.webp`, `apps/**/*.png`, and `apps/**/Menubar.xcodeproj/**`. Are there *.json files generated under `Menubar.xcodeproj/` (e.g., `xcuserdata`, `xcshareddata`) that fall inside the spellcheck glob and outside the ignore pattern? Are tokens inside `apps/menubar/Info.plist` matched (`.plist` is in ignorePaths, but the glob doesn't include `.plist` either — verify both layers agree)? The ticket explicitly says "keeps `apps/**/*.md` indexed" — verify `apps/menubar/README.md` is still spellchecked after the change.
5. **`biome.json` `!apps` exclude scope** — the change is `"includes": ["**", "!.son-of-anton", "!convex/_generated", "!apps"]`. Does `!apps` correctly exclude *all* descendants of `apps/`, or does biome's includes semantics require `!apps/**` to recurse? If biome still descends into `apps/`, does it choke on Swift, plist, or pbxproj content? Probe `bun run verify:quiet` and inspect file-count delta.
6. **`package.json` script symmetry — `mac:build` vs `mac:test`** — both scripts hardcode `apps/menubar/Menubar.xcodeproj` and `-scheme Menubar`. If `project.yml` ever renames the scheme or project, these break silently. Probe: does the README also document the bare `xcodebuild ...` invocations consistently, so a reviewer can copy-paste either path? Are the script names exactly `mac:build` and `mac:test` as the ticket requires (no `mac:verify` or other added script)?

#### Diff-derived attack surfaces

Probe each of the seven diff-derived classes against this diff:

1. **Output stability across schema-version drift.** The only persisted artifact added is the xcodegen-generated `project.pbxproj`. xcodegen version is `2.45.4`. Probe: does running `xcodegen generate` again produce a byte-identical pbxproj (deterministic ordering)? If not, regeneration produces noise diffs that obscure real review.
2. **CLI flag/arg symmetry.** No new CLI flags added to the TS code. Probe: are the two new `package.json` scripts (`mac:build`, `mac:test`) registered in any wrapper, README, or `bun run ci` chain that needs updating? Confirm `bun run ci` stays TS-only (no Swift call) per the locked grill-me decision.
3. **Error-class breadth in `catch` blocks.** No new `catch` blocks in Swift (no error handling beyond OS-managed lifecycle). N/A unless a `try?` / `try!` was introduced.
4. **Defensive layering at module boundaries.** The new module boundary is the TS/Swift toolchain seam: TS `package.json` scripts invoke `xcodebuild`. Does the script fail loud when `xcodebuild` is missing or `apps/menubar/Menubar.xcodeproj` is missing, or does it silently exit 0? Probe: what is the exit code when run on a machine without Xcode?
5. **Cross-file atomicity windows.** The xcodegen workflow is two-step: `project.yml` is the source, `Menubar.xcodeproj` is generated. If `project.yml` is edited but `xcodegen generate` is not run before commit, the committed `.xcodeproj` and `project.yml` diverge. Probe: is there any guard (pre-commit hook, README warning, CI check) that prevents this drift? If not, this is a real workflow risk worth flagging.
6. **Test-contract strength.** The smoke test asserts `XCTAssertNotNil(delegate)` only. The ticket explicitly says this exists to wire the test scheme, not to express a behavior contract — so light assertion is by design. Probe: does the test target actually load `@testable import Menubar` correctly (test passed end-to-end), and does the test target's host-app `TEST_HOST`/`BUNDLE_LOADER` setting work without explicit `project.yml` override? If xcodegen's defaults change in a future version, this test could silently fail to load the app.
7. **Doc-vs-code drift in the ticket Rationale.** Read `docs/product/delivery/phase-02/ticket-01-repo-skeleton.md` Rationale (especially the as-shipped Append block) and compare to the diff. Specifically:
   - The Rationale claims `GENERATE_INFOPLIST_FILE: NO` is in build settings. Verify this in `project.yml`'s per-target `settings.base` for `Menubar`.
   - The Rationale claims `Info.plist` contains only "the four required keys (`LSUIElement`, `CFBundleName`, `CFBundleIdentifier`, `CFBundleShortVersionString` + `CFBundleVersion`) plus xcodegen's standard defaults." Verify against the committed `apps/menubar/Info.plist`.
   - The Rationale claims `MenubarTests` is `bundle.unit-test` depending on `Menubar` and xcodegen wires `TEST_HOST`/`BUNDLE_LOADER` automatically. Verify in `project.yml` and in the generated `project.pbxproj`.
   - The Outcome bullet says "Root `package.json` gains exactly two new scripts." Verify *only* `mac:build` and `mac:test` were added.

Output form per class: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.

### Diff context

This is the first native Swift surface in the repo. The change adds `apps/menubar/` containing an xcodegen-generated Xcode project for a menu-bar agent app. The Swift code is ~44 lines: a single `@main` class `MenubarApp` that registers an `NSStatusItem` with a placeholder `pawprint` system symbol and a single "Quit Menubar" menu item, and a 15-line `XCTestCase` smoke test that instantiates the delegate. The bulk of the diff is the generated `project.pbxproj` (405 lines) and the xcscheme (118 lines) — both deterministic xcodegen output, not hand-authored.

Config-side: `biome.json` excludes `apps/**` from biome's includes; `cspell.json` adds ignorePaths for Swift/plist/binary/xcodeproj under `apps/**` and three new dictionary words (`xcodegen`, `webp`, `pbxproj`, `INFOPLIST`, `diffable`); `package.json` adds `mac:build` and `mac:test` scripts. `bun run ci` stays TS-only per the locked grill-me decision; Swift verify (`bun run mac:test`) is a manual local step the owner runs and pastes into PR bodies.

The locked grill-me decision is "committed `.xcodeproj`. Resources, Info.plist (`LSUIElement=true`), and entitlements all Xcode-native." The implementation deviates by adding `project.yml` as a source artifact alongside the committed `.xcodeproj` — that deviation is recorded in the Rationale Append block. No entitlements file is present yet (no sandbox, no signing); this is consistent with the dev-build-only stance.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path. Keep added surfaces tied to the ticket behavior; do not turn
this into broad style, cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
