You are conducting an adversarial review of a code change. Your job is not a general
code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P2.04 Swift MaliPet asset loader + hardcoded row-map table. Outcome:
`apps/menubar/Sources/MaliPet.swift` exposes a `MaliPet` type with
`init(petDirectory:)` (default `~/.codex/pets/mali/`), reads `pet.json`
and `spritesheet.webp`, exposes a hardcoded `[ActivityState: RowSpec]`
table mapping each floor state to `(rowIndex: Int, frameCount: Int)`
in the 8×9 spritesheet grid, and `frames(for: ActivityState) -> [NSImage]`
slices per-state animation frames at runtime via `CGImage.cropping(to:)`.
Frame dimensions: width = sheetWidth/8, height = sheetHeight/9.
Loader tolerates WebP via `NSImage(contentsOfFile:)`; document workaround
in Rationale if unstable. `apps/menubar/Fixtures/mali/` ships pet.json +
spritesheet.webp so tests don't touch `~/.codex/`. Tests assert: load
succeeds from fixtures, `frames(for: .implementing)` returns non-empty
with first frame at expected pixel dimensions, all four floor states
yield non-empty arrays, missing pet directory throws a named
`MaliPetLoadError` case. Swift notes at
`notes/private/phase-02-swift-notes/P2.04-mali-pet-loader.md` explain
NSImage/CGImage, `cropping(to:)` rect coords, and slicing in TS terms.

Rationale notes: row map is owner-confirmed (idle=0, implementing=7,
runningTests=8, celebrating=4). Frame counts (8/6/4/5) are visual
leading-cell estimates; wrong counts are visually obvious in P2.05.
WebP loaded cleanly via `NSImage(contentsOfFile:)` on macOS 13+, no
workaround needed. `bun run ci` extended to chain `bun run mac:test`
so the orchestrator's post-red gate observes Swift compile/test
failures.

### Files touched

Implementation:
apps/menubar/Sources/MaliPet.swift
apps/menubar/Menubar.xcodeproj/project.pbxproj
package.json

Tests:
apps/menubar/Tests/MenubarTests/MaliPetTests.swift
apps/menubar/Fixtures/mali/pet.json
apps/menubar/Fixtures/mali/spritesheet.webp

Docs:
docs/product/delivery/phase-02/ticket-04-mali-pet-loader.md (Rationale append only)
notes/private/phase-02-swift-notes/P2.04-mali-pet-loader.md

### Invariants to hold

1. For every `ActivityState` in `{.idle, .implementing, .runningTests, .celebrating}`,
   `MaliPet.frames(for: state)` returns a non-empty `[NSImage]` whose elements all have
   `CGImage` width == `sheetWidth / 8` and height == `sheetHeight / 9`.
2. `MaliPet(petDirectory: <missing-or-empty-dir>)` throws a named `MaliPetLoadError` case
   (`petJsonNotFound`, `spritesheetNotFound`, `petJsonMalformed`, `spritesheetUnreadable`,
   or `spritesheetIncompatibleGrid`), never crashes and never returns a partially-
   constructed instance.
3. The fixture-based test suite at `apps/menubar/Tests/MenubarTests/MaliPetTests.swift`
   runs to green using only files inside `apps/menubar/Fixtures/mali/` and does not
   require `~/.codex/pets/mali/` to exist or be readable.

### Attack surfaces to probe

1. `MaliPet.init(petDirectory:)` pet.json error mapping: when the directory exists but
   `pet.json` is missing, the loader throws `petJsonNotFound`. When the directory itself
   is missing (no `pet.json` reachable), does the error case still resolve to
   `petJsonNotFound` (because `Data(contentsOf:)` fails first), or does an earlier
   unhandled error escape the case mapping?
2. `MaliPet.init(petDirectory:)` PetManifest decoding: `JSONDecoder` with
   `keyDecodingStrategy = .convertFromSnakeCase` is applied even though the canonical
   `pet.json` already uses camelCase keys (`displayName`, `spritesheetPath`). Does this
   strategy swallow a pet.json that uses snake_case keys instead, masking schema drift?
   Probe whether a malformed pet.json still hits the intended `petJsonMalformed` path
   rather than an unexpected exception class.
3. `MaliPet.init(petDirectory:)` spritesheet path resolution: `manifest.spritesheetPath`
   is appended to `petDirectory` via `appendingPathComponent`. If `pet.json` declares an
   absolute path or a path containing `..`, can the loader load assets outside the pet
   directory (path-traversal class), and does the test fixture survive that resolution?
4. `MaliPet.frames(for:)` `CGImage.cropping(to:)` bounds: frame rect is computed as
   `(col * frameWidth, row * frameHeight, frameWidth, frameHeight)` using **integer
   division** of `cg.width / 8` and `cg.height / 9`. If `cg.width` or `cg.height` is not
   evenly divisible by 8 / 9, do trailing-column / bottom-row frames slip off the sheet
   bounds and trigger `cropping(to:)` returning `nil` (which the loader silently skips
   via `guard let`)? Does this cause `frames(for: .runningTests)` (row 8 — the bottom
   row, most exposed to the off-by-one risk) to ever return an empty array?
5. `MaliPet.init(petDirectory:)` grid-compatibility gate (`cg.width >= 8`,
   `cg.height >= 9`): this only rejects sheets smaller than 8×9 pixels — any larger sheet
   passes regardless of grid layout. A sheet that is, e.g., 7 columns × 9 rows wide
   would silently slice misaligned frames. Is this gate too weak to enforce the 8×9 grid
   invariant the loader documents?
6. `MaliPet.rowMap` row-map correctness: row indices are owner-confirmed (idle=0,
   implementing=7, runningTests=8, celebrating=4). Frame counts (8/6/4/5) are visual
   estimates of leading non-magenta cells. There are no tests that verify the frame
   contents are non-magenta — only that the array is non-empty. Could an off-by-one
   frame count cause the rendered animation to include unused magenta cells (Phase 02
   visual bug) while tests stay green?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `pet.json` adds no schema-version
   field; `PetManifest` accepts any JSON that decodes to {id, displayName,
   spritesheetPath}. Future schema additions (e.g. `schemaVersion: 2` with
   incompatible row layout) would silently load and animate wrong frames. Probe whether
   the loader has any version marker, and whether the absence of one is acceptable for
   Phase 02. Output form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
2. **CLI flag/arg symmetry** — this ticket adds no CLI flags. Output form:
   `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
3. **Error-class breadth in `catch` blocks** — `MaliPet.init` has two `catch` blocks
   (pet.json read, manifest decode) that swallow the underlying error and rethrow a
   named `MaliPetLoadError` case. Does this lose diagnostic detail (e.g. distinguishing
   "directory missing" from "permission denied" — both currently collapse to
   `petJsonNotFound`)? Probe whether the swallowed error class affects production
   debuggability. Output form: `[probed]` / `[N/A — reason]` / `[blocked —
   missing-input]`.
4. **Defensive layering at module boundaries** — `MaliPet` is the only module producing
   frames for the P2.05 renderer. Does the loader's contract with downstream renderer
   leave any invariants unenforced at this boundary (e.g. all sliced frames sharing the
   same dimensions, or `cgSheet` retaining the underlying memory for the lifetime of
   the sliced views)? Output form: `[probed]` / `[N/A — reason]` / `[blocked —
   missing-input]`.
5. **Cross-file atomicity windows** — the loader does no multi-step writes. Output
   form: `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
6. **Test-contract strength** — `testLoaderThrowsWhenDirectoryMissing` matches on a
   loose union (`petJsonNotFound` OR `spritesheetNotFound`). Is this assertion too
   loose to catch a regression where the error case shifts between those two? The
   frame-count assertions check only `isEmpty == false`, not the expected count. Probe
   whether the test contract is strong enough to catch (a) wrong error case, (b) wrong
   frame count, (c) wrong frame dimensions for non-implementing states. Output form:
   `[probed]` / `[N/A — reason]` / `[blocked — missing-input]`.
7. **Doc-vs-code drift in the ticket Rationale** — read the ticket Rationale and the
   Swift notes file. The ticket scope says "Frame width = spritesheet width / 8;
   frame height = spritesheet height / 9" and "first element has the expected pixel
   dimensions (rowHeight × frameWidth)". The Rationale claims `bun run ci` was
   extended to chain `mac:test` — verify this is true. The Swift notes file describes
   `CGImage.cropping(to:)` as top-left origin. Surface any mismatch under **Findings
   for human review**. Output form: `[probed]` / `[N/A — reason]` / `[blocked —
   missing-input]`.

### Diff context

The diff is three commits on `agents/p2-04-...`:

1. `chore(ci)`: extends `ci` and `ci:quiet` in root `package.json` to chain
   `bun run mac:test` so the orchestrator's `post-red` gate observes Swift
   compile/test failures.
2. `test(P2.04): ... [red]`: copies pet fixtures into `apps/menubar/Fixtures/mali/`,
   adds `MaliPetTests.swift`, regenerates xcodeproj to wire the new test file.
   Compile-fails by design (no `MaliPet` type yet).
3. `feat(P2.04)`: adds `MaliPet.swift` (the full loader), regenerates xcodeproj,
   adds `notes/private/phase-02-swift-notes/P2.04-mali-pet-loader.md`, appends a
   Rationale block to the ticket doc.

Key logic:
- `init(petDirectory:)` reads `pet.json` (camelCase), reads spritesheet via
  `NSImage(contentsOfFile:)`, derives `cgSheet`, asserts grid >= 8×9, stores
  per-frame dimensions = integer division of sheet by 8/9.
- `frames(for:)` looks up `rowMap[state]`, computes frame rects with top-left
  origin pixel coordinates, calls `cgSheet.cropping(to:)` per frame, wraps each
  slice in `NSImage(cgImage:size:)`.
- Row map: idle=0,8 / implementing=7,6 / runningTests=8,4 / celebrating=4,5.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Findings for human review** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path. Keep added surfaces tied to the ticket behavior; do not turn
this into broad style, cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Findings for human review** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Findings for human review**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
