stdout:
I have all the information needed. Here is the complete adversarial review.

---

## Adversarial Review — P2.01 apps/menubar Xcode project skeleton + toolchain seam

---

### Invariant results

**Invariant 1 — `LSUIElement = true` persists in the built bundle.**
`[held]` — `GENERATE_INFOPLIST_FILE: NO` is present in `project.yml` under `targets.Menubar.settings.base` (line 36). The committed `Info.plist` contains `<key>LSUIElement</key><true/>`. xcodegen's `info.path: Info.plist` with `info.properties: LSUIElement: true` writes the property list to the committed path; `GENERATE_INFOPLIST_FILE: NO` prevents Xcode from substituting its own. The pbxproj confirms `GENERATE_INFOPLIST_FILE = NO` on both the Debug (line 184) and Release (line 301) build configurations for the `Menubar` target. Build-time LSUIElement suppression requires no extra Xcode-GUI step.

**Invariant 2 — `bun run mac:build` / `bun run mac:test` work from a fresh shell without Xcode-GUI bootstrap.**
`[held]` — Both scripts use only `xcodebuild` with fully qualified project path and scheme. The committed `.xcodeproj` needs no regeneration step before use; `project.yml` and the committed `.xcodeproj` are in sync as shipped. `CODE_SIGN_IDENTITY: "-"` (ad-hoc) with `DEVELOPMENT_TEAM: ""` avoids any signing-identity prompt. No storyboard, no `.xib`, no resource that requires a GUI-only step.

**Invariant 3 — `bun run ci:quiet` stays green with `apps/**` content present.**
`[held — currently]` — biome's `ignoreUnknown: true` means it silently skips file types it cannot parse (Swift, plist, pbxproj, xcscheme, XML). No `.ts`/`.js`/`.json` files exist under `apps/menubar/`. cspell's spellcheck glob `"**/*.{ts,md,json,txt}"` does not match `.swift`, `.plist`, or `.pbxproj`; those are excluded by the glob itself, not only by `ignorePaths`. The one `.md` file (`README.md`) is indexed by design and its tokens (`LSUIElement` → `LSUI` + `Element`; both allowed) pass cspell.

---

### Surface results

**1 — `Info.plist` LSUIElement persistence vs `GENERATE_INFOPLIST_FILE`**
`[probed]` — `GENERATE_INFOPLIST_FILE: NO` is confirmed in `project.yml` (line 36) and in the generated `pbxproj` for both build configs (lines 184, 301). The `info.path` key points to the committed `Info.plist`; xcodegen writes `LSUIElement: true` into it. No parallel auto-generated plist exists in `project.yml`. The attack surface is closed.

**2 — `MenubarApp.main` + `applicationDidFinishLaunching` dock/window side effects**
`[probed]` — `static func main()` calls `app.setActivationPolicy(.accessory)` before `app.run()`. With `LSUIElement = true` in the bundle's `Info.plist`, macOS applies the accessory policy before the app even reaches `applicationDidFinishLaunching`. No storyboard, no `NSWindowController`, no SwiftUI scene manifest exists in the project. `applicationDidFinishLaunching` only calls `NSStatusBar.system.statusItem(withLength:)` and sets a menu — no window creation. The dock suppression path is doubly defended (plist + activation policy).

**3 — `MenubarTests.testAppCanInstantiate` side effects**
`[probed]` — The test calls `MenubarApp()`, which is just `NSObject.init()` — no custom `init` defined. `applicationDidFinishLaunching` is a delegate callback; it only fires when the `NSApplication` run loop delivers the `NSApplicationDidFinishLaunchingNotification`. The test host is `Menubar.app` (loaded by XCTest as a bundle), but the app's `main()` entry point is not re-invoked during testing. `MenubarApp()` does not touch `NSStatusBar.system`; no status item leaks. The smoke test is side-effect-free at instantiation time.

**4 — `cspell.json` `ignorePaths` coverage vs spellcheck glob**
`[probed]` — The spellcheck glob is `"**/*.{ts,md,json,txt}"`. Files with extensions `.swift`, `.plist`, `.pbxproj`, `.xcscheme`, `.xcworkspacedata` are never matched by this glob; `ignorePaths` for those is belt-and-suspenders, not load-bearing. No `.json` files exist under `apps/menubar/Menubar.xcodeproj/` (the pbxproj uses its own text format; `xcuserdata` and `xcshareddata` contain `.xcscheme` XML, not JSON). `apps/menubar/README.md` is `.md`, matched by the glob, and NOT in `ignorePaths` — it is indexed as required by the ticket. All tokens in `README.md` that could be flagged (`LSUIElement`, `xcodegen`, `pawprint`, `pbxproj`, `INFOPLIST`) are covered by the `words` list. No spellcheck gap.

**5 — `biome.json` `!apps` exclude scope**
`[probed — latent pattern risk, no current breakage]` — The `includes` entry is `"!apps"` (no trailing `/**`). In biome 2.x glob semantics, `!apps` matches the literal directory entry named `apps` at the project root and may not unconditionally recurse-exclude all descendants without `!apps/**`. However, `ignoreUnknown: true` means biome silently skips any file type it cannot parse (Swift, plist, XML). No `.ts`/`.js`/`.json` files currently exist under `apps/`. Present-day `bun run ci:quiet` is green. **This is a latent pattern risk**: if a future ticket adds a `.json` file under `apps/` (e.g., a test fixture or config), biome may process it with the wrong rules. The pattern should arguably be `!apps/**` for clarity. See **Findings for human review**.

**6 — `package.json` script symmetry — `mac:build` vs `mac:test`**
`[probed]` — Exactly two new scripts exist: `mac:build` (line 23) and `mac:test` (line 24). Both shell out to `xcodebuild -project apps/menubar/Menubar.xcodeproj -scheme Menubar`. The README documents the identical bare `xcodebuild` invocations (lines 22–27) — copy-pasteable and consistent. No `mac:verify` or other added script. `bun run ci` stays TS-only. Ticket requirement satisfied exactly.

**Diff-derived surface 1 — Output stability / pbxproj determinism**
`[probed — cannot fully verify without running xcodegen, but no evidence of non-determinism]` — xcodegen 2.x uses deterministic UUID assignment seeded from target/file names and stable sort order for file groups. The committed pbxproj has no hand-edits. Without running `xcodegen generate` in this review I cannot byte-compare, but the known xcodegen behavior for this version is deterministic within the same version. If drift occurs it would surface on first regeneration as a noisy diff — a workflow friction item, not a correctness break. See **Findings for human review**.

**Diff-derived surface 2 — CLI flag/arg symmetry**
`[probed]` — The two new scripts are the only additions to `package.json`. No wrapper, no CI chain, no Makefile references them. `bun run ci` does not call `mac:*`. README documents both the raw `xcodebuild` and the `bun run` aliases. Symmetric.

**Diff-derived surface 3 — Error-class breadth in `catch` blocks**
`[N/A — no `try`/`catch`/`try?`/`try!` in the Swift code; no new error handling paths.]`

**Diff-derived surface 4 — Defensive layering at the TS/Swift seam**
`[probed]` — `xcodebuild` is a standard macOS CLI binary. On a machine without Xcode, it exits non-zero with a diagnostic error message to stderr. `bun run mac:test` will therefore propagate the non-zero exit code and fail loudly — no silent exit-0 path. Acceptable; the README and ticket both state this is dev-only with no CI enforcement.

**Diff-derived surface 5 — Cross-file atomicity: `project.yml` vs committed `.xcodeproj` drift**
`[probed — real workflow risk, no guard exists]` — No pre-commit hook, no CI check, and no `bun run` script enforces that `project.yml` and the committed `.xcodeproj` are in sync. The README documents the regeneration workflow (`cd apps/menubar && xcodegen generate`) but does not enforce it. If `project.yml` is edited and `xcodegen generate` is not re-run, the committed `.xcodeproj` silently diverges. See **Findings for human review**.

**Diff-derived surface 6 — Test-contract strength and `TEST_HOST`/`BUNDLE_LOADER` wiring**
`[probed]` — The pbxproj contains `TEST_HOST = "$(BUILT_PRODUCTS_DIR)/Menubar.app/Contents/MacOS/Menubar"` and `BUNDLE_LOADER = "$(TEST_HOST)"` on both Debug and Release configs for `MenubarTests` (lines 199/209 and 282/292). `@testable import Menubar` will reach `MenubarApp`. If xcodegen changes its default TEST_HOST wiring in a future version and `project.yml` has no explicit override, a regeneration could silently drop the test-host settings. This is acknowledged as speculative; the explicit `dependencies: - target: Menubar` in `project.yml` should continue to trigger xcodegen's test-host injection. No explicit `TEST_HOST` override in `project.yml` — relies on xcodegen default behavior.

**Diff-derived surface 7 — Doc-vs-code drift in ticket Rationale**
`[probed — three sub-claims checked, one minor over-claim found]`

- *Claim: `GENERATE_INFOPLIST_FILE: NO` in build settings.* **Verified.** `project.yml` line 36: `GENERATE_INFOPLIST_FILE: NO`. pbxproj lines 184 and 301 confirm both Debug and Release configs. ✓
- *Claim: Info.plist contains "only the four required keys … plus xcodegen's standard defaults."* **Verified with minor drift.** The committed `Info.plist` has 8 keys: the 5 stated (`LSUIElement`, `CFBundleName`, `CFBundleIdentifier`, `CFBundleShortVersionString`, `CFBundleVersion`) plus xcodegen's standard additions (`CFBundleDevelopmentRegion`, `CFBundleExecutable`, `CFBundleInfoDictionaryVersion`, `CFBundlePackageType`). The Rationale says "four required keys" but the `info.properties` block in `project.yml` actually declares **five** keys (`LSUIElement` + the four bundle keys). This is a trivial miscounting in the Rationale prose (5 ≠ 4), not a code defect. The committed plist is correct. See **Findings for human review**.
- *Claim: `MenubarTests` is `bundle.unit-test` depending on `Menubar`; xcodegen wires `TEST_HOST`/`BUNDLE_LOADER` automatically.* **Verified.** `project.yml` line 37: `type: bundle.unit-test`. `dependencies: - target: Menubar` at line 43. pbxproj lines 199/209/282/292 confirm xcodegen did inject `TEST_HOST`/`BUNDLE_LOADER`. ✓
- *Claim: "Root `package.json` gains exactly two new scripts."* **Verified.** `mac:build` and `mac:test` are the only additions. ✓

---

### Actionable findings

**None.** All three invariants hold. No code path breaks a stated invariant, and no correctness gap that passes the three finding-discipline clauses was found.

---

### Findings for human review

1. **`biome.json`: `"!apps"` should be `"!apps/**"`** — The current pattern `"!apps"` may not recursively exclude all files under `apps/` in biome 2.x depending on glob engine behavior. `ignoreUnknown: true` prevents current breakage (no `.ts`/`.js`/`.json` files exist there now), but the pattern is fragile: any future `.json` file added under `apps/` could be processed by biome with unexpected results. Recommend changing to `"!apps/**"` for defensive clarity.

2. **`project.yml` / `.xcodeproj` drift — no enforcement guard** — There is no pre-commit hook, CI step, or script that detects when `project.yml` is edited without re-running `xcodegen generate`. The README documents the regeneration workflow, but a developer could commit a `project.yml` change with a stale `.xcodeproj`. The Rationale acknowledges the trade-off but does not recommend a mitigation. Consider a one-liner pre-commit check (e.g., `diff <(xcodegen generate --spec apps/menubar/project.yml --dry-run) apps/menubar/Menubar.xcodeproj/project.pbxproj`) or at minimum a README callout in the commit section.

3. **Rationale minor miscounting** — The Append block states "only the four required keys (`LSUIElement`, `CFBundleName`, `CFBundleIdentifier`, `CFBundleShortVersionString` + `CFBundleVersion`)." That is five keys, not four. The committed `Info.plist` is correct; only the Rationale prose miscounts. Worth a one-word fix (`five`) to keep the doc trustworthy.

4. **`project.yml` does not declare an explicit `TEST_HOST`/`BUNDLE_LOADER` override** — xcodegen currently injects these settings automatically for a `bundle.unit-test` target with a named dependency. If a future xcodegen version changes that default, the test target would silently lose the host-app link and `@testable import Menubar` would fail at link time with no build error at generation time. Low-probability, but an explicit `TEST_HOST` entry in `project.yml`'s `MenubarTests.settings.base` would make the contract explicit and version-proof.

---

### Runner termination

`runnerStatus`: `completed`
`terminatedReason`: All attack surfaces were probed from the committed source; the review completed normally with no early stops.
