You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** -
anything off-scope but real is welcome there.
Your job is not a general code review - it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Outcome:
- A SpriteKit-backed floating scene/controller can render the active pet frames at desktop scale without creating the floating panel yet.
- The scene has explicit layers for pet sprite content and future overlays, leaving room for HP hearts, XP bar, stage badge, and loot effects in later phases.
- The scene can update to any existing `ActivityState` served by the current Codex/codogotchi loaders.
- The scene supports desaturated visual mode or an equivalent failure visual consistent with the menu bar renderer.
- Frame selection and state transitions are testable without opening an `NSPanel`.
- No HP, XP, stage, loot, particle, or character-sheet UI ships in this ticket.

Rationale notes:
- Red first added tests for idle/codogotchi-sheet state frame resolution, state-transition frame reset, missing codogotchi-frame fallback, and sizing.
- Implementation added `FloatingPetScene` as a SpriteKit-only scene boundary with injected pet loaders, so frame resolution and state transitions are testable without creating an `NSPanel`.
- Reusing `MenubarRenderer` directly was avoided because it would couple SpriteKit scene concerns to `NSStatusItem` image sinks and timer behavior.
- Production panel/window ownership, menu wiring, and live state fanout stay in P4.04/P4.05.
- The scene exposes named pet and overlay layers now, but overlay UI remains intentionally empty for this ticket.

### Files touched

Implementation:
- `apps/menubar/Sources/FloatingPetScene.swift`
- `apps/menubar/Codogotchi.xcodeproj/project.pbxproj`
- `docs/product/delivery/phase-04/ticket-03-spritekit-floating-scene-foundation.md`
- `docs/product/delivery/phase-04/reviews/P4.02-subagent-review.ledger.json` (mechanical formatter-only gate fix)

Tests:
- `apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift`

### Invariants to hold

1. `FloatingPetScene.update(state:visualMode:)` must resolve non-empty frames for Codex-sheet states and codogotchi-sheet states through the same loader precedence as the menu bar renderer, and must fall back to Codex idle frames when neither loader can serve the requested state.
2. A state change must reset `frameIndex` to 0 and repaint the new state's first frame without requiring an `NSPanel`, `NSStatusItem`, or application event loop.
3. The SpriteKit boundary must remain scene-only: it may create a transparent `SKScene`, named pet/overlay layers, and a sprite node, but it must not create or own any AppKit panel/window/menu behavior or ship visible overlay UI.

### Attack surfaces to probe

1. `FloatingPetScene.resolveFrames(for:)` - fallback ordering bug: probe whether Codex rows win for Codex-served states, codogotchi rows serve SoA-owned states, and missing codogotchi rows fall back to idle rather than leaving an empty sprite/crashing.
2. `FloatingPetScene.update(state:visualMode:)` and `advanceFrame()` - frame-index reset bug: probe repeated updates, same-state mode changes, and state switches after frame advancement for stale index or out-of-range access.
3. `FloatingPetScene.paintCurrentFrame()` / `fittedSpriteSize(for:)` - rendering and sizing bug: probe empty frame arrays, zero-sized scenes/images, desaturated texture generation fallback, and whether supplied scene size controls layer positioning without distorting state resolution.
4. `FloatingPetScene` initialization and `size.didSet` - scene-boundary bug: probe whether initialization/layout requires an `NSPanel`/`NSApplication` side effect, or whether overlay layers become visible content contrary to ticket scope.
5. Xcode project wiring - target-membership bug: probe whether the new source and tests are actually included in the app/test targets and whether adding SpriteKit introduces broader target settings or resource churn.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** - [N/A - no persisted schema or runtime JSON shape is introduced by the SpriteKit scene; the P4.02 ledger change is formatter-only and should be checked for semantic no-op.]
2. **CLI flag/arg symmetry** - [N/A - no CLI flags or args changed.]
3. **Error-class breadth in `catch` blocks** - [N/A - no new `catch` blocks were added.]
4. **Defensive layering at module boundaries** - [probed] New boundary crosses from tests/scene into `MaliPet` and `CodogotchiPet` loaders via injected instances; probe whether the scene defensively handles empty frame results and nil codogotchi pet rather than assuming loaders always serve frames.
5. **Cross-file atomicity windows** - [N/A - no runtime multi-file writes were added; only source/test/project/docs/review artifacts changed in git.]
6. **Test-contract strength** - [probed] Tests assert state, frame counts, reset behavior, fallback behavior, and layer positions without an `NSPanel`; probe whether they fail if the test file is not target-wired or if the implementation only satisfies Codex rows.
7. **Doc-vs-code drift in the ticket Rationale** - [probed] Compare `docs/product/delivery/phase-04/ticket-03-spritekit-floating-scene-foundation.md` rationale against `FloatingPetScene.swift` and tests; surface mismatches under Advisory Observations.

### Diff context

This ticket adds `FloatingPetScene`, a `@MainActor` `SKScene` with injected `MaliPet` and optional `CodogotchiPet`, transparent background, named `pet` and `overlays` layers, and one `SKSpriteNode`. It resolves frames in the same order as `MenubarRenderer`: Codex frames first, codogotchi frames second, then Codex idle fallback. `update(state:visualMode:)` resets frame index only on state changes or empty frames, repaints immediately, and supports desaturated mode by passing the current frame through `CIColorControls` before creating an `SKTexture`. Tests cover idle/Codex resolution, codogotchi-sheet resolution, frame reset after manual advancement, missing codogotchi sheet idle fallback, and scene/layer sizing without opening a panel. The Xcode project adds the new scene file to the app target and the new test file to the test target.

The branch also includes two delivery-artifact/doc changes: the P4.03 ticket rationale was appended to document the scene-only boundary and deferrals, and `docs/product/delivery/phase-04/reviews/P4.02-subagent-review.ledger.json` was mechanically formatted because `bun run verify:quiet` checks the whole tree and failed on the inherited ledger formatting.

Verification already run by the primary agent:
- `bun run mac:test` passed: 100 tests.
- `bun run verify:quiet` passed.
- `bun run ci:quiet` passed: `verify:quiet`, `bun test packages convex` (197 pass), and `mac:test` (100 pass).

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only - no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift - the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe -
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only - no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` - one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A - <reason> | blocked - missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
