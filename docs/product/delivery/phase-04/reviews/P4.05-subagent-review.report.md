**Invariant results**
`[broken]` Invariant 1: I traced live polling through [`MenubarApp`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/MenubarApp.swift:214>) into [`PetStateFanout.apply`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/PetStateFanout.swift:18>) and then into the floating scene path; the shared fanout exists, but a `.desaturated` live-failure update can still leave the floating pet showing the previous healthy colored texture because [`FloatingPetScene.paintCurrentFrame`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/FloatingPetScene.swift:115>) returns early on desaturation failure.

`[held]` Invariant 2: I checked the demo wiring in [`MenubarApp`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/MenubarApp.swift:183>) and the new fanout unit tests, then ran the targeted `xcodebuild` suite; demo updates go through `stateFanout.applyDemo(state:)`, which forces `.normal` to both surfaces.

`[held]` Invariant 3: I inspected [`FloatingPetController.apply`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/FloatingPetController.swift:63>), [`FloatingPetPanelController.apply`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/FloatingPetPanel.swift:44>), and `show()`, and ran the targeted controller tests; hidden/pre-scene updates do not crash, cache correctly at the panel layer, and transition-log ownership remains only in the live/demo drivers.

**Surface results**
`[probed]` `PetStateFanout.apply` / `applyDemo`: the seam is genuinely shared. Live uses `apply(state:visualMode:)` and demo uses `applyDemo(state:)`, which delegates to the same two-target path with `.normal`; I found no driver-specific mode skew here.

`[probed]` `MenubarApp.applicationDidFinishLaunching` demo/live wiring: both `DemoCycleDriver` and `LivePollingDriver` now feed closures that call `stateFanout`, and I found no remaining direct renderer-only update path in those two launch branches. The only direct `renderer.update` left is the initial idle bootstrap before either driver starts.

`[probed]` `FloatingPetController.apply` and `FloatingPetPanelController.apply`: hidden-panel updates are safe. `FloatingPetController.apply` is a straight pass-through, and `FloatingPetPanelController.apply` updates cached state/mode before optionally touching `scene`, so hidden or pre-scene updates are no-ops only at the rendering edge, not state-loss points.

`[probed]` `FloatingPetPanelController.show`: late scene creation replays cached `currentState/currentMode` into the newly created scene before presentation, so the code path honors “show latest known state, not default idle.” I did not find a direct regression in code, but there is still no scene-level test covering this replay.

`[probed]` `LivePollingDriver` transition-log ownership boundary: the floating surface does not participate in logging. `LivePollingDriver` still records transitions only from `lastAgentState` on successful reads, and the fanout closure carries only render state/mode.

`[probed]` Output stability across schema-version drift: the only persisted-shape touch in scope is [`P4.04-subagent-review.ledger.json`](</Users/cesar/code/codogotchi_p4_05/docs/product/delivery/phase-04/reviews/P4.04-subagent-review.ledger.json:21>), and that change is formatting-only. I found no app-state schema or reader contract drift caused by this ticket.

`[N/A - no CLI surface changed]` CLI flag/arg symmetry: no CLI entrypoint, flag, or command contract changed in this ticket, and the Xcode project regeneration only added source membership.

`[probed]` Error-class breadth in `catch` blocks: the new hidden-update path adds no new `catch`/swallow behavior. The existing persistence swallow in `setFloatingPetVisible` is unchanged, while `apply`/`show` themselves do not silently discard thrown errors because they do not throw.

`[probed]` Defensive layering at module boundaries: the new controller/panel seam is defensive about absent `scene` and the app/fanout seam uses weak captures, but the app also holds strong references to `renderer` and `floatingPetController`, so I did not find a normal-flow nil-deref or lost-update boundary here.

`[probed]` Cross-file atomicity windows: fanout is synchronous and non-throwing, so there is no async split where one surface commits and the other resumes later. The ticket-relevant exception is the floating scene’s internal desaturation fallback, which can still leave the two surfaces visually diverged on a live-failure update.

`[probed]` Test-contract strength: the new tests cover the fanout seam and hidden-controller forwarding, and the targeted `xcodebuild` run passed. They do not cover the late-show replay path, and the existing [`FloatingPetSceneTests.testDesaturationFailureKeepsPreviousTexture`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift:109>) currently asserts behavior that conflicts with this ticket’s “don’t look healthy on failure” outcome.

`[probed]` Doc-vs-code drift in the ticket Rationale: the rationale claims a shared `PetStateFanout`, hidden/pre-scene caching, deferred scene-level replay assertions, and regenerated Xcode membership; all of that matches the code and project file. I found no ticket-rationale drift in the touched delivery docs.

**Actionable findings**
[`apps/menubar/Sources/FloatingPetScene.swift:115`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/FloatingPetScene.swift:115>), with the behavior codified by [`apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift:109`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Tests/MenubarTests/FloatingPetSceneTests.swift:109>): when a live-polling failure fans out `.desaturated`, the floating scene logs and keeps the previous colored texture if grayscale generation fails. That breaks invariant 1 and finding-discipline clause 1 because the menu bar can show failure while the desktop pet still looks healthy. Fix by making the floating scene switch to a deterministic non-healthy failure representation when desaturation fails instead of preserving the prior normal texture, and update the test to assert that failure rendering is visibly non-healthy rather than unchanged.

**Advisory Observations**
[`apps/menubar/Sources/FloatingPetPanel.swift:22`](</Users/cesar/code/codogotchi_p4_05/apps/menubar/Sources/FloatingPetPanel.swift:22>) implements the deferred “hidden panel later shows most recent state” behavior correctly, but the rationale in [`ticket-05-shared-live-demo-state-fanout.md`](</Users/cesar/code/codogotchi_p4_05/docs/product/delivery/phase-04/ticket-05-shared-live-demo-state-fanout.md:56>) is accurate that there is still no scene-level assertion for it. That is doc-aligned, not a drift bug.

**Runner termination**
`runnerStatus`: `completed`
`terminatedReason`: Completed the scoped adversarial review with source inspection and a targeted `xcodebuild` test run.
