**Invariant results**
`[held]` Invariant 1: I traced `FloatingPetInteractionView.hitTest`, `layout`, and `mouseDown` in [apps/menubar/Sources/FloatingPetPanel.swift](/Users/cesar/code/codogotchi_p4_06/apps/menubar/Sources/FloatingPetPanel.swift:152). The container now always returns `self` for in-bounds hit tests, so the full-size `SKView` and the affordance subview cannot steal the initial mouse event before drag/resize selection runs.

`[held]` Invariant 2: I checked `FloatingInteractionPolicy.draggedFrame` and `resizedFrame` against [apps/menubar/Sources/AppState.swift](/Users/cesar/code/codogotchi_p4_06/apps/menubar/Sources/AppState.swift:11). Both paths clamp through `FloatingFramePolicy.clamp`, so negative deltas, oversized growth, and off-screen movement all collapse back into the visible frame and Phase 04 min/max bounds.

`[held]` Invariant 3: I traced completion and display-change persistence through [apps/menubar/Sources/FloatingPetController.swift](/Users/cesar/code/codogotchi_p4_06/apps/menubar/Sources/FloatingPetController.swift:87). Drag/resize completion and screen-parameter changes both save a clamped frame while copying `state.isFloatingPetVisible`, and visible panels are re-shown at the persisted safe frame.

**Surface results**
`[probed]` Event-routing attack: `FloatingPetInteractionView.hitTest` now returns `self` for any in-bounds point, which blocks both `skView` and `resizeAffordanceView` from consuming the initiating mouse event. There is no AppKit integration test for this exact route, but the implementation itself directly closes the failure path that the earlier fix commit addressed.

`[probed]` Geometry attack: `resizeAffordanceRect(in:)` places the target at bottom-right in unflipped AppKit coordinates (`x = maxX - 28`, `y = minY`) and `draw(_:)` renders a restrained L-shape inside that box. I did not find a bottom-left/bottom-right inversion or an affordance size that dominates the pet.

`[probed]` Clamp attack: I checked negative and huge positive deltas through `resizedFrame` and `draggedFrame`, plus the shared clamp logic. The frame can shift left/down when growth would overflow the visible frame, but it remains clamped and usable; I did not find a path to exceed min/max size or leave the window off-screen.

`[probed]` Persistence attack: `persistFrameChange` and `saveClampedFrame` re-clamp before save and preserve the current visibility bit. On save failure they log and return without mutating controller state, so they do not write an unclamped frame or accidentally toggle visibility; the tradeoff is transient visible/persisted divergence, which is not a ticket-contract break by itself.

`[probed]` Lifecycle attack: the controller registers for `NSApplication.didChangeScreenParametersNotification`, removes the observer in `deinit`, and reuses `visibleFrameProvider()` at notification time rather than caching an old frame. I did not find a stale-frame path in the reclamp logic.

`[probed]` Rendering attack: replacing the content view with `FloatingPetInteractionView` preserves transparent `SKView` setup, scene presentation, and later `apply(state:visualMode)` updates. During live resize the `SKView` tracks bounds via autoresizing, and on interaction completion the controller re-shows the panel with the persisted frame, refreshing `scene.size`; I did not find a ticket-scope regression here.

`[probed]` Output stability across schema-version drift: the P4.05 ledger change is formatter-only array compaction in [docs/product/delivery/phase-04/reviews/P4.05-subagent-review.ledger.json](/Users/cesar/code/codogotchi_p4_06/docs/product/delivery/phase-04/reviews/P4.05-subagent-review.ledger.json:1). I did not find a schema or semantic change that would affect ledger consumers.

`[N/A - no CLI surface changed]` CLI flag/arg symmetry: this ticket does not touch CLI parsing or invocation contracts.

`[probed]` Error-class breadth in catch blocks: `saveClampedFrame` catches all save errors, logs, and leaves in-memory state untouched. That is broad but internally consistent; I did not find a second-order state corruption path caused by the catch.

`[probed]` Defensive layering at module boundaries: the panel clamps during interaction and the controller clamps again before persistence and display-change saves. That double boundary is sufficient for untrusted panel frames within this ticket’s scope.

`[probed]` Cross-file atomicity windows: completion updates persistence first and only then re-shows the visible panel at the saved frame. If save fails, the panel may remain where the user last dragged it until restart, but I did not find a path that writes bad state or loses the visibility flag.

`[probed]` Test-contract strength: tests cover pure hit-testing, clamp bounds, persisted frame updates, and display reclamp, but not AppKit event dispatch itself. Because the implementation now hard-overrides `hitTest` to return the container, I did not elevate that coverage gap to a finding.

`[probed]` Doc-vs-code drift in the ticket Rationale: the rationale’s behavioral claims match the implemented split between pure interaction policy, AppKit view handling, and controller persistence/reclamping. I found only minor wording drift noted below, not a contract mismatch.

**Actionable findings**
None.

**Advisory Observations**
In [docs/product/delivery/phase-04/ticket-06-drag-resize-affordance-cursor-persistence.md](/Users/cesar/code/codogotchi_p4_06/docs/product/delivery/phase-04/ticket-06-drag-resize-affordance-cursor-persistence.md:64), the contract note says display changes reclamp “through the same renderer-local app-state path,” but the actual persistence path is controller-owned in [apps/menubar/Sources/FloatingPetController.swift](/Users/cesar/code/codogotchi_p4_06/apps/menubar/Sources/FloatingPetController.swift:103). That is wording drift, not a behavioral defect.

The resize affordance cursor is `.resizeLeftRight` on a bottom-right corner affordance in [apps/menubar/Sources/FloatingPetPanel.swift](/Users/cesar/code/codogotchi_p4_06/apps/menubar/Sources/FloatingPetPanel.swift:247). The ticket only asked for a resize cursor “when practical on macOS,” so this is within scope, but it is horizontally suggestive rather than diagonal.

**Runner termination**
`runnerStatus`: `completed`
`terminatedReason`: Completed the targeted adversarial review from repo inspection and directly related code-path analysis without finding a ticket-scope correctness break.
