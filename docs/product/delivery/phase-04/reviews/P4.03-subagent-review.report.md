**Invariant results**

1. `[held]` - Checked `FloatingPetScene.resolveFrames(for:)` against `MenubarRenderer.resolveFrames(for:)`; both use Codex frames first, codogotchi frames second, then Codex idle fallback.
2. `[held]` - Checked `update(state:visualMode:)` and `advanceFrame()`; state changes reset `frameIndex` to `0` and repaint synchronously without panel/status-item/event-loop dependencies.
3. `[held]` - Checked scene initialization and node creation; it creates a transparent `SKScene`, named `pet` / `overlays` layers, and one sprite node, with no `NSPanel`, `NSWindow`, menu, HP, XP, loot, particle, or overlay UI.

**Surface results**

1. `[probed]` `FloatingPetScene.resolveFrames(for:)` - Codex-owned states win via `codexPet.frames(for:)`; SoA-owned states fall through to `codogotchiPet?.frames(for:)`; missing codogotchi frames fall back to Codex `.idle`.
2. `[probed]` `update(state:visualMode:)` / `advanceFrame()` - Repeated same-state updates keep the current index, visual-mode-only updates repaint the current frame, and state switches after advancement reset to frame `0`. No out-of-range access because painting indexes with modulo and advancing guards empty arrays.
3. `[probed]` `paintCurrentFrame()` / `fittedSpriteSize(for:)` - Empty frames clear texture without crashing; zero scene/image sizes avoid division. Found one desaturation fallback mismatch: `FloatingPetScene` emits the original colored frame when Core Image desaturation fails, while `MenubarRenderer` deliberately skips emitting colored output.
4. `[probed]` initialization and `size.didSet` - Initialization only constructs SpriteKit nodes and `CIContext`; `size.didSet` only relayouts and repaints. Overlay layer is named and positioned but has no visible children.
5. `[probed]` Xcode project wiring - `FloatingPetScene.swift` is in the app target sources, and `FloatingPetSceneTests.swift` is in the test target sources. I did not see unrelated target-setting/resource churn tied to SpriteKit.

Diff-derived surfaces:

1. `[N/A - no persisted schema or runtime JSON shape is introduced]` I checked the P4.02 ledger change with whitespace-insensitive diff; it is formatting-only.
2. `[N/A - no CLI flags or args changed]`
3. `[N/A - no new catch blocks were added]`
4. `[probed]` Defensive layering at module boundaries - Nil `codogotchiPet` and empty codogotchi results degrade to Codex idle; empty active frames clear texture/skip advance. The only defensive gap found is the desaturation failure fallback described below.
5. `[N/A - no runtime multi-file writes were added]`
6. `[probed]` Test-contract strength - Tests cover Codex idle, codogotchi state resolution, transition reset, missing codogotchi fallback, and layer sizing. They do not assert desaturated texture behavior or Core Image failure behavior.
7. `[probed]` Doc-vs-code drift - Ticket rationale says scene-only boundary, deferred panel/menu/live fanout, and empty overlay layer; implementation matches. No behavioral drift found.

**Actionable findings**

`apps/menubar/Sources/FloatingPetScene.swift:111` - In `.desaturated` mode, `desaturate(frame) ?? frame.cgImage` silently paints a colored frame when Core Image fails. This breaks the ticket outcome requiring desaturated mode or an equivalent failure visual consistent with the menu bar renderer; `MenubarRenderer` explicitly returns `nil` instead of emitting colored output on desaturation failure. Fix recommendation: make the scene’s desaturated branch skip replacing `spriteNode.texture` on failure, or apply another guaranteed failure visual, and log the skipped desaturation path.

**Advisory Observations**

The rationale has no doc-vs-code behavioral drift. One test gap remains: no test asserts desaturated output or the failure fallback behavior, which is why the colored-frame fallback can slip through despite the menu bar renderer having explicit guardrails.

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: Review completed from repository reads only; no files were modified.
