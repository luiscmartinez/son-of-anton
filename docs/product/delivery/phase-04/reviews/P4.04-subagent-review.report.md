**Invariant results**

1. `[broken]` Hidden initial state does not call `panel.show`, and normal toggles write app-state in covered paths, but save failures are swallowed so a toggle can change in-memory/panel state without persisting to `~/.codogotchi/app-state.json`.
2. `[held]` `MenubarMenu.build()` adds the floating toggle before Quit, keeps existing Open/Reveal/Quit targets, and refreshes the sender title after toggle.
3. `[held]` Static inspection shows `MenubarApp` remains LSUIElement/accessory, creates the floating controller only after assets load, and leaves renderer, polling, and transition-log setup independent of hide/show.

**Surface results**

1. `[probed]` `FloatingPetController.init` and `setFloatingPetVisible`: hidden initial state does not display; visible initial state displays saved/clamped frame. Save failure creates a UI/persistence split because `try? AppStateStore.save(nextState)` is ignored.
2. `[probed]` `MenubarMenu.build` and `toggleFloatingPet`: item order and title behavior match the contract; existing Open log folder, Reveal pet folder, and Quit remain callable.
3. `[probed]` `FloatingPetPanelController.show`: creates a borderless non-activating `NSPanel`, clear/transparent, floating level, SKView content, and presents `FloatingPetScene`; no ticket-relevant defect found.
4. `[probed]` `MenubarApp.applicationDidFinishLaunching`: menu creation happens after attempted asset load; renderer, polling, and transition log setup are not tied to floating visibility; asset-load failure leaves the disabled toggle path as documented.
5. `[probed]` Xcode project regeneration: new source/test files are in the correct app/test source phases; no tracked-file move or SoA migration requirement found.
6. `[probed]` Output stability across schema-version drift: app-state remains schema version 1 with `floating_pet.visible` and frame; no unversioned new field found.
7. `[N/A — no CLI flags, args, package scripts, or command contracts were changed by this ticket path]`
8. `[probed]` Error-class breadth in catch blocks: `setFloatingPetVisible` swallows all `AppStateStore.save` failures, which violates the persistence invariant.
9. `[probed]` Defensive layering at module boundaries: app-state frame is clamped on load and toggle; menu handles nil controller by disabling the item.
10. `[probed]` Cross-file atomicity windows: in-memory state changes before disk save and panel operations happen after an ignored save, so interruption/save failure can leave UI state and persisted state inconsistent.
11. `[probed]` Test-contract strength: tests cover hidden/visible initial state, normal persistence, menu order/title/actions; they do not cover save failure or nil-controller disabled-item behavior.
12. `[probed]` Doc-vs-code drift: ticket rationale matches the implementation on controller seam, deferred drag/resize, and disabled toggle on Mali load failure.

**Actionable findings**

`apps/menubar/Sources/FloatingPetController.swift:43`: `setFloatingPetVisible` assigns `state = nextState`, ignores `AppStateStore.save(nextState)` with `try?`, then shows/hides the panel. If `~/.codogotchi/app-state.json` cannot be written, the visible panel/menu state changes but the next launch reloads the old visibility. This breaks invariant 1 and finding-discipline clause 1. Fix by making persistence failure observable and preventing UI/in-memory state from advancing unless save succeeds, or by returning/logging an explicit failure and refreshing the menu from the persisted/current state; add a test with an unwritable `CODOGOTCHI_HOME` or injectable save closure.

**Advisory Observations**

`apps/menubar/Tests/MenubarTests/MenuItemsTests.swift`: nil-controller soft-degrade behavior is documented and implemented, but not directly tested; a focused test should assert the Show Floating Pet item is present, disabled, and still before Quit when assets fail to load.

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: Review completed; `bun run mac:test` passed with 106 tests and 0 failures.
