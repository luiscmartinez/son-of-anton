You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P4.04 Outcome:
- Codogotchi creates a transparent float-on-top panel that hosts the SpriteKit floating scene.
- The panel opens at the app-state frame or safe bottom-right default when no valid state exists.
- The menu includes a Show Floating Pet / Hide Floating Pet toggle.
- The toggle persists visibility to `~/.codogotchi/app-state.json`.
- Hiding the floating pet does not stop the menu bar micro-pet, polling, or transition logging.
- The app remains LSUIElement with no Dock icon and no conventional main window.

Rationale notes added during implementation:
- `MenubarMenu` takes a narrow `FloatingPetVisibilityControlling` seam.
- `FloatingPetController` owns renderer-local app-state persistence and delegates panel operations to `FloatingPetPanelManaging`.
- Direct AppKit panel behavior is kept out of `MenubarApp` so tests can observe show/hide without showing a real window.
- Drag/resize gestures and frame persistence after direct manipulation are deferred to P4.06.
- If Mali assets fail to load, the menu still includes the floating-pet item but disables it, preserving existing placeholder-icon launch behavior.

### Files touched

Implementation:
- `apps/menubar/Sources/FloatingPetController.swift`
- `apps/menubar/Sources/FloatingPetPanel.swift`
- `apps/menubar/Sources/MenubarApp.swift`
- `apps/menubar/Sources/MenubarMenu.swift`
- `apps/menubar/Codogotchi.xcodeproj/project.pbxproj`
- `docs/product/delivery/phase-04/ticket-04-floating-panel-shell-and-menu-toggle.md`

Tests:
- `apps/menubar/Tests/MenubarTests/FloatingPetControllerTests.swift`
- `apps/menubar/Tests/MenubarTests/MenuItemsTests.swift`

### Invariants to hold

1. When `FloatingAppState.isFloatingPetVisible` loads as `false`, `FloatingPetController` must not call panel display on initialization, and when it toggles visibility it must persist the new visibility to `~/.codogotchi/app-state.json`.
2. `MenubarMenu.build()` must include a floating-pet toggle before Quit whose title reflects the current controller visibility, and invoking it must call the controller without breaking Open log folder, Reveal pet folder, or Quit.
3. When pet assets load, `MenubarApp` must create a transparent float-on-top `NSPanel` hosting `FloatingPetScene` without changing the LSUIElement/accessory app model or stopping the existing menu bar renderer, polling, or transition log setup.

### Attack surfaces to probe

1. `FloatingPetController.init` and `setFloatingPetVisible` — visibility persistence/display ordering: does hidden initial state accidentally show a panel, or does toggling persist the wrong frame/visibility?
2. `MenubarMenu.build` and `toggleFloatingPet` — menu contract regression: does adding the toggle shift or disable existing menu actions, or leave the title stale after toggle?
3. `FloatingPetPanelController.show` — AppKit/SpriteKit shell correctness: is the panel actually borderless, transparent, floating, non-activating, and backed by an `SKView` presenting `FloatingPetScene` at the app-state frame?
4. `MenubarApp.applicationDidFinishLaunching` — app lifecycle integration: does delayed menu creation or floating controller creation interfere with renderer startup, demo/live polling, transition logging, or soft-degrade behavior when pet assets are missing?
5. Xcode project regeneration — target membership: are new source/test files included in the correct app/test targets without moving tracked files or requiring an SoA sync migration?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — app-state JSON schema stays on `APP_STATE_SCHEMA_VERSION = 1`; probe whether the new controller changes persisted shape or relies on a new unversioned field.
2. **CLI flag/arg symmetry** — no CLI flags or args are changed; report N/A unless you find an indirect script/package command contract touched by project regeneration.
3. **Error-class breadth in `catch` blocks** — `FloatingPetController.setFloatingPetVisible` uses `try? AppStateStore.save`; probe whether swallowing persistence failures violates the ticket's “toggle persists visibility” invariant.
4. **Defensive layering at module boundaries** — probe app-state frame clamping at the controller/panel boundary and optional controller behavior in `MenubarMenu` when pet assets fail to load.
5. **Cross-file atomicity windows** — toggling visibility updates in-memory state, writes app-state, and shows/hides panel; probe whether interruption or save failure can leave UI and persisted state observably inconsistent.
6. **Test-contract strength** — tests should cover hidden initial state, visible initial state, toggle persistence, title changes, existing menu actions, and optional controller/menu behavior where relevant.
7. **Doc-vs-code drift in the ticket Rationale** — compare `docs/product/delivery/phase-04/ticket-04-floating-panel-shell-and-menu-toggle.md` against actual implementation, especially disabled toggle on asset-load failure and deferred drag/resize.

### Diff context

The red commit added `FloatingPetControllerTests` and expanded `MenuItemsTests` around a missing controller seam, a floating menu item before Quit, title refresh, initial hidden state, and persistence. The implementation adds `FloatingPetController` for app-state load/save and panel show/hide, `FloatingPetPanelController` for a borderless floating `NSPanel` with an `SKView` hosting `FloatingPetScene`, and wires `MenubarApp` to create the controller after Mali/Codogotchi assets load. `MenubarMenu` now accepts an optional `FloatingPetVisibilityControlling`; when absent, the Show Floating Pet item is present but disabled. The Xcode project was regenerated from `project.yml`, and the ticket rationale records the controller seam and asset-load soft-degrade behavior.

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits):

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
